# Environment
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
*.sqlite
# Exception: the prod snapshot is a sanitised fixture for migration tests
# (regenerated by server/scripts/refresh_prod_snapshot.py). MUST stay tracked.
!server/tests/fixtures/prod_snapshot.sqlite

# Flutter/Dart
client/.dart_tool/
client/.packages
client/build/
client/.flutter-plugins
client/.flutter-plugins-dependencies
*.iml

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Hetzner
*.pem

# CI deploy — written by the workflow after rsync (see deploy/README.md).
# Never committed; present only inside /opt/.../releases/<sha>/ on the VPS.
server/.git_sha
