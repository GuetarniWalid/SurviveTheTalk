# generated: 2026-03-30
# last_updated: 2026-05-15 (Story 6.6 dev-story complete — `in-progress` → `review`. New files: `pipeline/exchange_classifier.py` (~210 LOC, ExchangeClassifier async judge with qwen-flash + 2.0s timeout + JSON-strict + fence/first-`{...}` fallback) and `pipeline/checkpoint_manager.py` (~270 LOC, CheckpointManager FrameProcessor mirrors EmotionEmitter gen-counter + cancel + cleanup pattern). Extended `patience_tracker.py` with `hang_up_line_survived` kwarg + new `_REASON_SURVIVED` constant + new public methods `apply_exchange_outcome(success)` (wires Story 6.4 dormant fail_penalty/recovery_bonus into the meter, bounded `[0, initial_patience]`, no-op during hang-up) and `schedule_completion(survival_pct)` (routes to `_run_hang_up` via `_pending_survival_pct` attribute; idempotent during hang-up). 3-way exit-line + survival_pct switch in `_run_hang_up`: silence/inappropriate use meter-ratio, survived uses the override (100). `_schedule_hang_up` validates `reason` against `_VALID_REASONS` 3-tuple — ValueError on unknown. Extended `scenarios.py`: `dict(preset)` → `copy.deepcopy(preset)` (retires DW line 357); 4 type/range validators on fail_penalty/recovery_bonus/escalation_thresholds/silence_hangup_seconds (retires DW line 350); loads `exit_lines.hangup` → both silence+inappropriate; loads `exit_lines.completion` → survived; new `load_scenario_checkpoints` (shape-validates id/hint_text/prompt_segment/success_criteria) + `load_scenario_base_prompt` (no _SPEAK_FIRST_DIRECTIVE suffix). `bot.py` pipeline now `transport.input → stt → transcript_user → emotion_emitter → context_aggregator.user() → checkpoint_manager → patience_tracker → llm → ...`; PatienceTracker(...) threaded with the 3 new hang_up_line_* kwargs from `patience_config`. **5 deviations**: #1 two distinct survival_pct formulas (100% for survived, meter-ratio for character_hung_up/inappropriate); #2 system-prompt swap via `llm._settings.system_instruction =` (NOT `LLMContext.set_messages` — OpenAI adapter prepends from _settings at every invocation, LLMContext is empty at boot, mutating it would produce 2 system messages with a warning); #3 `exit_lines.hangup` shared by silence+inappropriate (single source of truth in YAML); #4 last_character_line sourced from `llm_context.get_messages()` (NOT frame observation — LLM TextFrame flows DOWNSTREAM but CheckpointManager sits UPSTREAM of LLM); naming reconciliation epics.md `reason "completed"` → `reason "survived"` to match Story 6.5 D4 wire format. Tests: server 242 → 292 (+50 net), client 357 unchanged (zero net Flutter code per AC8 — `data_channel_handler` `default` branch already silently drops unknown envelope types). Déviation-#28 regression net landed in `test_bot_pipeline_wiring.py::test_checkpoint_manager_observes_finalized_TranscriptionFrame_via_real_pipeline_drive` — drives a TranscriptionFrame through a real Pipeline+PipelineTask+PipelineRunner (retires DW line 369). Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Smoke Test Gate sits with Walid for `review → done`. Awaiting `/commit`. Previously: Story 6.6 spec created — `backlog` → `ready-for-dev`. CheckpointManager Pipecat FrameProcessor + ExchangeClassifier async LLM service; mutates the shared LLMContext system message in-place on advance (set_messages mechanism); emits `checkpoint_advanced` envelopes via data channel; routes all-checkpoints-passed through new `PatienceTracker.schedule_completion(survival_pct=100)` with new `_REASON_SURVIVED` path + new `hang_up_line_survived` constructor kwarg sourced from YAML `exit_lines.completion`; new `PatienceTracker.apply_exchange_outcome(success: bool)` finally wires the Story 6.4 dormant `fail_penalty` / `recovery_bonus`. `resolve_patience_config` switches `dict(preset)` → `copy.deepcopy(preset)` (deferred-work line 357) + adds type/range validation on fail_penalty/recovery_bonus/escalation_thresholds/silence_hangup_seconds (deferred-work line 350). New `load_scenario_checkpoints` + `load_scenario_base_prompt` helpers in scenarios.py. Pipeline-drive contract test in test_bot_pipeline_wiring.py is the Déviation-#28 regression net (deferred-work line 369). Zero net Flutter code (Story 6.7 owns CheckpointStepper UI consumer). Hard prerequisite: 6.5 must be `done` (verified ✅). 3 up-front deviations: (#1) two distinct survival_pct formulas — 100% for `survived`, meter-ratio for `character_hung_up`/`inappropriate_content`; (#2) system-prompt swap via `LLMContext.set_messages`, NOT `_settings.system_instruction`; (#3) YAML `exit_lines.completion` → `hang_up_line_survived`, `exit_lines.hangup` shared by both silence/inappropriate. Naming alignment: epic spec line 1193 amended from `reason "completed"` to `reason "survived"` to match wire format already shipped in Story 6.5 D4 widening. Previously: 2026-05-13 — Story 6.5 code review complete; D1-D6 decisions resolved by Walid-delegated trust pass + 27 patches applied — flipped to `ready-for-dev`. Drilled deep on POST /calls/{id}/end endpoint contract (4-reason Literal whitelist, 404 on cross-user, BEGIN IMMEDIATE for status flip, idempotent), migration 008_call_sessions_status.sql (status column with CHECK constraint, historical-row backfill = 'completed' to preserve cap counter), background janitor sweep (asyncio lifespan task, 15-min cadence, fail-soft), Popen-rollback LiveKit cleanup (livekit_delete_room helper), count_user_call_sessions_* status filter (WHERE status IN ('pending','completed')), client CallRepository.endCall + fire-and-forget _endCallSilently from all 3 CallBloc exit paths (user_hung_up / character_hung_up / inappropriate_content / network_lost), NoNetworkScreen UX-DR7 rewrite (WiFi-barred icon top-right, character avatar circle, "Call failed" SemiBold + "No network available" Regular, hang-up button bottom; ZERO new color tokens). 10 deviations documented up-front. ADR-003 numbering drift formally retires deferred-work.md items 121, 275-289, 322. Hard prerequisite: 6.4 must be `done` before opening dev-story 6.5.)
# project: surviveTheTalk2
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-03-30
last_updated: 2026-05-20 (Story 6.8 dev-story code complete — `in-progress` → `review`. Phases 1+2+Phase 3 Waiter-only all green — COHERENCE_CHARTER constant in prompts.py (~200 tokens, 5 conversation-memory rules) threaded through new required `coherence_charter` kwarg on `CheckpointManager.__init__` + 3-piece swap composition `base + charter + segment` (matches AC7 exactly + WARN-on-duplicate guard for AC15 box 3). bot.py composes initial_system_prompt with charter slotted between base_prompt and first_segment (3-branch defensive composition); user_speech_timeout 1.8→0.6 with full empirical-tuning-context comment + VAD `stop_secs=0.8` audit block (net silence-to-turn-end ≈ 1.4 s, under PRD 2 s ceiling); exchange_classifier timeout 2.0→1.0 (+ HTTP 1.8→0.8 mirror); new `pipeline/latency_probe.py` (~85 LOC env-var-gated LatencyProbe FrameProcessor, 2 instances wired llm↔transcript + tts↔output for AC3 measurement, inert in prod). Waiter YAML — explicit Menu inventory block (main+drinks+desserts), `drink` success_criteria accepts Coke/Pepsi/Sprite/sparkling synonyms + explicit refusals, `clarify` accepts "as I said before"-style re-statements; inline call_id=118 comments. Tests — server 325 → 329 (+4 net new — charter behavioral + WARN-on-dup guard + bot import assertion + bot wiring source-text). 3 new deviations — #5 LatencyProbe ships opt-in prod, #6 3-branch initial_system_prompt, #7 WARN-on-duplicate inside _classify_and_advance. Pre-commit gates ALL green — ruff check + ruff format + pytest (329) + flutter analyze + flutter test (373 unchanged). Sprint-status + story Status flipped to `review`; `review → done` reserved for Walid post-smoke-gate. Manual items deferred to Walid — Tasks 1.3 empirical user_speech_timeout floor, 3.2-3.3 LATENCY_PROBE=1 LLM→TTS gap measurement, 8.3 4-scenario inventory design face-to-face, 10 Cartesia voice audition+selection, 11+12 10 device calls Pass A+B calibration on Pixel 9 Pro XL, 14 14-box Smoke Test Gate. Awaiting `/commit`. Previously 2026-05-19 (Story 6.8 spec created — `backlog` → `ready-for-dev`. SCOPE EXPANSION from epics.md's "Post-CheckpointManager Scenario Calibration" (YAML-only) to a 3-phase launch-blocker hardening pass driven by Story 6.7 smoke test findings on Pixel 9 Pro XL (call_id=118, 2026-05-19). **Phase 1 — Latency floor under PRD ceiling** — tune `user_speech_timeout` 1.8→~0.6s (single biggest win — PRD hard ceiling is 2s, "concept dead" per requirements table), audit VAD `stop_secs=0.8` redundancy, verify LLM→TTS streaming overlap via 3-point instrumentation, cap Dev #7 ExchangeClassifier timeout 2.0→1.0s. Target: median perceived latency ≤1500ms (FR45 ops alert threshold), p95 ≤2000ms. **Phase 2 — Conversation coherence charter** — new `COHERENCE_CHARTER` module-level constant in `prompts.py` (~200 tokens), threaded as a required `coherence_charter` parameter through `CheckpointManager.__init__` + composition into every `_settings.system_instruction` swap (Story 6.6 Deviation #2). Charter encodes 5 scenario-agnostic memory rules: confirmed-items locked, user-reference-prior-trusts-conversation, menu-listings-from-base_prompt-verbatim, no-deny-prior-listing, history-wins-on-disagreement. Walid's explicit ask: "le gardien ne doit pas être au niveau de chaque scénario mais plutôt au niveau général". **Phase 3 — Per-scenario YAML calibration** (original 6.8 scope) — enumerate inventory in each scenario's `base_prompt` (kills hallucinated menus), audit Waiter `drink`+`clarify` `success_criteria` for over-strictness (call_id=118 saw 4 consecutive `checkpoint_unmet drink` even after user said "Coke"), select non-null `tts_voice_id` for Mugger/Girlfriend/Cop/Landlord, run Pass A + Pass B for all 5 scenarios via existing `TranscriptLogger`+`score_transcript.py` tooling, populate `calibration.pass_a/pass_b` blocks with verdict against target bands (Mugger/Girlfriend 35-55%, Cop/Landlord 15-35%, Waiter 60-90%), tune nullable difficulty overrides if outside band ±10%. **15 ACs total, 14-box smoke gate** — deploys+latency-floor-verified+coherence-regression-replay (call_id=118 Cola test)+menu-consistency-across-turns+10 calibration runs (5 scenarios × 2 passes)+stepper-no-regression+server-logs-clean+FR45-threshold-validated. Hard prerequisites: 6.6 done ✅, 6.7 review/done ✅. 4 up-front deviations documented: (#1) scope expansion vs epics.md line 1232-1260, (#2) phase ordering puts arch fixes BEFORE calibration (calibration on broken pipeline = broken numbers), (#3) charter as Python constant not YAML-overridable, (#4) `user_speech_timeout` value is empirical not theoretical. Server-only story (zero Flutter changes). Test target: server ~327 (baseline 325 + 2 new charter tests). Memory pointers: `feedback_latency_kill_criterion_exceeded.md` + `feedback_coherence_must_be_system_wide.md` + deferred-work.md lines 16-45 (2 🚨 entries at top). **MVP launch is gated on this story** — Phases 1+2 close the PRD's "concept dead" + Walid's "app entière est en jeu" findings. Previously 2026-05-18: Story 6.7 spec created — `backlog` → `ready-for-dev`. CheckpointStepper Flutter overlay widget consuming Story 6.6's `checkpoint_advanced` data-channel envelope. New files: `client/lib/features/call/views/widgets/checkpoint_stepper.dart` (~250 LOC widget — adaptive 20×20/14×14 circles, 8-token spacing block in AppSpacing) + `client/lib/features/call/services/checkpoint_advanced_payload.dart` (~30 LOC typed value class). Extends `DataChannelHandler` with `onCheckpointAdvanced` typed callback (replaces the existing silent-default-branch behaviour at lines 129-138). Extends `_CallScreenState` with `ValueNotifier<CheckpointSnapshot?>` (UI-only state on the State, NOT on the bloc — same pattern as `_awaitingPlaybackIdle` from Story 6.4 and `_canvasInFallback` from Story 6.2). Server-side: tiny 1-method addition to `CheckpointManager.emit_initial_state()` + 1-line wiring in `bot.py::on_first_participant_joined` so the stepper has initial data on call connect (uses the SAME `checkpoint_advanced` envelope shape — single-wire-format approach). 3 up-front deviations: (#1) NO `v: 1` schema version on envelope (retires deferred-work line 396 with documented decision — additive evolution via `data.{}`); (#2) client reconciles stepper state from `call_end.checkpoints_passed` to close cancel-mid-flight envelope-lost race (retires deferred-work line 406); (#3) conditional Semantics fix if Rive's existing `Semantics(button:true,label:'End call')` traps screen-reader focus — dev-decided at impl time. Hard prerequisite: 6.6 must be `done` (verified ✅). 8 ACs total, ~22 net new client tests + ~2 net new server tests. Pre-commit gates green expected: ruff check + ruff format + pytest (target ≥323) + flutter analyze + flutter test (target ≥379). Smoke Test Gate (11 boxes — initial-state envelope fires, stepper renders on connect, advances animate, adaptive sizing N/A if no scenario ≥7 checkpoints today, TalkBack announces) sits with Walid for `review → done` per Story 6.5 D6. Previously 2026-05-18: Story 6.6 OFFICIALLY DONE — smoke gate fully validated end-to-end on Pixel 9 Pro XL post Dev #29 fix. Happy-path call_id=108 completion in 98s with all 6 checkpoints + Dev #7 suppression + reason=survived; off-topic call_id=109 with checkpoint_unmet on greet + fail_penalty -15. Dev #29 emerged post-deploy: PatienceTracker was sitting downstream of context_aggregator.user() since Story 6.4, but pipecat 0.0.108 LLMUserAggregator absorbs TranscriptionFrames internally — `_cancel_silence_timer()` on user-speech observation was dormant for 1.5 epics. Surfaced on the first 6.6 call (first scenario where user MUST speak mid-ladder), fix amended into the Story 6.6 commit as Dev #29 mirroring CheckpointManager's Dev #5. Pre-commit gates green (321 server tests, ruff clean, flutter 357 unchanged). Force-pushed at SHA 85fdd66. One story = one commit.). Previously 2026-05-15 (Story 6.6 dev-story complete — `in-progress` → `review`. New files: `pipeline/exchange_classifier.py` (~210 LOC) + `pipeline/checkpoint_manager.py` (~270 LOC). Extended `patience_tracker.py` with `hang_up_line_survived` kwarg + `_REASON_SURVIVED` + `apply_exchange_outcome(success)` + `schedule_completion(survival_pct)` + 3-way exit-line/survival_pct switch in `_run_hang_up` + `_VALID_REASONS` validation. Extended `scenarios.py`: deepcopy fix + 4 validators + `exit_lines.hangup`/`exit_lines.completion` loading + `load_scenario_checkpoints` + `load_scenario_base_prompt`. `bot.py` pipeline now `context_aggregator.user() → checkpoint_manager → patience_tracker`. **5 deviations**: #1 two distinct survival_pct formulas; #2 `llm._settings.system_instruction =` direct mutation (NOT `LLMContext.set_messages` — OpenAI adapter prepends from settings at every invocation, LLMContext empty at boot, mutating it would produce 2 system messages with a warning); #3 `exit_lines.hangup` shared by silence+inappropriate; #4 last_character_line sourced from `llm_context.get_messages()` (NOT frame observation — LLM TextFrame flows DOWNSTREAM but manager sits UPSTREAM of LLM); naming reconciliation epics.md line 1193 `reason "completed"` → `reason "survived"` for Story 6.5 D4 wire-format alignment. Tests: server 242 → 292 (+50 net new), client 357 unchanged (zero net Flutter per AC8). Déviation-#28 regression net landed in `test_bot_pipeline_wiring.py` — drives TranscriptionFrame through real Pipeline+PipelineTask+PipelineRunner (retires DW line 369). DW lines 350+357 retired by scenarios.py changes. Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Smoke Test Gate (live VPS deploy + journalctl proofs) sits with Walid for `review → done` per Story 6.5 D6. Awaiting `/commit`. Previously 2026-05-15: Story 6.6 spec created — `backlog` → `ready-for-dev`. CheckpointManager Pipecat FrameProcessor + ExchangeClassifier async LLM service (qwen-flash, 2.0s timeout); mutates the shared LLMContext system message in-place on advance; emits `checkpoint_advanced` envelopes on the data channel; routes all-checkpoints-passed through new `PatienceTracker.schedule_completion(survival_pct=100)` + new `_REASON_SURVIVED` path + new `hang_up_line_survived` kwarg (sourced from YAML `exit_lines.completion`); new `PatienceTracker.apply_exchange_outcome(success)` finally wires Story 6.4 dormant `fail_penalty`/`recovery_bonus`. `resolve_patience_config` deepcopy fix + 4-field type/range validation (retires deferred-work lines 350, 357). Pipeline-drive contract test in test_bot_pipeline_wiring.py is the Déviation-#28 regression net (retires deferred-work line 369). Zero net Flutter code (Story 6.7 owns CheckpointStepper UI). Hard prerequisite: 6.5 done ✅. 3 up-front deviations: (#1) two distinct survival_pct formulas (100% for `survived`, meter-ratio for `character_hung_up`/`inappropriate_content`); (#2) system-prompt swap via `LLMContext.set_messages`, NOT `_settings.system_instruction`; (#3) YAML `exit_lines.completion` → `hang_up_line_survived`. Naming alignment: epic line 1193 amended `reason "completed"` → `reason "survived"` to match wire format from Story 6.5 D4. Previously 2026-05-13: Story 6.5 code review complete; D1-D6 decisions resolved by Walid-delegated trust pass + 27 patches applied. AC7/AC10 amended for Deviation #12 hybrid (EmpatheticErrorScreen now accepts bodyOverride/retryLabel/semanticsLabel; NoNetworkScreen wires call-context copy + 'Go back' + 'Close' Semantics label). Server: rollback path FLIPs to 'failed' instead of DELETE (D3 / Deviation #14); EndCallIn.reason Literal widened with 'survived' (D4 / Deviation #13); EndCallOut.status narrowed to Literal['completed','failed'] (Deviation #21); `/end` ownership check moved before BEGIN IMMEDIATE (Deviation #19); aiosqlite locked-DB → 503 with Retry-After (P17); defensive started_at parse (P20); idempotent NULL duration_sec logs ERROR (P6); `_safe_livekit_delete_room` consolidates timeout + shield + log-only-on-cancel (Deviation #22); janitor uses julianday() comparison (Deviation #20) + failure-streak backoff + bounded shutdown (Deviation #23). Client: 3 exit paths funnel through `_fireEndCall` with `_endPostFired` guard + `_pendingEndCalls` set awaited in close() (Deviation #17); connect-failure catches now POST 'network_lost' (Deviation #16); 'unknown' reason coerced to 'character_hung_up' client-side (Deviation #18); `_endCallSilently` log line scrubbed of raw exception text (P18 — PII protection). Tests: server +29 (P25 rollback ordering, P20 malformed started_at, P10 failed-row idempotent, P6 NULL duration log, P17 503, P22 no-write-lock-on-404, D4 survived, P23 +00:00 offset sweep, P24 cost_cents NULL); client +6 (P1 connect throw + timeout + mic, P4 double-POST guard, P5 unknown coercion, P3 close-awaits-in-flight). D5: prod_snapshot stays hand-doctored, tracker entry added to deferred-work.md for post-deploy refresh. D6: spec ligne 412 amended to formalise "deploy-gates are stop-ship for review→done, not in-progress→review". Smoke Test Gate still owned by Walid. Story 6.4 status unchanged.)
project: surviveTheTalk2
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Voice Pipeline Proof of Concept (Phase 0)
  epic-1: done
  1-1-initialize-monorepo-and-deploy-server-infrastructure: done
  1-2-build-pipecat-voice-pipeline-with-sarcastic-character: done
  1-3-create-minimal-flutter-app-with-voice-call: done
  1-4-validate-poc-kill-gates: done
  epic-1-retrospective: done

  # Epic 2: UX Design Completion & Asset Creation
  epic-2: done
  2-1-design-onboarding-flow-screens: done
  2-2-design-first-call-incoming-call-animation: done
  2-3-design-call-ended-transition-screen: done
  2-4-design-debrief-screen: done
  2-5-design-paywall-screen: done
  2-6-create-rive-character-puppet-file: done
  2-7-create-visual-assets: done
  epic-2-retrospective: done

  # Epic 3: Scenario Content Design & Authoring
  epic-3: done
  3-0-build-scenario-calibration-testing-tools: done
  3-1-define-scenario-structure-and-authoring-format: done  # review corrections applied
  3-2-create-launch-scenarios: done  # review corrections applied, calibration deferred to post-Epic 6
  epic-3-retrospective: done

  # Epic 4: User Onboarding & Authentication
  epic-4: done
  4-1-restructure-flutter-project-to-mvp-architecture: done  # review corrections applied
  4-1b-implement-design-system: done  # review corrections applied 2026-04-16 (WCAG fixes + test hardening + naming + OFL)
  4-2-build-fastapi-server-with-passwordless-auth-system: done  # review corrections applied 2026-04-16
  4-3-build-email-authentication-flow-in-flutter: done  # review corrections applied 2026-04-21
  4-4-build-consent-ai-disclosure-and-microphone-permission-flow: done  # review corrections applied 2026-04-22
  4-5-build-first-call-incoming-call-experience: done  # review corrections applied 2026-04-23
  epic-4-retrospective: done  # 2026-04-23 retro completed, doc: epic-4-retro-2026-04-23.md

  # Epic 5: Scenario Browsing & Progression
  epic-5: done
  5-1-build-scenarios-api-and-database: done  # code review completed 2026-04-24: 15 patches applied (seeder hardening, route ValidationError→SCENARIO_CORRUPT, stricter ordering tests, parametrised corruption coverage, ok_list delegates to ok, ScenariosListOut meta), 126 pytest green, pipecat.service reverted to www-data (5.1-CI-deploy sub-tag), 2 infra issues deferred to deferred-work.md
  5-2-build-scenario-list-screen-with-scenariocard-component: done  # code review 2026-04-27: 6 decisions resolved, 7 patches applied (1 deferred to Story 6.1 — back-press blocking on /call), 1 deferred item closed (debrief back-button), 156 flutter tests green, flutter analyze clean
  5-3-build-bottomoverlaycard-and-daily-call-limit-enforcement: done  # code review 2026-04-28: 3 decisions + 8 patches applied (TOCTOU BEGIN IMMEDIATE + busy_timeout, BOC visual drift documented in AC6, Semantics gate, mounted check, staticContentHeight recompute, conditional padding, frozen clock for paid-tier tests, AppSpacing tokens, buildWhen removed, predicate-based test finder), 8 items deferred to Epic 6/8/etc.
  5-4-build-content-warning-display-for-intense-scenarios: done  # code review 2026-04-29: 3 decisions resolved + 6 patches applied (rename dialog→sheet, AC7 minimumSize/tapTargetSize removal, release-build null-guard, AC7 320×1.5 textScale test surfaced 390px overflow → fixed via SingleChildScrollView, scrim-dismiss screen-integration test, deviation #5 declared for YAML edits), 6 items deferred (token discipline, double-tap re-entry, mid-dialog limit bypass, RTL, back-press untested, AA contrast claim), 208 flutter tests green
  5-5-refactor-scenario-list-error-screen-with-empathetic-ux: done  # code review 2026-04-29: 7 decisions resolved (6 accept + spec amendments, 1 status quo, 1 deferred HIGH-PRIORITY auth 401), 7 patches applied (ApiException.statusCode propagation, Semantics on HOLD ON/HEADS UP, FlutterError.onError overflow capture, pumpEventQueue × 8, comment fixes, deviation #4 withdrawn), 1 dismissed (false positive). 213 flutter tests green, analyze clean, server pytest 145 green. ⚠️ Auth 401 silent loop deferred to deferred-work.md as MUST-FIX-BEFORE-MVP-LAUNCH
  epic-5-retrospective: done  # 2026-04-29 retro completed, doc: epic-5-retro-2026-04-29.md

  # Epic 6: Animated Call Experience
  epic-6: in-progress
  6-1-build-call-initiation-from-scenario-list-with-connection-animation: done  # code review 2026-04-30: 17 patches applied + 1 from D2 (red AppToast for generic 5xx); 2 decisions resolved; 4 deferred (already in deferred-work.md). Smoke gate fully green at SHA 28fd11c (8/8 boxes); 234 client tests + 154 server tests green; flutter analyze + ruff clean.
  6-2-build-call-screen-with-rive-character-canvas: done  # code review 2026-04-30: 14 patches applied (3 HIGH: Semantics scope, duplicate Rive listener, missing onHangUp event test; 3 MEDIUM: onFallback idempotency, FileLoader leak guard, StateMachine reference capture; 8 LOW: bang-fallback, AC9 sibling assertion, debug asserts, real-asset tests, catalog test, CallError rationale, rootNavigator pop symmetry, _popScheduled guard); 4 decisions resolved (catalog names approved, sigma 3 confirmed intentional DoF — saved to memory, JPG avatar visually validated, FPS check OK on Pixel 9 Pro XL); 6 deferred (already in deferred-work.md). Final: flutter analyze clean, 258 tests green (+14 from baseline 244).
  6-3-implement-emotional-reactions-and-lip-sync-via-data-channels: done  # code review 2026-05-01: 16 patches applied (3 HIGH: cleanup hook + pts_ns is None + httpx cleanup investigated; 10 MED: emit-layer rejection filter + generation counter + finalized=False default + regex Markdown fence + reserved-emotion test + subscribe-once assertion + API key validation + mounted check + cross-contract tests + Deviation #5 doc + Box 6 already done; 3 LOW: client allow-list mirror + prune-releases.sh `$resolved` + DataChannelHandler try/catch); 8 deferred to deferred-work.md; 4 dismissed (per-spec await design, Flutter sync-dispose pattern, status-flip-with-Note-8, OpenRouter slug smoke-proven). 198 server tests + 275 client tests green; flutter analyze + ruff clean. Smoke gate revealed catastrophic word-level lip-sync 2026-05-01 → fix shipped via 6-3b before close-out.
  6-3b-improve-lip-sync-from-word-to-syllable-level: done  # 2026-05-12 architectural pivot + code review. Server-side syllable-level viseme emission (original scope) abandoned after smoke trace showed data-channel lag dwarfing chunk-level precision — SCTP slow-start put visemes 2-3 s behind audio on cellular. Replaced with client-side native PCM-buffer analysis on the audio thread: Android `AudioClockChannel` hooks flutter_webrtc's playback callback and feeds each chunk to `FormantVisemeAnalyzer` (RMS + ZCR + 512-pt FFT band-energy formant detection over F1/F2). Sync is now structural (analysis lives on the playback thread) and cannot drift. Dropped: `viseme_emitter.py` + tests, `audio_clock_bridge.dart`, data-channel viseme path on both sides. OVRLipSync was prototyped end-to-end (JNI wrapper, NDK config, .so shipping) and rejected — Meta's `libOVRLipSync.so` isn't 16 KB-aligned, would fail Google Play. Code review 2026-05-12: 4 decisions resolved + 11 patches applied (CRIT native-callback detach + post-cancel race; HIGH audioFormat guard + channelCount==0 + true stereo downmix; MED reflection getDeclaredField + Dart cast safety; LOW bandEnergy exclusive bounds + strengthened test + story-tag drift 6.3d/e→6.3b across 11 files). New JVM unit-test source set at `client/android/app/src/test/kotlin/` with FftTest (4) + FormantVisemeAnalyzerTest (6) — first DSP-level test coverage. 2 items deferred to deferred-work.md (latent data.size check, brittle flutter_webrtc reflection coupling). 279 client tests + 175 server tests + 10 JVM Kotlin tests green; flutter analyze + ruff + Android assembleDebug clean. Walid validated lip-sync visually on Pixel 9 Pro XL 2026-05-12.
  6-4-implement-silence-handling-and-character-hang-up-mechanic: done  # code review 2026-05-13: 17 patches applied + 4 decisions resolved (D1 constructor restored 4 dormant kwargs / Deviation #15; D2 hang-up tail asyncio.Event+wait_for / Deviation #16; D3 emotion debounce omission accepted / Deviation #17; D4 anti-pattern reworded for control-signal carve-out). Server patches: _hang_up_in_progress try/finally, handle_playback_idle concurrent-ladder guard, TranscriptionFrame finalized gate, abuse_classifier try/except, BSF direction guard, validate initial_patience>0 (constructor + resolve_patience_config), extended config log line (8 fields), reordered empty-text cancel. Client patches: HangUpPressed clears _remoteEndDrainTimer+_remoteEndPending, _onPlaybackDrained emit.isDone re-check, _publishPlaybackIdle null-log + sync try/catch, VisemeScheduler Dart-side dedup, RoomDisconnectedEvent mirrors _roomDisconnected, DataChannelHandler logs on fallback. 16 items deferred to deferred-work.md (dev OQ6/OQ7/DW1/DW5 + other pre-existing concerns). 200 server tests (+7 net new: P3, P5, P6, P7, P9, P17, resolve_patience_config validation) + 301 client tests (+3 net new: P1, P14, P12) green; ruff + flutter analyze clean. Awaiting /commit.
  6-5-build-voluntary-call-end-and-no-network-screen: done  # 2026-05-15 closed. Shipped well beyond the original spec: 28 deviations total (#1-#12 up-front, #13-#23 from /bmad-code-review, #24-#26 from post-deploy PD1-PD3 Pixel 9 Pro XL device testing, #27 from user-design feedback, #28 from Story 6.4 silent-regression discovery). Server: migration 008 (status column + CHECK) + 009 (gifted column + CHECK) + POST /calls/{id}/end (auth, ownership-before-BEGIN-IMMEDIATE, idempotent, 503-on-busy, julianday() janitor with failure-streak backoff, _safe_livekit_delete_room shield+5s timeout, bounded 30s shutdown) + 3-per-day gift quota shared across network_lost / character_hung_up<30s / inappropriate_content<30s + Déviation #28 1-word fix DOWNSTREAM→UPSTREAM in patience_tracker.py + cross-reference contract test + new server/CLAUDE.md. Client: connectivity_plus mid-call detection + EndCallRetryStorage + EndCallRetryService with 3 drain triggers (bootstrap, onConnectivityRegained, AppLifecycleState.resumed) + EndCallResult model + CallEndedNoticeScreen 4 copy variants (English, optimistic) + EmpatheticErrorScreen overrides. Tests: server 200→242 (+42), client 279→357 (+78). All pre-commit gates green. Smoke Test Gate validated end-to-end on Pixel 9 Pro XL 2026-05-15 — Test 1 (network_lost row 92 gifted), Test 2 (character_hung_up row 96 with patience tracker FINALLY firing post #28), Test 3 (user_hung_up row 97 not gifted). Mini-review by Auditor agent 2026-05-15 returned no blockers (only bookkeeping rot — all 4 findings applied). Deploy: commits f674eaf + 0cefcd7 + #27/#28 follow-ups via CI/CD.
  6-6-build-checkpointmanager-and-checkpoint-aware-exchangeclassifier: done  # smoke-gate validated 2026-05-18 on Pixel 9 Pro XL post-redeploy at SHA 85fdd66: call_id=108 happy-path completion (6/6 checkpoints, reason=survived, 98s, Dev #7 frame suppression confirmed) + call_id=109 off-topic checkpoint_unmet (greet stayed, fail_penalty -15 applied). Dev #29 added post-deploy (PatienceTracker moved BEFORE context_aggregator.user() — same root cause as Dev #5; pipecat 0.0.108 LLMUserAggregator consumes TranscriptionFrames internally, so _cancel_silence_timer() was dormant since Story 6.4; surfaced on the first 6.6 call because it's the first scenario where the user MUST speak mid-ladder). 9 consecutive silence ladder cancelled events confirmed Dev #29 fix live. All 12 smoke gate boxes green. Code review 2026-05-18: 5 decisions resolved + 14 patches applied. D1 asyncio.Lock around terminal-turn path + post-acquire re-check (race where concurrent process_frame cancels in-flight task → Dev#7 suppression silently broke); D2 kept async warning-band UX (option A — realistic restaurant flow, Walid confirmed); D3 prompt-injection resistance via XML <user_response>/<character_line> tags; D4 kept asymmetric finalized default + cross-ref comments + server/CLAUDE.md §1 doc; D5 fail-loud RuntimeError at manager init on missing llm._settings.system_instruction (pipecat API drift detection). Patches: P1 PatienceTracker.__init__ rejects None/bool fail_penalty/recovery_bonus; P2 dismissed (str.format single-pass substitution doesn't crash on user braces); P3 isinstance(x, bool) guard in resolve_patience_config; P4 exit_lines list type-check BEFORE or {} fallback; P5 generation-guard stale-drop logger.debug; P6 _warning_emitted flag moved INSIDE _emit_patience_warning post-push + dual-guard via _warning_task done() check; P7 _pending_survival_pct cleared post-consume in _run_hang_up; P8 dismissed (defensive _VALID_REASONS check is correctly placed); P9 silence_prompt_seconds + silence_penalty validators; P10 _SPEAK_FIRST_DIRECTIVE substring assertion in load_scenario_base_prompt; P11 conftest.py narrowed to (ImportError, ModuleNotFoundError, AttributeError); P12 sprint-status test count corrected; P13 Dev#7 caveat comment on test_pass_through_for_all_frame_types; P14 stale-verdict test docstring rewritten (cancel + generation guard belt-and-braces). 18 items deferred to deferred-work.md "code review of story-6.6 (2026-05-18)"; 28 dismissed as noise/handled/documented-deviations. Tests: server 292 → 321 (+29 net new); client 357 unchanged. Pre-commit gates green: ruff check + ruff format + pytest. Smoke Test Gate per Story 6.5 D6 remains Walid-owned post-deploy.
  6-7-build-checkpointstepper-overlay-for-call-screen: done  # 2026-05-20: Smoke Test Gate 11/11 validated on Pixel 9 Pro XL post Deviation #8 server fix (PatienceTracker.set_checkpoints_passed wiring — retires Story-6.4-era hardcoded `checkpoints_passed=0` in `call_end` envelope, retires deferred-work line 65/74/213 TODO). Code review: 3 decisions resolved + 16 patches applied + 5 deferred + 4 dismissed. Tests: server 326 (+1 net from review), client 373.
  6-9-dtln-noise-suppression: review  # 2026-05-20 Story 6.9 created + dev complete + flipped `in-progress → review`. New file `pipeline/dtln_audio_filter.py` (~140 LOC) — `DTLNAudioFilter(BaseAudioFilter)` wraps livekit-plugins-dtln (Aloware MIT, ~4 MB ONNX, server-side DTLN noise suppression). `bot.py` imports + instantiates + threads via `LiveKitParams(audio_in_filter=audio_in_filter)`. `pyproject.toml` adds `livekit-plugins-dtln>=0.1.5,<0.2.0`; `uv.lock` regenerated (livekit 1.1.3→1.1.8, livekit-protocol 1.1.3→1.1.9, livekit-agents 1.5.11 added, onnxruntime + opentelemetry stack + grpcio + av + sounddevice transitively pulled). 4 up-front deviations — #1 server-side denoising not client-side (vs Krisp $$/Picovoice $6K-year/DeepFilterNet iOS-gap rejected after web research), #2 calling `_process` directly is the public hook per rtc.FrameProcessor abstract base, #3 strength=0.5 default for natural timbre (full-strength sounds thin), #4 passthrough on every error (init crash, mid-call exception, stop-after-error) per server/CLAUDE.md "better than broken". Tests — server 329 → 341 (+12 net new — 11 DTLN unit tests covering round-trip/disabled-passthrough/init-failure-passthrough/FilterEnableFrame-toggle/filter-exception-passthrough/stop-releases-resources/strength-clamped + 1 wiring source-text guard). Pre-commit gates green — ruff check + ruff format + pytest 341 + flutter analyze + flutter test (zero Flutter changes). 6-box Smoke Test Gate reserved for Walid post-deploy (baseline calm + café-bondé real noise test + optional A/B + server CPU + journalctl clean). Awaiting `/commit` + VPS deploy + smoke gate.
  6-8-post-checkpointmanager-scenario-calibration: review  # 2026-05-20 dev-story code complete — `in-progress` → `review`. New file: `pipeline/latency_probe.py` (~85 LOC, env-var gated LatencyProbe FrameProcessor — opt-in via LATENCY_PROBE=1 for smoke-gate AC3 measurement, zero noise in prod). Modified `prompts.py` (+COHERENCE_CHARTER ~200 tokens, 5 conversation-memory rules); `checkpoint_manager.py` (new required `coherence_charter` kwarg + 3-piece swap composition `base + charter + segment` + WARN-on-duplicate guard for AC15 box 3); `bot.py` (import COHERENCE_CHARTER + LatencyProbe + TextFrame + OutputAudioRawFrame; load scenario data early; 3-branch initial_system_prompt composition; `user_speech_timeout 1.8→0.6` with full tuning-context comment + VAD audit block; thread coherence_charter into CheckpointManager; wire 2 LatencyProbe instances); `exchange_classifier.py` (_CLASSIFIER_TIMEOUT_SECONDS 2.0→1.0 + _HTTP_TIMEOUT_SECONDS 1.8→0.8 + docstring update); `the-waiter.yaml` (explicit Menu inventory block + `drink` success_criteria rewrite accepting Coke/Pepsi/Sprite synonyms + `clarify` success_criteria rewrite accepting "as I said before" + inline call_id=118 comments); `test_checkpoint_manager.py` (+`coherence_charter` to `_make_manager` default "CHARTER." + 3 direct constructions updated + 2 system_instruction equality assertions updated + 2 new tests: `test_coherence_charter_appears_in_every_system_instruction_swap` AC8 + `test_warn_on_duplicate_charter_in_composed_prompt` AC15 box 3); `test_bot_pipeline_wiring.py` (+COHERENCE_CHARTER import assertion + coherence_charter="CHARTER." in direct construction + new `test_coherence_charter_threaded_to_checkpoint_manager_and_llm_settings` AC8 wiring test); `test_exchange_classifier.py` (docstring 2.0→1.0). 3 new deviations: #5 LatencyProbe ships as opt-in prod code (not test fixture) per zero-redeploy smoke-gate ergonomics; #6 bot.py initial_system_prompt is 3-branch composition (YAML-first / env-var-fallback / SARCASTIC absolute fallback — all 3 append charter); #7 WARN-on-duplicate guard sits inside _classify_and_advance (catches duplication at the swap moment, not a separate post-write step). Tests: server 325 → 329 (+4 net new), client 373 unchanged (zero net Flutter per Dev Notes). Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Manual items reserved for Walid post-deploy: empirical user_speech_timeout floor tuning (Task 1.3), LATENCY_PROBE=1 LLM→TTS gap measurement (Task 3.2-3.3), 4-scenario inventory design (Task 8.3), Cartesia voice audition+selection (Task 10), Pass A+B calibration 5 scenarios × 2 = 10 device calls on Pixel 9 Pro XL (Tasks 11+12), 14-box Smoke Test Gate (Task 14). Awaiting `/commit`.
  epic-6-retrospective: optional

  # Epic 7: Post-Call Debrief & Learning
  epic-7: backlog
  7-1-build-debrief-generation-backend: backlog
  7-2-build-call-ended-overlay-transition: backlog
  7-3-build-debrief-screen: backlog
  7-4-build-pre-scenario-briefing-display: backlog
  epic-7-retrospective: optional

  # Epic 8: Monetization & Subscription
  epic-8: backlog
  8-1-integrate-storekit-2-and-google-play-billing: backlog
  8-2-build-paywall-screen-with-invisible-tier-design: backlog
  8-3-build-subscription-management-and-full-tier-enforcement: backlog
  epic-8-retrospective: optional

  # Epic 9: Offline Access & Data Sync
  epic-9: backlog
  9-1-build-local-cache-with-sqflite-for-scenarios-and-debriefs: backlog
  9-2-build-automatic-data-sync-on-network-availability: backlog
  epic-9-retrospective: optional

  # Epic 10: App Publication & Launch Readiness
  epic-10: backlog
  10-1-create-privacy-policy-terms-of-service-and-legal-compliance-pages: backlog
  10-2-provision-domain-dns-ssl-and-server-infrastructure: backlog
  10-3-configure-app-store-and-play-store-listings: backlog
  10-4-set-up-beta-testing-pipeline-and-analytics: backlog
  10-5-execute-final-launch-checklist-and-store-submission: backlog
  epic-10-retrospective: optional
