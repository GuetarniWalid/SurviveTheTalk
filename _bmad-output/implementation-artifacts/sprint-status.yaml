# generated: 2026-03-30
# last_updated: 2026-05-15 (Story 6.6 dev-story complete — `in-progress` → `review`. New files: `pipeline/exchange_classifier.py` (~210 LOC, ExchangeClassifier async judge with qwen-flash + 2.0s timeout + JSON-strict + fence/first-`{...}` fallback) and `pipeline/checkpoint_manager.py` (~270 LOC, CheckpointManager FrameProcessor mirrors EmotionEmitter gen-counter + cancel + cleanup pattern). Extended `patience_tracker.py` with `hang_up_line_survived` kwarg + new `_REASON_SURVIVED` constant + new public methods `apply_exchange_outcome(success)` (wires Story 6.4 dormant fail_penalty/recovery_bonus into the meter, bounded `[0, initial_patience]`, no-op during hang-up) and `schedule_completion(survival_pct)` (routes to `_run_hang_up` via `_pending_survival_pct` attribute; idempotent during hang-up). 3-way exit-line + survival_pct switch in `_run_hang_up`: silence/inappropriate use meter-ratio, survived uses the override (100). `_schedule_hang_up` validates `reason` against `_VALID_REASONS` 3-tuple — ValueError on unknown. Extended `scenarios.py`: `dict(preset)` → `copy.deepcopy(preset)` (retires DW line 357); 4 type/range validators on fail_penalty/recovery_bonus/escalation_thresholds/silence_hangup_seconds (retires DW line 350); loads `exit_lines.hangup` → both silence+inappropriate; loads `exit_lines.completion` → survived; new `load_scenario_checkpoints` (shape-validates id/hint_text/prompt_segment/success_criteria) + `load_scenario_base_prompt` (no _SPEAK_FIRST_DIRECTIVE suffix). `bot.py` pipeline now `transport.input → stt → transcript_user → emotion_emitter → context_aggregator.user() → checkpoint_manager → patience_tracker → llm → ...`; PatienceTracker(...) threaded with the 3 new hang_up_line_* kwargs from `patience_config`. **5 deviations**: #1 two distinct survival_pct formulas (100% for survived, meter-ratio for character_hung_up/inappropriate); #2 system-prompt swap via `llm._settings.system_instruction =` (NOT `LLMContext.set_messages` — OpenAI adapter prepends from _settings at every invocation, LLMContext is empty at boot, mutating it would produce 2 system messages with a warning); #3 `exit_lines.hangup` shared by silence+inappropriate (single source of truth in YAML); #4 last_character_line sourced from `llm_context.get_messages()` (NOT frame observation — LLM TextFrame flows DOWNSTREAM but CheckpointManager sits UPSTREAM of LLM); naming reconciliation epics.md `reason "completed"` → `reason "survived"` to match Story 6.5 D4 wire format. Tests: server 242 → 292 (+50 net), client 357 unchanged (zero net Flutter code per AC8 — `data_channel_handler` `default` branch already silently drops unknown envelope types). Déviation-#28 regression net landed in `test_bot_pipeline_wiring.py::test_checkpoint_manager_observes_finalized_TranscriptionFrame_via_real_pipeline_drive` — drives a TranscriptionFrame through a real Pipeline+PipelineTask+PipelineRunner (retires DW line 369). Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Smoke Test Gate sits with Walid for `review → done`. Awaiting `/commit`. Previously: Story 6.6 spec created — `backlog` → `ready-for-dev`. CheckpointManager Pipecat FrameProcessor + ExchangeClassifier async LLM service; mutates the shared LLMContext system message in-place on advance (set_messages mechanism); emits `checkpoint_advanced` envelopes via data channel; routes all-checkpoints-passed through new `PatienceTracker.schedule_completion(survival_pct=100)` with new `_REASON_SURVIVED` path + new `hang_up_line_survived` constructor kwarg sourced from YAML `exit_lines.completion`; new `PatienceTracker.apply_exchange_outcome(success: bool)` finally wires the Story 6.4 dormant `fail_penalty` / `recovery_bonus`. `resolve_patience_config` switches `dict(preset)` → `copy.deepcopy(preset)` (deferred-work line 357) + adds type/range validation on fail_penalty/recovery_bonus/escalation_thresholds/silence_hangup_seconds (deferred-work line 350). New `load_scenario_checkpoints` + `load_scenario_base_prompt` helpers in scenarios.py. Pipeline-drive contract test in test_bot_pipeline_wiring.py is the Déviation-#28 regression net (deferred-work line 369). Zero net Flutter code (Story 6.7 owns CheckpointStepper UI consumer). Hard prerequisite: 6.5 must be `done` (verified ✅). 3 up-front deviations: (#1) two distinct survival_pct formulas — 100% for `survived`, meter-ratio for `character_hung_up`/`inappropriate_content`; (#2) system-prompt swap via `LLMContext.set_messages`, NOT `_settings.system_instruction`; (#3) YAML `exit_lines.completion` → `hang_up_line_survived`, `exit_lines.hangup` shared by both silence/inappropriate. Naming alignment: epic spec line 1193 amended from `reason "completed"` to `reason "survived"` to match wire format already shipped in Story 6.5 D4 widening. Previously: 2026-05-13 — Story 6.5 code review complete; D1-D6 decisions resolved by Walid-delegated trust pass + 27 patches applied — flipped to `ready-for-dev`. Drilled deep on POST /calls/{id}/end endpoint contract (4-reason Literal whitelist, 404 on cross-user, BEGIN IMMEDIATE for status flip, idempotent), migration 008_call_sessions_status.sql (status column with CHECK constraint, historical-row backfill = 'completed' to preserve cap counter), background janitor sweep (asyncio lifespan task, 15-min cadence, fail-soft), Popen-rollback LiveKit cleanup (livekit_delete_room helper), count_user_call_sessions_* status filter (WHERE status IN ('pending','completed')), client CallRepository.endCall + fire-and-forget _endCallSilently from all 3 CallBloc exit paths (user_hung_up / character_hung_up / inappropriate_content / network_lost), NoNetworkScreen UX-DR7 rewrite (WiFi-barred icon top-right, character avatar circle, "Call failed" SemiBold + "No network available" Regular, hang-up button bottom; ZERO new color tokens). 10 deviations documented up-front. ADR-003 numbering drift formally retires deferred-work.md items 121, 275-289, 322. Hard prerequisite: 6.4 must be `done` before opening dev-story 6.5.)
# project: surviveTheTalk2
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-03-30
last_updated: 2026-06-02 (Story 6.17 /bmad-code-review COMPLETE on the 6.17 net-new scope (per-scenario voice wiring tts_factory/bot, the LLM voice matcher + auto-repair loop + the wizard new_scenario.py, --deploy; the scenario_builder/build_scenario 6.16 core was already reviewed). Acceptance Auditor verified ALL 7 ACs on the ON-DISK code (it caught that the fan-out diff was stale — built from the commit, missing the uncommitted 6.15/6.16 review patches — and audited the real files). No critical bug. 2 patches applied + 2 regression tests: P1 select_voice now keeps the DEFAULT voice instead of silently widening to ALL genders when no required-gender match (was: a feminine puppet could get a male voice — AC2); P2 shared golden_inconclusive() helper so build_scenario --validate prints INCONCLUSIVE on an all-unsure rate-limited judge (the wizard already guarded it; the CLI printed a FALSE PASS). 9 deferred (top: --deploy to prod root with no confirmation + not gated on golden-pass, carried from the 6.16 review). ~6 dismissed (mostly stale-diff artifacts the Auditor verified against on-disk code). Gates green on the changed surface: ruff check + ruff format clean; builder+engine+tts_factory pytest 79 passed. Status STAYS review — Walid owns the live calibrate_scenario (waiter + cop) + the on-device voice test + /commit of the 6.15-6.17 bundle + the review->done flip for all three.) Previously: 2026-06-02 (Story 6.16 /bmad-code-review COMPLETE on the 6.16 net-new scope (scenario_builder + build_scenario + cop-interrogation-01.yaml + the PROD _multi_max_tokens fix; the calibration_engine/calibrate_scenario "touched" files were already covered under the 6.15 review). 3 patches applied + 2 regression tests: P1 AC3 now ENFORCED — finalize_build flags a checkpoint-count mismatch (the builder shipped a wrong count silently; the Auditor proved it via a fake LLM returning 3 when 20 were requested), P2 --id path-traversal guard (snake_case only — default_scenario_path put --id straight into the filename), P3 doc 400->544. 11 deferred (incl. the Story-6.17 --deploy-to-prod-root-without-confirmation + the auto-repair loop + voice selection — revisited in the 6.17 review). 7 dismissed incl. 3 verified false positives (BH#5 validator-fork: parity with scenarios.py confirmed; BH#9 deploy shell-injection: subprocess uses an argv list; BH#10 auto-repair dead code: it is 6.17 reached via new_scenario.py). All 10 ACs delivered (AC3 now enforced by the patch); cop artifact = 20 unique checkpoints loading through every prod loader; PROD _multi_max_tokens fix (64+24n=544 at 20 goals) low-risk, only raises the ceiling. Gates green on the changed surface: ruff check + ruff format clean; builder+classifier pytest 66 passed. Status STAYS review — Walid owns the live calibrate_scenario cop_interrogation_01 + /commit + review->done flip; --deploy concerns ride into the 6.17 review.) Previously: 2026-06-02 (Story 6.15 /bmad-code-review COMPLETE on the 6.15-scoped diff 137c577..HEAD — 3 adversarial layers (Blind Hunter diff-only + Edge Case Hunter + Acceptance Auditor). **1 CRITICAL fixed:** the primary `calibrate_scenario <id>` command called `record_verdict(verdict, report_path=...)` but the signature is `record_verdict(ledger, verdict, *, report_path)` → `TypeError` crashing the main path right after the (paid) calibration, before the ledger write — invisible because the live path is gated out of pytest (AC6) and the unit test used the correct `(ledger, verdict)` order. Plus 4 minor patches (unknown-scenario-id clean error+exit code instead of a raw FileNotFoundError traceback; `load_golden_fixture` corrupt-JSON guard; `_fixture_cases` missing-required-key guard; DAR count + File-List accuracy) and 3 regression tests (engine tests 36→39; safe non-livekit subset 194→197). 12 deferred to deferred-work.md "code review of story-6.15 (2026-06-02)" (top item: the golden net classifies one goal in isolation while the 2026-05-30 judge-passes-everything bug was a multi-goal over-eager flip — partially covered by the calibration runs). 10 dismissed incl. 4 VERIFIED false positives from the diff-only Blind Hunter (BH#9 success-bonus skip — prod does the same via schedule_completion; BH#10 self._goals rebind — no durable alias; BH#15 polite off_topic trips guardrail — survival needs ALL goals; BH#21 disjoint Waiter clarify positives — each golden case judged independently). Gates green on the changed surface: ruff check + ruff format clean; safe non-livekit pytest subset 197 passed. Acceptance Auditor confirmed all 13 ACs delivered (AC1 reuse-prod-paths genuine + identity-asserted; AC2 lenient-judge proof genuine). Status STAYS `review` — all review findings resolved; Walid owns the live `calibrate_scenario` validation (now unblocked by the fix), full `pytest`, `/commit`, and the `review → done` flip. The 5 review patches are uncommitted (amend into the Story 6.15-6.17 chain per one-story-one-commit). Findings in the Review Findings section of `6-15-automated-scenario-calibration-harness.md`.) Previously: 2026-06-01 (Story 6.14 review → done — Walid Pixel 9 smoke gate PASSED (Cartesia voice fluid, checkpoint OK); deployed a90cbcf. See the 6-14 entry below for the full close-out. Previously: Story 6.14 /bmad-code-review COMPLETE on the uncommitted diff — 3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor). Acceptance Auditor: ZERO AC violations, all Dev-Record claims verified true (WS-proxy surface covered, FreshContext fully removed, Cartesia error-schema logged correctly, isinstance "trap" non-existent — siblings). 1 decision-needed + 5 deferred + ~12 dismissed. **PATCH P1 applied** (Walid chose option 1): added a `field_validator` on `Settings.livekit_min_playout_delay_ms` rejecting `<0` and `>2000` (PRD 2 s latency ceiling) — without it a typo'd `LIVEKIT_MIN_PLAYOUT_DELAY_MS` would overflow the protobuf int32 → 502 on EVERY call, or silently breach the latency kill-criterion. +3 config tests (negative reject / >ceiling reject / 0-disables). Gates on changed files GREEN: ruff check + format clean, test_config.py 20 passed. Full server pytest (497) + flutter (404) reserved for Walid before /commit (sandbox livekit import hang — patch is config-only, unrelated). 5 deferred → deferred-work.md "code review of story-6.14 (2026-06-01)" (all `InboundAudioStatsLogger` dev-only-diagnostic edge cases + room-reuse no-op). Status STAYS `review` — all review findings resolved, only the Walid-owned Pixel 9 smoke gate (AC4.3 Cartesia device re-test + AC6 6-box + empirical 200 ms tune) remains for `review → done`. Findings in the Review Findings section of `6-14-tts-audio-jitter-resilience.md`. Awaiting `/commit`. Previously: Story 6.13 review → done — Walid approval; Epic 6 pre-launch pipeline hardening pass closed, code at commit 8d44cf2. Previously: Story 6.11 DONE — Pixel 9 smoke gate validated by Walid; parasitic-voice detection → mute mic (InputGate) → full in-character exit line → gifted call end. Required 2 smoke-fixes (exit-line interruption race + "stop listening" mic mute). Same-day separate fix `4ca78fe`: checkpoint judge default-MET → default-UNMET (was passing every checkpoint). New stories drafted ready-for-dev: 6.14 (TTS jitter + Cartesia-as-default) + 6.15 (automated scenario calibration harness). See the `6-11` entry below for the full change list. Previously: Story 6.11 `/bmad-dev-story` COMPLETE — `ready-for-dev` → `review`; parasitic-background-voice detection via Soniox diarization. See the `6-11-noisy-environment-detection` entry below for the full change list. Server 449→476 tests, client 390→399, all gates green. Smoke gate Walid-owned. Previously: Story 6.10 `/bmad-code-review` COMPLETE on range `6fd0151..HEAD` — 4 decisions resolved (D1 deferred → "verify on Pixel 9 smoke gate" the double-complete-final-turn disjoint UX; D2 dismissed-verified against pipecat 0.0.108 — non-system Data frames drain via the single `__process_frame_task_handler` loop one-at-a-time, so a non-terminal `_schedule_classification` cannot bump `_generation` during a terminal turn's `await`, the lock + generation guard are belt-and-suspenders; D3 patched per below; D4 dismissed as out-of-scope for this UI-refonte story — no a11y path existed in 6.7 either) + 6 patches applied + 3 deferred (call-end reconcile `metIndices=[0..pi-1]` best-guess on out-of-order completion; urgent-vs-queued envelope ordering + HUD first-snapshot/shrinking-met-set animation edge cases; multi-goal completion not pre-suppressed when ≥2 goals complete same final turn) + 10 dismissed as noise/already-handled. **Patches:** P1 LLM_BASE_URL test override example was a full chat-completions URL (would double-append `/chat/completions` and 404) → fixed to base URL per `llm_provider.py` contract; P2 `load_scenario_checkpoints` now rejects duplicate checkpoint ids (latent silent goal-state collapse: `self._goals` / `_id_to_index` dedupe but `len(checkpoints)` counts both → client HUD `metCount` can never reach `total` → `allMet` never fires; pre-6.10 linear engine indexed positionally and was immune); P3 two stale doc comments still described the abandoned `{"goals_met":[...],"goals_unmet":[...]}` array contract (the format that caused the 2026-05-29 silent-no-flip bug) → rewritten to the per-id `{goal_id: "met"|"unmet"|"unsure"}` structured-output contract; P4 data_channel_handler comment claimed the HUD "falls back to `next_hint`" on empty `hints` but the HUD actually hides → comment corrected (acceptable behaviour for the rare server-rollback case on single-VPS deploy); P5 Dev Agent Record / AC9 test counts refreshed (dev-story 438/388 → 2026-05-29 hardening 449/390 → review patches 453/390); **P6 (from D3, runtime-changing)** — `classify_multi` now returns `None` on a true infra failure (timeout / HTTP / closed-client / parse failure) while a PARSED response (even all-"unsure") returns a dict, breaking the conflation that made a healthy-but-uncertain classifier fabricate the false `checkpoint_classifier_sustained_failure` backstop alert after 5 consecutive all-unsure turns; manager `_classify_and_flip_goals` rewritten to: `verdicts is None` → infra path (`_consecutive_none_count++` + backstop at N=5); parsed dict with ≥1 flip → success; parsed dict no flip + ≥1 `False` → genuine off-topic drain (AC8); parsed dict all-None (all-unsure) → patience-neutral, reset `_consecutive_none_count` (no false sustained-failure alert). `_parse_multi_classifier_output` returns `None` on parse failure (treated as infra) instead of all-None dict. **Gates ALL GREEN:** ruff check + ruff format + pytest **453** (+4 net new from review: P2 duplicate-id reject + P6 `test_classify_multi_returns_none_on_unparseable_body` + `test_classify_multi_parsed_all_unsure_returns_dict` + `test_parsed_all_unsure_does_not_drain_or_feed_backstop`) + flutter analyze (No issues) + flutter test **390** (P4 was comment-only). Commit `c79c9e9` force-pushed (one story = one commit per Walid; amended into the Story 6.10 chain replacing `ae1f236`) + auto-deployed via `deploy-server.yml` (healthcheck git_sha match confirmed). **Pixel 9 smoke gate validated by Walid 2026-05-30 → sprint-status + story Status flipped `6-10-goal-based-dialogue: review → done`.** Walid-owned next steps per project convention: monitor on subsequent calls whether the D1 double-complete-final-turn disjointedness manifests; if observed, the deferred-work entry is ready to be promoted. Previously 2026-05-27 (Story 6.13 `/bmad-code-review` COMPLETE on full story range 436144f..HEAD — 3 parallel adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor). 2 decision-needed resolved + 4/5 patches applied + 8 deferred + 6 dismissed. **P1 CRITICAL** (Edge Case Hunter, verified): the AC2 `BotStartedSpeakingFrame` UPSTREAM branch in `patience_tracker.py` called `_cancel_silence_timer()` with NO `_self_speaking` guard — so when the silence ladder reached stage 2 and pushed its OWN "Are you still there?" prompt, the prompt's audio made the output transport emit a BSF that round-tripped and cancelled the ladder task itself, killing stages 3 (anger) + 4 (silence hang-up). The character would NEVER hang up on a silent user. Untested (AC2 tests only drove BSF pre-stage-1 with stubbed push_frame). FIXED with `if not self._self_speaking:` guard + regression test `test_stage2_prompt_BSF_does_not_self_cancel_the_ladder`. **P5/D1** (corroborated by all 3 layers): `AuthInterceptor._handling` was a per-instance latch but 9 `ApiClient()` sites each build their own interceptor → concurrent 401s on different clients double-fired the global handler (stacked toasts + double ResetAuthEvent), AND the latch never reset in prod → after re-login a later 401 was silently swallowed (Story 5.5 silent-loop returns). FIXED: `_handling` made `static` + auto-reset on `onResponse` (next successful authenticated response reopens it; no app.dart wiring) + 2 tests (cross-instance dedup, post-relogin reset). **D2** prod_snapshot.sqlite REGENERATED via `refresh_prod_snapshot.py` from live VPS (real post-010 prod shape: 97 call_sessions, 0 FK violations, integrity ok) — replaces the hand-doctored fixture that bypassed the migration guardrail. **P3** warm-up `asyncio.create_task` result retained in module-level `_BACKGROUND_TASKS` set + done-callback (was GC-able fire-and-forget). **P4** `Settings.cartesia_api_key` defaulted to "" so an ElevenLabs-only deploy boots (factory still fails loud when cartesia selected). **P2 SHIPPED** (HIGH): AC4 widget test added in `app_test.dart` asserting the globalHandler closure's 3 effects (clear JWT + dispatch ResetAuthEvent + "Session expired" toast). The test EXPOSED A REAL PROD BUG and FIXED it: the toast used `Overlay.of(navigatorKey.currentContext)` which throws (the Navigator's Overlay is a CHILD of that context, not an ancestor) and the exception was swallowed by the interceptor's `catch (_)` — so the "Session expired" toast NEVER rendered in prod. Fixed via new `AppToast.showInOverlay(OverlayState)` + `navigatorKey.currentState?.overlay` in `app.dart`. 8 deferred to `deferred-work.md` "code review of story-6.13 (2026-05-27)" (watchdog BSSF-emit gap, late-frame reconcile, synthetic-push test hardening, AC1 idempotency wording, stage-2 clamp cross-validation, /auth/ 401 exclusion, warm-up reasoning placement, FreshContext default) — most are LOW prod-impact because `TTS_PROVIDER=elevenlabs` is the deployed default so AC1's Cartesia watchdog is now a safety-net that rarely fires. Context note carried into findings: AC1 watchdog demoted to safety-net under ElevenLabs default; AC1 Box 2/3 Cartesia smoke premise moot in prod. Pre-commit gates ALL GREEN post-patches: ruff check + ruff format + pytest 416 + flutter analyze (No issues) + flutter test 382. Status stays `review` — ALL review findings resolved (no dev action items left); only the Walid-owned Pixel 9 smoke gate remains for `review → done`, and the P1 silence-ladder fix changes runtime behaviour that the smoke gate must re-validate (silence-to-hang-up path). Awaiting `/commit` — the 5 review patches + spec/deferred-work updates fold into one amended commit per Walid convention. Findings written to the Review Findings section of `6-13-epic-6-prelaunch-hardening.md`. Previously 2026-05-26 (Story 6.13 dev-story COMPLETE — `in-progress` → `review`. All 7 ACs delivered: AC1 Cartesia silent-stall watchdog (new `pipeline/tts_watchdog.py` + 6 tests) wired in `bot.py` between `tts` and `tts_first_audio_probe`; AC2 PatienceTracker BSF (started) UPSTREAM cancel + cross-reference contract test for direction parity; AC3 per-difficulty `ladder_impatience_seconds` (easy 4.5 / medium 3.5 / hard 2.5) through full plumbing — preset + resolver + required kwarg + range validator + bot.py thread + 5 scenario YAML overrides + new migration 010 + schemas + queries + seeder + routes + prod_snapshot.sqlite refreshed locally; AC4 cross-cutting Dio 401 interceptor (`client/lib/core/api/auth_interceptor.dart` + global-callback pattern + re-entry guard + 6 unit tests) wired in `App.initState` → clears JWT + dispatches ResetAuthEvent + shows AppToast; AC5 deleted both `logger.debug` calls + TODO comment block from `exchange_classifier.py`; AC6 Waiter `clarify.success_criteria` rewritten to accept drink answers + synonyms + polite acknowledgements when Tina's prior line shifted into drinks. **6 up-front deviations preserved** — Cartesia watchdog as mitigation (Deviation #1), BSF Option A cancel (Deviation #2), per-difficulty + nullable YAML (Deviation #3), Dio interceptor (Deviation #4), delete-not-gate debug logs (Deviation #5), Waiter-only clarify amend (Deviation #6), 3-call smoke gate (Deviation #7). Tests: server 376 → 394 (+18 net new); client 373 → 379 (+6 net new). Pre-commit gates ALL GREEN: ruff check + ruff format + pytest 394 + flutter analyze + flutter test 379. Smoke gate boxes 1+2+4+5+6+7+8+9 sit with Walid for `review → done` per Story 6.5 D6. Box 3 (Cartesia watchdog server unit test) self-validated. Awaiting `/commit`. Previously 2026-05-22 (Story 6.9b `/bmad-code-review` complete on uncommitted Phase A + Phase A.5 diff— status STAYS `review` per D1:c accept-as-doc-debt. 3 parallel reviewer layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) surfaced 82 raw findings → 6 decision-needed (all resolved live with Walid, D1:c / D2:c / D3:a / D4:a / D5:c / D6:c) + 25 patches applied (P1-P22 original + P23 D2 truthful-rollback-doc + P24 D3 streaming-skipped-paragraph + P25 D4 three-new-deviations) + 22 deferred (appended to `deferred-work.md` under "code review of story-6.9b (2026-05-22)" heading) + ~30 dismissed as noise. Three new declared deviations in the spec: **#8** premise empirically falsified (Qwen baseline measured 587 ms p50 / 859 ms p95 vs cited 1046 ms / 1700-1900 ms — migration justification shifts from latency to accuracy 98.7 % vs 94.7 % + 0 FPs vs 3 + Story 6.12 sync-verdict-everywhere unblock); **#9** provider catalog grew 4 → 7 (DashScope×2 + Groq Llama 3.3 70B added mid-flight); **#10** `Settings.model_config` `extra: "ignore"` validation loosening (mitigated by `test_settings_loads_all_pipeline_env_vars`). Doc fixes landed in code: `server/CLAUDE.md` §4 rollback paragraph now leads with "REQUIRES A REDEPLOY" (was misleading env-only-rollback framing); `server/config.py` `classifier_model` comment now explicitly disclaims "NOT a Qwen rollback knob"; `server/pipeline/exchange_classifier.py` module docstring extended with explicit "Streaming skipped — gain <100 ms" paragraph (AC3 satisfied); `_bmad-output/implementation-artifacts/deferred-work.md` PII-logging entry rewritten from `logger.info` → `logger.debug` framing (P21 / D6:c). Hardening patches landed in production code: `_escape_format_braces()` helper escapes `{`/`}` in user_text/last_character_line/success_criteria/scenario_description before `str.format()` (P14 — fixes KeyError/ValueError crash if STT/character text contains literal braces); `max_tokens` raised 32 → 64 (P15 — Llama 70B verbosity tendency mid-JSON truncation); `except RuntimeError` narrowed to lifecycle-closed errors only, re-raises other RuntimeErrors (P9); JSON `ValueError` branch now logs content-type + first 200 chars of response body (P16 — Cloudflare interstitial visibility); empty `choices: []` now logs distinctly with usage payload (P22 — Groq content-filter refusal monitoring, conservative None verdict preserved). Benchmark harness hardening: `load_dotenv()` moved from module-import to `main()` only (P4 — test hermeticity); empty-string env vars treated as unset via `.strip()` (P6); 64 KiB cap on `aiter_bytes` body accumulation (P7); retry-on-5xx in addition to retry-on-429 (P8); deterministic tiebreak `(p50_total_ms, -accuracy_pct, name)` in `_recommend` (P12); new `baseline_failed` verdict when baseline.p50_total_ms is None (P13); `samples_total` from `max()` not `summaries[0]` (P18); new `false_positive_rate_pct` + `false_negative_rate_pct` fields on `ProviderSummary` exposed in `_summary_to_dict` (P19 — surfaces over-permissive providers, safety net for D5 default-to-MET concern). Test hardening: P1 migration witness asserts against `_PROVIDER_MODEL` constant + literal (was literal only); P2 `test_settings_classifier_model_defaults_to_groq_70b` + overrides_via_env both use `patch.dict(clear=True)` for hermetic env (was clear=False with two-level nesting that would silently fail if dev shell had CLASSIFIER_MODEL exported); P3 module-scope `os.environ.setdefault("GROQ_API_KEY", "test-key")` deleted from `test_benchmark_classifier.py` (conftest.py TEST_ENV_VARS is single source of truth); P5 `import os` removed from test_benchmark_classifier.py (no longer needed post-P3); P11 new `test_corpus_has_canonical_size_and_label_split` asserts 75 samples / 47 MET / 28 NOT-MET (locks the corpus shape against silent drops). Test count: server 375 → 376 (+1 net P11). Per P20, the prior sprint-status entry's "+12 net new" was incorrect — actual Phase A+A.5 added +14 net new (5 principle-regression + **2 Groq failure-mode** `test_classify_returns_None_on_429_rate_limit` + `test_classify_returns_None_on_5xx_groq_incident` + 2 Settings.classifier_model + 5 benchmark harness); plus this review's +1 = +15 total since Story 6.9. Status stays `review` (Walid Phase B owns the 7-box Smoke Test Gate on Pixel 9 Pro XL + Task 2.3 memory update + Task 6.5 deferred-work.md re-tag per existing Story 6.5 D6 deploy-gate convention). Awaiting `/commit` for the 25 review patches + spec amendments. Previously 2026-05-22 Story 6.9b Phase A.5 complete — Groq Llama 3.3 70B migration landed, status `in-progress → review`. Same-session interactive bench + decision + migration. 6 benchmark reports under `_bmad-output/implementation-artifacts/calibration-tests/`. Lever 1 (DashScope direct, skip OpenRouter proxy) tested and ABORTED — zero gain because Qwen is hosted in Singapore not US, OpenRouter overhead ~10 ms. Groq Llama 3.3 70B benched from VPS at 121 ms p50 / 320 ms p95 / 98.7 % accuracy / 0 false positives vs Qwen prod 587 / 859 / 94.7 % / 3 FPs — winner on every axis, +10 €/mois at 100-user MVP scale. Migration: 9 server files modified, persistent-client lifecycle from Story 6.9 preserved unchanged, smoke-witness test rewritten as Story 6.9b migration witness. Pre-commit gates GREEN: ruff + pytest 375 + flutter analyze + flutter test 373. Phase B remaining for Walid: key rotation (3 keys leaked into chat history today) + VPS provision GROQ_API_KEY + Task 2.3 cosmetic memory update + Task 6 7-box Smoke Test Gate + Task 6.5 deferred-work.md re-tag + flip `review → done`. Previously 2026-05-21 Phase A landed via `/bmad-dev-story`. Scoped Phase A as the autonomous slice (no provider API keys / no Walid label sign-off / no device smoke gate); Phase B (benchmark execution + provider migration + deploy + 7-box smoke gate) handed off to Walid via the story's Dev Agent Record. Phase A landed: (1) `Settings.classifier_model` field added to `server/config.py` (default `"qwen/qwen3.5-flash-02-23"`, `CLASSIFIER_MODEL` env override) + `ExchangeClassifier.__init__` accepts `model: str = _OPENROUTER_MODEL` kwarg + `bot.py:324` wires `model=settings.classifier_model` + 2 new `test_config.py` tests (default + env-override) + wiring assertion in `test_bot_pipeline_wiring.py::test_bot_instantiates_emitters` — **retires `deferred-work.md` line 450** (Story 6.9 Defer #3 — hardcoded model id). (2) `EXCHANGE_CLASSIFIER_PROMPT` compressed ~600-700 → ~340 tokens preserving all 6 GUIDING PRINCIPLES + key exemplars (`"Coke"="cola"`, `"I already said pasta"`, `"uh"`/`"um"`, P21 `"Default to MET"` + `"False positives"`, XML-tag injection-resistance from Story 6.6 D3) + inline comment block updated with rationale + 6-test cross-reference. (3) 5 new principle-regression tests in `test_exchange_classifier.py` (intent_over_literal / accepts_synonym_or_brand / accepts_fragmented_response / accepts_restatement / evaluates_current_objective_only) following the P21 text-assertion pattern. (4) New `server/scripts/benchmark_classifier.py` (~460 LOC) — 4 providers wired (`qwen_via_openrouter` baseline + `groq_llama_3.1_8b` + `cerebras_llama_3.1_8b` + `anthropic_haiku_4.5`); reuses prod `_parse_classifier_output` + `EXCHANGE_CLASSIFIER_PROMPT` so harness exercises EXACT prod code paths; per-provider request shapes (OpenAI-compat vs Anthropic messages API); streaming TTFT measurement; recommendation logic implements Deviation #1 (winner beats baseline on BOTH latency AND accuracy else abort → Story 6.12 falls back to option F visual-first); missing API keys skip provider with stderr warning. (5) New `server/tests/fixtures/classifier_benchmark_corpus.json` (75 synthetic samples, ~33 KB, 5 scenarios × ~6 checkpoints × happy/borderline/off-topic; 47 MET / 28 NOT-MET; 6 principles covered) — **PENDING WALID** ground-truth sign-off (Task 1.3). (6) New `server/tests/test_benchmark_classifier.py` (5 tests) — end-to-end via MockTransport + `_recommend()` Deviation-#1-abort + pick-fastest-qualifier + pure-function `build_report` + 4-provider config sanity. (7) `deploy/.env.example` — added `CLASSIFIER_MODEL` (commented) + 3 benchmark-only API key slots. **Phase B reserved for Walid** (5 items): (a) corpus sign-off, (b) provision `GROQ_API_KEY` / `CEREBRAS_API_KEY` / `ANTHROPIC_API_KEY` locally, (c) run benchmark → pick winner → update `memory/project_story_6_9b_classifier_latency_slash.md` §1 with real numbers, (d) provider migration in `exchange_classifier.py` if winner emerges (Tasks 3.1, 3.2, 3.4, 3.5), (e) VPS deploy + 7-box Smoke Test Gate on Pixel 9 Pro XL + `deferred-work.md` line 451 re-tag + flip `review → done`. Tests: server 363 → 375 (+12 net new: 5 principle-regression + 2 Settings.classifier_model + 5 benchmark harness); client 373 unchanged (zero net Flutter). Pre-commit gates GREEN: ruff check + ruff format + pytest 375 + flutter analyze + flutter test 373. Story Status stays `in-progress` (NOT review) — full transition `in-progress → review` fires after Walid completes Phase B (or amends Deviation #1 with abort outcome if no provider qualifies, in which case the prompt compression alone ships as the value delivered). Previously 2026-05-21: Story 6.9b spec drafted via `/bmad-create-story` — `backlog → ready-for-dev`. New spec file `_bmad-output/implementation-artifacts/6-9b-classifier-latency-slash.md` (~430 lines, 7 ACs + 7 deviations + 7-box smoke gate + 6 tasks across benchmark / migration / streaming / prompt-compression / tests / deploy). Spec drafted from `memory/project_story_6_9b_classifier_latency_slash.md` (must-read strategic frame: cost matrix, turn-taking research, option-E rationale, Story 6.12 link) per Walid 2026-05-21 explicit ask "don't let it ship as a generic swap-providers story". Up-front Deviations: #1 empirical provider choice (abort to Story 6.12 option F if no provider beats Qwen on BOTH latency AND accuracy), #2 streaming activation contingent on ≥100ms measured gain, #3 prompt compression validated by 6-test regression suite NOT byte count, #4 5-scenario re-calibration OUT OF SCOPE for in-progress→review (Walid-owned follow-up), #5 corpus mixes real + synthetic with anonymization, #6 persistent-client lifecycle from Story 6.9 sacred, #7 consecutive-None backstop N=5 stays. Retires `deferred-work.md` line 450 (hardcoded model id → Settings). Re-tags `deferred-work.md` line 451 (5-scenario re-calibration "post 6.9b/6.12" → "post 6.9b" — Story 6.12 now blocks on this story's benchmark outcome). Test target +5-6 net new → ≥369 server, client 373 unchanged. Awaiting `/bmad-dev-story` to flip `→ in-progress`. Previously 2026-05-21: Story 6.9 `/bmad-code-review` complete on cumulative diff 28a67c4..89bd10c — `review` → `done`. 3 parallel adversarial layers (Blind Hunter 17 + Edge Case Hunter 19 + Acceptance Auditor 5 = 41 raw findings) triaged into 5 decision-needed (all resolved live with Walid), 22 patches (all applied), 3 deferred, 8 dismissed. Decisions resolved: D1→consecutive-None counter N=5 (P16 — backstop against unbounded soft-lock when verdict=None patience-neutral); D2→explicit Story 6.8 AC5 retract for terminal-turn-only path + latency-probe regression test bounding terminal turn ≤3000ms (P17, P18 — dette temporary, queued to unwind via Story 6.9b); D3→8s wall-clock EndpointWatchdog new FrameProcessor backstopping Soniox endpoint detection (P19, new file `pipeline/endpoint_watchdog.py` + 6 new tests); D4→Deviation #10 documenting the EXCHANGE_CLASSIFIER_PROMPT rewrite that landed under "+6.8 prompt amend" in commit 0e1ba96 (the GUIDING PRINCIPLES block + default-to-MET) + default-to-MET regression net (P20, P21) + deferred re-calibration of 5 scenarios; D5→DTLN steady-state denoise test feeding 20 chunks across the warm-up window with strength=1.0 (P22). Patches applied: iscoroutinefunction cleanup guard, DTLN NaN/Inf guard + idempotent re-start + CancelledError handling + empty/odd-length bytes guard + N-consecutive-failure disable, ExchangeClassifier _closed flag + lock-guarded close() + RuntimeError handling in _classify, reset_mock + strengthened persistent-client (instantiation-count) assertion + AC5 enable=True direction + close-during-in-flight + classify-after-close regression tests, pipeline ordering test for endpoint_watchdog placement. Spec amends: explicit Story 6.8 AC5 retract for terminal turn (under Deviation #6), new Deviation #10 for prompt rewrite, File List reconciliation (orig/amend/review tags), Deviation #5 mitigation text rephrased to "infra-billing pressure + silence ladder + consecutive-None backstop". Follow-up stories queued (priority order): Story 6.9b "Classifier Latency Slash" — benchmark Groq Llama 3.1 8B / Cerebras / Anthropic Haiku 4.5 vs Qwen 3.5 Flash on past transcripts, activate streaming, compress prompt 600-900→300-400 tokens, target classifier total latency ~200-300ms (5x improvement, decouples MVP from PRD ceiling, makes Story 6.12 trivially viable) — IMMEDIATE priority post-6.9; Story 6.12 "Reactive Character Mood" — fix the asynchronous cause-effect mismatch where patience drain and emotion classification happen in background during character speech, so the user sees the character "react late" by one turn (decoupled from the user phrase that caused the reaction) — post-6.9b. Tests: server 343 → 363 (+20 net new across test_dtln_audio_filter.py, test_exchange_classifier.py, test_checkpoint_manager.py, test_bot_pipeline_wiring.py + new test_endpoint_watchdog.py with 6 tests). Pre-commit gates GREEN: ruff check + ruff format + pytest 363 + flutter analyze + flutter test 373 unchanged (zero net Flutter code this round). Café smoke test (happy path) was already validated 2026-05-21 by Walid post-amends; the new review-patch code (consecutive-None counter, EndpointWatchdog, hardening patches) is inert on happy-path calls — only fires on degraded-classifier / never-finalizing-Soniox / corrupt-ONNX failure modes that the café smoke gate did not exercise. Smoke gate for those failure-mode paths rides with Story 6.9b smoke gate. Awaiting `/commit` of the 22 review patches (uncommitted on this branch at review-complete time). Previously 2026-05-20 (Story 6.8 dev-story code complete — `in-progress` → `review`. Phases 1+2+Phase 3 Waiter-only all green — COHERENCE_CHARTER constant in prompts.py (~200 tokens, 5 conversation-memory rules) threaded through new required `coherence_charter` kwarg on `CheckpointManager.__init__` + 3-piece swap composition `base + charter + segment` (matches AC7 exactly + WARN-on-duplicate guard for AC15 box 3). bot.py composes initial_system_prompt with charter slotted between base_prompt and first_segment (3-branch defensive composition); user_speech_timeout 1.8→0.6 with full empirical-tuning-context comment + VAD `stop_secs=0.8` audit block (net silence-to-turn-end ≈ 1.4 s, under PRD 2 s ceiling); exchange_classifier timeout 2.0→1.0 (+ HTTP 1.8→0.8 mirror); new `pipeline/latency_probe.py` (~85 LOC env-var-gated LatencyProbe FrameProcessor, 2 instances wired llm↔transcript + tts↔output for AC3 measurement, inert in prod). Waiter YAML — explicit Menu inventory block (main+drinks+desserts), `drink` success_criteria accepts Coke/Pepsi/Sprite/sparkling synonyms + explicit refusals, `clarify` accepts "as I said before"-style re-statements; inline call_id=118 comments. Tests — server 325 → 329 (+4 net new — charter behavioral + WARN-on-dup guard + bot import assertion + bot wiring source-text). 3 new deviations — #5 LatencyProbe ships opt-in prod, #6 3-branch initial_system_prompt, #7 WARN-on-duplicate inside _classify_and_advance. Pre-commit gates ALL green — ruff check + ruff format + pytest (329) + flutter analyze + flutter test (373 unchanged). Sprint-status + story Status flipped to `review`; `review → done` reserved for Walid post-smoke-gate. Manual items deferred to Walid — Tasks 1.3 empirical user_speech_timeout floor, 3.2-3.3 LATENCY_PROBE=1 LLM→TTS gap measurement, 8.3 4-scenario inventory design face-to-face, 10 Cartesia voice audition+selection, 11+12 10 device calls Pass A+B calibration on Pixel 9 Pro XL, 14 14-box Smoke Test Gate. Awaiting `/commit`. Previously 2026-05-19 (Story 6.8 spec created — `backlog` → `ready-for-dev`. SCOPE EXPANSION from epics.md's "Post-CheckpointManager Scenario Calibration" (YAML-only) to a 3-phase launch-blocker hardening pass driven by Story 6.7 smoke test findings on Pixel 9 Pro XL (call_id=118, 2026-05-19). **Phase 1 — Latency floor under PRD ceiling** — tune `user_speech_timeout` 1.8→~0.6s (single biggest win — PRD hard ceiling is 2s, "concept dead" per requirements table), audit VAD `stop_secs=0.8` redundancy, verify LLM→TTS streaming overlap via 3-point instrumentation, cap Dev #7 ExchangeClassifier timeout 2.0→1.0s. Target: median perceived latency ≤1500ms (FR45 ops alert threshold), p95 ≤2000ms. **Phase 2 — Conversation coherence charter** — new `COHERENCE_CHARTER` module-level constant in `prompts.py` (~200 tokens), threaded as a required `coherence_charter` parameter through `CheckpointManager.__init__` + composition into every `_settings.system_instruction` swap (Story 6.6 Deviation #2). Charter encodes 5 scenario-agnostic memory rules: confirmed-items locked, user-reference-prior-trusts-conversation, menu-listings-from-base_prompt-verbatim, no-deny-prior-listing, history-wins-on-disagreement. Walid's explicit ask: "le gardien ne doit pas être au niveau de chaque scénario mais plutôt au niveau général". **Phase 3 — Per-scenario YAML calibration** (original 6.8 scope) — enumerate inventory in each scenario's `base_prompt` (kills hallucinated menus), audit Waiter `drink`+`clarify` `success_criteria` for over-strictness (call_id=118 saw 4 consecutive `checkpoint_unmet drink` even after user said "Coke"), select non-null `tts_voice_id` for Mugger/Girlfriend/Cop/Landlord, run Pass A + Pass B for all 5 scenarios via existing `TranscriptLogger`+`score_transcript.py` tooling, populate `calibration.pass_a/pass_b` blocks with verdict against target bands (Mugger/Girlfriend 35-55%, Cop/Landlord 15-35%, Waiter 60-90%), tune nullable difficulty overrides if outside band ±10%. **15 ACs total, 14-box smoke gate** — deploys+latency-floor-verified+coherence-regression-replay (call_id=118 Cola test)+menu-consistency-across-turns+10 calibration runs (5 scenarios × 2 passes)+stepper-no-regression+server-logs-clean+FR45-threshold-validated. Hard prerequisites: 6.6 done ✅, 6.7 review/done ✅. 4 up-front deviations documented: (#1) scope expansion vs epics.md line 1232-1260, (#2) phase ordering puts arch fixes BEFORE calibration (calibration on broken pipeline = broken numbers), (#3) charter as Python constant not YAML-overridable, (#4) `user_speech_timeout` value is empirical not theoretical. Server-only story (zero Flutter changes). Test target: server ~327 (baseline 325 + 2 new charter tests). Memory pointers: `feedback_latency_kill_criterion_exceeded.md` + `feedback_coherence_must_be_system_wide.md` + deferred-work.md lines 16-45 (2 🚨 entries at top). **MVP launch is gated on this story** — Phases 1+2 close the PRD's "concept dead" + Walid's "app entière est en jeu" findings. Previously 2026-05-18: Story 6.7 spec created — `backlog` → `ready-for-dev`. CheckpointStepper Flutter overlay widget consuming Story 6.6's `checkpoint_advanced` data-channel envelope. New files: `client/lib/features/call/views/widgets/checkpoint_stepper.dart` (~250 LOC widget — adaptive 20×20/14×14 circles, 8-token spacing block in AppSpacing) + `client/lib/features/call/services/checkpoint_advanced_payload.dart` (~30 LOC typed value class). Extends `DataChannelHandler` with `onCheckpointAdvanced` typed callback (replaces the existing silent-default-branch behaviour at lines 129-138). Extends `_CallScreenState` with `ValueNotifier<CheckpointSnapshot?>` (UI-only state on the State, NOT on the bloc — same pattern as `_awaitingPlaybackIdle` from Story 6.4 and `_canvasInFallback` from Story 6.2). Server-side: tiny 1-method addition to `CheckpointManager.emit_initial_state()` + 1-line wiring in `bot.py::on_first_participant_joined` so the stepper has initial data on call connect (uses the SAME `checkpoint_advanced` envelope shape — single-wire-format approach). 3 up-front deviations: (#1) NO `v: 1` schema version on envelope (retires deferred-work line 396 with documented decision — additive evolution via `data.{}`); (#2) client reconciles stepper state from `call_end.checkpoints_passed` to close cancel-mid-flight envelope-lost race (retires deferred-work line 406); (#3) conditional Semantics fix if Rive's existing `Semantics(button:true,label:'End call')` traps screen-reader focus — dev-decided at impl time. Hard prerequisite: 6.6 must be `done` (verified ✅). 8 ACs total, ~22 net new client tests + ~2 net new server tests. Pre-commit gates green expected: ruff check + ruff format + pytest (target ≥323) + flutter analyze + flutter test (target ≥379). Smoke Test Gate (11 boxes — initial-state envelope fires, stepper renders on connect, advances animate, adaptive sizing N/A if no scenario ≥7 checkpoints today, TalkBack announces) sits with Walid for `review → done` per Story 6.5 D6. Previously 2026-05-18: Story 6.6 OFFICIALLY DONE — smoke gate fully validated end-to-end on Pixel 9 Pro XL post Dev #29 fix. Happy-path call_id=108 completion in 98s with all 6 checkpoints + Dev #7 suppression + reason=survived; off-topic call_id=109 with checkpoint_unmet on greet + fail_penalty -15. Dev #29 emerged post-deploy: PatienceTracker was sitting downstream of context_aggregator.user() since Story 6.4, but pipecat 0.0.108 LLMUserAggregator absorbs TranscriptionFrames internally — `_cancel_silence_timer()` on user-speech observation was dormant for 1.5 epics. Surfaced on the first 6.6 call (first scenario where user MUST speak mid-ladder), fix amended into the Story 6.6 commit as Dev #29 mirroring CheckpointManager's Dev #5. Pre-commit gates green (321 server tests, ruff clean, flutter 357 unchanged). Force-pushed at SHA 85fdd66. One story = one commit.). Previously 2026-05-15 (Story 6.6 dev-story complete — `in-progress` → `review`. New files: `pipeline/exchange_classifier.py` (~210 LOC) + `pipeline/checkpoint_manager.py` (~270 LOC). Extended `patience_tracker.py` with `hang_up_line_survived` kwarg + `_REASON_SURVIVED` + `apply_exchange_outcome(success)` + `schedule_completion(survival_pct)` + 3-way exit-line/survival_pct switch in `_run_hang_up` + `_VALID_REASONS` validation. Extended `scenarios.py`: deepcopy fix + 4 validators + `exit_lines.hangup`/`exit_lines.completion` loading + `load_scenario_checkpoints` + `load_scenario_base_prompt`. `bot.py` pipeline now `context_aggregator.user() → checkpoint_manager → patience_tracker`. **5 deviations**: #1 two distinct survival_pct formulas; #2 `llm._settings.system_instruction =` direct mutation (NOT `LLMContext.set_messages` — OpenAI adapter prepends from settings at every invocation, LLMContext empty at boot, mutating it would produce 2 system messages with a warning); #3 `exit_lines.hangup` shared by silence+inappropriate; #4 last_character_line sourced from `llm_context.get_messages()` (NOT frame observation — LLM TextFrame flows DOWNSTREAM but manager sits UPSTREAM of LLM); naming reconciliation epics.md line 1193 `reason "completed"` → `reason "survived"` for Story 6.5 D4 wire-format alignment. Tests: server 242 → 292 (+50 net new), client 357 unchanged (zero net Flutter per AC8). Déviation-#28 regression net landed in `test_bot_pipeline_wiring.py` — drives TranscriptionFrame through real Pipeline+PipelineTask+PipelineRunner (retires DW line 369). DW lines 350+357 retired by scenarios.py changes. Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Smoke Test Gate (live VPS deploy + journalctl proofs) sits with Walid for `review → done` per Story 6.5 D6. Awaiting `/commit`. Previously 2026-05-15: Story 6.6 spec created — `backlog` → `ready-for-dev`. CheckpointManager Pipecat FrameProcessor + ExchangeClassifier async LLM service (qwen-flash, 2.0s timeout); mutates the shared LLMContext system message in-place on advance; emits `checkpoint_advanced` envelopes on the data channel; routes all-checkpoints-passed through new `PatienceTracker.schedule_completion(survival_pct=100)` + new `_REASON_SURVIVED` path + new `hang_up_line_survived` kwarg (sourced from YAML `exit_lines.completion`); new `PatienceTracker.apply_exchange_outcome(success)` finally wires Story 6.4 dormant `fail_penalty`/`recovery_bonus`. `resolve_patience_config` deepcopy fix + 4-field type/range validation (retires deferred-work lines 350, 357). Pipeline-drive contract test in test_bot_pipeline_wiring.py is the Déviation-#28 regression net (retires deferred-work line 369). Zero net Flutter code (Story 6.7 owns CheckpointStepper UI). Hard prerequisite: 6.5 done ✅. 3 up-front deviations: (#1) two distinct survival_pct formulas (100% for `survived`, meter-ratio for `character_hung_up`/`inappropriate_content`); (#2) system-prompt swap via `LLMContext.set_messages`, NOT `_settings.system_instruction`; (#3) YAML `exit_lines.completion` → `hang_up_line_survived`. Naming alignment: epic line 1193 amended `reason "completed"` → `reason "survived"` to match wire format from Story 6.5 D4. Previously 2026-05-13: Story 6.5 code review complete; D1-D6 decisions resolved by Walid-delegated trust pass + 27 patches applied. AC7/AC10 amended for Deviation #12 hybrid (EmpatheticErrorScreen now accepts bodyOverride/retryLabel/semanticsLabel; NoNetworkScreen wires call-context copy + 'Go back' + 'Close' Semantics label). Server: rollback path FLIPs to 'failed' instead of DELETE (D3 / Deviation #14); EndCallIn.reason Literal widened with 'survived' (D4 / Deviation #13); EndCallOut.status narrowed to Literal['completed','failed'] (Deviation #21); `/end` ownership check moved before BEGIN IMMEDIATE (Deviation #19); aiosqlite locked-DB → 503 with Retry-After (P17); defensive started_at parse (P20); idempotent NULL duration_sec logs ERROR (P6); `_safe_livekit_delete_room` consolidates timeout + shield + log-only-on-cancel (Deviation #22); janitor uses julianday() comparison (Deviation #20) + failure-streak backoff + bounded shutdown (Deviation #23). Client: 3 exit paths funnel through `_fireEndCall` with `_endPostFired` guard + `_pendingEndCalls` set awaited in close() (Deviation #17); connect-failure catches now POST 'network_lost' (Deviation #16); 'unknown' reason coerced to 'character_hung_up' client-side (Deviation #18); `_endCallSilently` log line scrubbed of raw exception text (P18 — PII protection). Tests: server +29 (P25 rollback ordering, P20 malformed started_at, P10 failed-row idempotent, P6 NULL duration log, P17 503, P22 no-write-lock-on-404, D4 survived, P23 +00:00 offset sweep, P24 cost_cents NULL); client +6 (P1 connect throw + timeout + mic, P4 double-POST guard, P5 unknown coercion, P3 close-awaits-in-flight). D5: prod_snapshot stays hand-doctored, tracker entry added to deferred-work.md for post-deploy refresh. D6: spec ligne 412 amended to formalise "deploy-gates are stop-ship for review→done, not in-progress→review". Smoke Test Gate still owned by Walid. Story 6.4 status unchanged.)
project: surviveTheTalk2
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Voice Pipeline Proof of Concept (Phase 0)
  epic-1: done
  1-1-initialize-monorepo-and-deploy-server-infrastructure: done
  1-2-build-pipecat-voice-pipeline-with-sarcastic-character: done
  1-3-create-minimal-flutter-app-with-voice-call: done
  1-4-validate-poc-kill-gates: done
  epic-1-retrospective: done

  # Epic 2: UX Design Completion & Asset Creation
  epic-2: done
  2-1-design-onboarding-flow-screens: done
  2-2-design-first-call-incoming-call-animation: done
  2-3-design-call-ended-transition-screen: done
  2-4-design-debrief-screen: done
  2-5-design-paywall-screen: done
  2-6-create-rive-character-puppet-file: done
  2-7-create-visual-assets: done
  epic-2-retrospective: done

  # Epic 3: Scenario Content Design & Authoring
  epic-3: done
  3-0-build-scenario-calibration-testing-tools: done
  3-1-define-scenario-structure-and-authoring-format: done  # review corrections applied
  3-2-create-launch-scenarios: done  # review corrections applied, calibration deferred to post-Epic 6
  epic-3-retrospective: done

  # Epic 4: User Onboarding & Authentication
  epic-4: done
  4-1-restructure-flutter-project-to-mvp-architecture: done  # review corrections applied
  4-1b-implement-design-system: done  # review corrections applied 2026-04-16 (WCAG fixes + test hardening + naming + OFL)
  4-2-build-fastapi-server-with-passwordless-auth-system: done  # review corrections applied 2026-04-16
  4-3-build-email-authentication-flow-in-flutter: done  # review corrections applied 2026-04-21
  4-4-build-consent-ai-disclosure-and-microphone-permission-flow: done  # review corrections applied 2026-04-22
  4-5-build-first-call-incoming-call-experience: done  # review corrections applied 2026-04-23
  epic-4-retrospective: done  # 2026-04-23 retro completed, doc: epic-4-retro-2026-04-23.md

  # Epic 5: Scenario Browsing & Progression
  epic-5: done
  5-1-build-scenarios-api-and-database: done  # code review completed 2026-04-24: 15 patches applied (seeder hardening, route ValidationError→SCENARIO_CORRUPT, stricter ordering tests, parametrised corruption coverage, ok_list delegates to ok, ScenariosListOut meta), 126 pytest green, pipecat.service reverted to www-data (5.1-CI-deploy sub-tag), 2 infra issues deferred to deferred-work.md
  5-2-build-scenario-list-screen-with-scenariocard-component: done  # code review 2026-04-27: 6 decisions resolved, 7 patches applied (1 deferred to Story 6.1 — back-press blocking on /call), 1 deferred item closed (debrief back-button), 156 flutter tests green, flutter analyze clean
  5-3-build-bottomoverlaycard-and-daily-call-limit-enforcement: done  # code review 2026-04-28: 3 decisions + 8 patches applied (TOCTOU BEGIN IMMEDIATE + busy_timeout, BOC visual drift documented in AC6, Semantics gate, mounted check, staticContentHeight recompute, conditional padding, frozen clock for paid-tier tests, AppSpacing tokens, buildWhen removed, predicate-based test finder), 8 items deferred to Epic 6/8/etc.
  5-4-build-content-warning-display-for-intense-scenarios: done  # code review 2026-04-29: 3 decisions resolved + 6 patches applied (rename dialog→sheet, AC7 minimumSize/tapTargetSize removal, release-build null-guard, AC7 320×1.5 textScale test surfaced 390px overflow → fixed via SingleChildScrollView, scrim-dismiss screen-integration test, deviation #5 declared for YAML edits), 6 items deferred (token discipline, double-tap re-entry, mid-dialog limit bypass, RTL, back-press untested, AA contrast claim), 208 flutter tests green
  5-5-refactor-scenario-list-error-screen-with-empathetic-ux: done  # code review 2026-04-29: 7 decisions resolved (6 accept + spec amendments, 1 status quo, 1 deferred HIGH-PRIORITY auth 401), 7 patches applied (ApiException.statusCode propagation, Semantics on HOLD ON/HEADS UP, FlutterError.onError overflow capture, pumpEventQueue × 8, comment fixes, deviation #4 withdrawn), 1 dismissed (false positive). 213 flutter tests green, analyze clean, server pytest 145 green. ⚠️ Auth 401 silent loop deferred to deferred-work.md as MUST-FIX-BEFORE-MVP-LAUNCH
  epic-5-retrospective: done  # 2026-04-29 retro completed, doc: epic-5-retro-2026-04-29.md

  # Epic 6: Animated Call Experience
  epic-6: in-progress
  6-1-build-call-initiation-from-scenario-list-with-connection-animation: done  # code review 2026-04-30: 17 patches applied + 1 from D2 (red AppToast for generic 5xx); 2 decisions resolved; 4 deferred (already in deferred-work.md). Smoke gate fully green at SHA 28fd11c (8/8 boxes); 234 client tests + 154 server tests green; flutter analyze + ruff clean.
  6-2-build-call-screen-with-rive-character-canvas: done  # code review 2026-04-30: 14 patches applied (3 HIGH: Semantics scope, duplicate Rive listener, missing onHangUp event test; 3 MEDIUM: onFallback idempotency, FileLoader leak guard, StateMachine reference capture; 8 LOW: bang-fallback, AC9 sibling assertion, debug asserts, real-asset tests, catalog test, CallError rationale, rootNavigator pop symmetry, _popScheduled guard); 4 decisions resolved (catalog names approved, sigma 3 confirmed intentional DoF — saved to memory, JPG avatar visually validated, FPS check OK on Pixel 9 Pro XL); 6 deferred (already in deferred-work.md). Final: flutter analyze clean, 258 tests green (+14 from baseline 244).
  6-3-implement-emotional-reactions-and-lip-sync-via-data-channels: done  # code review 2026-05-01: 16 patches applied (3 HIGH: cleanup hook + pts_ns is None + httpx cleanup investigated; 10 MED: emit-layer rejection filter + generation counter + finalized=False default + regex Markdown fence + reserved-emotion test + subscribe-once assertion + API key validation + mounted check + cross-contract tests + Deviation #5 doc + Box 6 already done; 3 LOW: client allow-list mirror + prune-releases.sh `$resolved` + DataChannelHandler try/catch); 8 deferred to deferred-work.md; 4 dismissed (per-spec await design, Flutter sync-dispose pattern, status-flip-with-Note-8, OpenRouter slug smoke-proven). 198 server tests + 275 client tests green; flutter analyze + ruff clean. Smoke gate revealed catastrophic word-level lip-sync 2026-05-01 → fix shipped via 6-3b before close-out.
  6-3b-improve-lip-sync-from-word-to-syllable-level: done  # 2026-05-12 architectural pivot + code review. Server-side syllable-level viseme emission (original scope) abandoned after smoke trace showed data-channel lag dwarfing chunk-level precision — SCTP slow-start put visemes 2-3 s behind audio on cellular. Replaced with client-side native PCM-buffer analysis on the audio thread: Android `AudioClockChannel` hooks flutter_webrtc's playback callback and feeds each chunk to `FormantVisemeAnalyzer` (RMS + ZCR + 512-pt FFT band-energy formant detection over F1/F2). Sync is now structural (analysis lives on the playback thread) and cannot drift. Dropped: `viseme_emitter.py` + tests, `audio_clock_bridge.dart`, data-channel viseme path on both sides. OVRLipSync was prototyped end-to-end (JNI wrapper, NDK config, .so shipping) and rejected — Meta's `libOVRLipSync.so` isn't 16 KB-aligned, would fail Google Play. Code review 2026-05-12: 4 decisions resolved + 11 patches applied (CRIT native-callback detach + post-cancel race; HIGH audioFormat guard + channelCount==0 + true stereo downmix; MED reflection getDeclaredField + Dart cast safety; LOW bandEnergy exclusive bounds + strengthened test + story-tag drift 6.3d/e→6.3b across 11 files). New JVM unit-test source set at `client/android/app/src/test/kotlin/` with FftTest (4) + FormantVisemeAnalyzerTest (6) — first DSP-level test coverage. 2 items deferred to deferred-work.md (latent data.size check, brittle flutter_webrtc reflection coupling). 279 client tests + 175 server tests + 10 JVM Kotlin tests green; flutter analyze + ruff + Android assembleDebug clean. Walid validated lip-sync visually on Pixel 9 Pro XL 2026-05-12.
  6-4-implement-silence-handling-and-character-hang-up-mechanic: done  # code review 2026-05-13: 17 patches applied + 4 decisions resolved (D1 constructor restored 4 dormant kwargs / Deviation #15; D2 hang-up tail asyncio.Event+wait_for / Deviation #16; D3 emotion debounce omission accepted / Deviation #17; D4 anti-pattern reworded for control-signal carve-out). Server patches: _hang_up_in_progress try/finally, handle_playback_idle concurrent-ladder guard, TranscriptionFrame finalized gate, abuse_classifier try/except, BSF direction guard, validate initial_patience>0 (constructor + resolve_patience_config), extended config log line (8 fields), reordered empty-text cancel. Client patches: HangUpPressed clears _remoteEndDrainTimer+_remoteEndPending, _onPlaybackDrained emit.isDone re-check, _publishPlaybackIdle null-log + sync try/catch, VisemeScheduler Dart-side dedup, RoomDisconnectedEvent mirrors _roomDisconnected, DataChannelHandler logs on fallback. 16 items deferred to deferred-work.md (dev OQ6/OQ7/DW1/DW5 + other pre-existing concerns). 200 server tests (+7 net new: P3, P5, P6, P7, P9, P17, resolve_patience_config validation) + 301 client tests (+3 net new: P1, P14, P12) green; ruff + flutter analyze clean. Awaiting /commit.
  6-5-build-voluntary-call-end-and-no-network-screen: done  # 2026-05-15 closed. Shipped well beyond the original spec: 28 deviations total (#1-#12 up-front, #13-#23 from /bmad-code-review, #24-#26 from post-deploy PD1-PD3 Pixel 9 Pro XL device testing, #27 from user-design feedback, #28 from Story 6.4 silent-regression discovery). Server: migration 008 (status column + CHECK) + 009 (gifted column + CHECK) + POST /calls/{id}/end (auth, ownership-before-BEGIN-IMMEDIATE, idempotent, 503-on-busy, julianday() janitor with failure-streak backoff, _safe_livekit_delete_room shield+5s timeout, bounded 30s shutdown) + 3-per-day gift quota shared across network_lost / character_hung_up<30s / inappropriate_content<30s + Déviation #28 1-word fix DOWNSTREAM→UPSTREAM in patience_tracker.py + cross-reference contract test + new server/CLAUDE.md. Client: connectivity_plus mid-call detection + EndCallRetryStorage + EndCallRetryService with 3 drain triggers (bootstrap, onConnectivityRegained, AppLifecycleState.resumed) + EndCallResult model + CallEndedNoticeScreen 4 copy variants (English, optimistic) + EmpatheticErrorScreen overrides. Tests: server 200→242 (+42), client 279→357 (+78). All pre-commit gates green. Smoke Test Gate validated end-to-end on Pixel 9 Pro XL 2026-05-15 — Test 1 (network_lost row 92 gifted), Test 2 (character_hung_up row 96 with patience tracker FINALLY firing post #28), Test 3 (user_hung_up row 97 not gifted). Mini-review by Auditor agent 2026-05-15 returned no blockers (only bookkeeping rot — all 4 findings applied). Deploy: commits f674eaf + 0cefcd7 + #27/#28 follow-ups via CI/CD.
  6-6-build-checkpointmanager-and-checkpoint-aware-exchangeclassifier: done  # smoke-gate validated 2026-05-18 on Pixel 9 Pro XL post-redeploy at SHA 85fdd66: call_id=108 happy-path completion (6/6 checkpoints, reason=survived, 98s, Dev #7 frame suppression confirmed) + call_id=109 off-topic checkpoint_unmet (greet stayed, fail_penalty -15 applied). Dev #29 added post-deploy (PatienceTracker moved BEFORE context_aggregator.user() — same root cause as Dev #5; pipecat 0.0.108 LLMUserAggregator consumes TranscriptionFrames internally, so _cancel_silence_timer() was dormant since Story 6.4; surfaced on the first 6.6 call because it's the first scenario where the user MUST speak mid-ladder). 9 consecutive silence ladder cancelled events confirmed Dev #29 fix live. All 12 smoke gate boxes green. Code review 2026-05-18: 5 decisions resolved + 14 patches applied. D1 asyncio.Lock around terminal-turn path + post-acquire re-check (race where concurrent process_frame cancels in-flight task → Dev#7 suppression silently broke); D2 kept async warning-band UX (option A — realistic restaurant flow, Walid confirmed); D3 prompt-injection resistance via XML <user_response>/<character_line> tags; D4 kept asymmetric finalized default + cross-ref comments + server/CLAUDE.md §1 doc; D5 fail-loud RuntimeError at manager init on missing llm._settings.system_instruction (pipecat API drift detection). Patches: P1 PatienceTracker.__init__ rejects None/bool fail_penalty/recovery_bonus; P2 dismissed (str.format single-pass substitution doesn't crash on user braces); P3 isinstance(x, bool) guard in resolve_patience_config; P4 exit_lines list type-check BEFORE or {} fallback; P5 generation-guard stale-drop logger.debug; P6 _warning_emitted flag moved INSIDE _emit_patience_warning post-push + dual-guard via _warning_task done() check; P7 _pending_survival_pct cleared post-consume in _run_hang_up; P8 dismissed (defensive _VALID_REASONS check is correctly placed); P9 silence_prompt_seconds + silence_penalty validators; P10 _SPEAK_FIRST_DIRECTIVE substring assertion in load_scenario_base_prompt; P11 conftest.py narrowed to (ImportError, ModuleNotFoundError, AttributeError); P12 sprint-status test count corrected; P13 Dev#7 caveat comment on test_pass_through_for_all_frame_types; P14 stale-verdict test docstring rewritten (cancel + generation guard belt-and-braces). 18 items deferred to deferred-work.md "code review of story-6.6 (2026-05-18)"; 28 dismissed as noise/handled/documented-deviations. Tests: server 292 → 321 (+29 net new); client 357 unchanged. Pre-commit gates green: ruff check + ruff format + pytest. Smoke Test Gate per Story 6.5 D6 remains Walid-owned post-deploy.
  6-7-build-checkpointstepper-overlay-for-call-screen: done  # 2026-05-20: Smoke Test Gate 11/11 validated on Pixel 9 Pro XL post Deviation #8 server fix (PatienceTracker.set_checkpoints_passed wiring — retires Story-6.4-era hardcoded `checkpoints_passed=0` in `call_end` envelope, retires deferred-work line 65/74/213 TODO). Code review: 3 decisions resolved + 16 patches applied + 5 deferred + 4 dismissed. Tests: server 326 (+1 net from review), client 373.
  6-9b-classifier-latency-slash: done  # 2026-05-26 smoke gate Pixel 9 Pro XL complete (3 calls: 148 calm-room survived, 150 café-noisy retry survived, 151 off-topic Mode A user-hung-up). Classifier scope ✅ delivered: Groq Llama 3.3 70B in prod at 121ms p50 / 320ms p95 VPS (98.7% accuracy, 0 FPs vs Qwen baseline 587/859/94.7%/3 FPs). Smoke gate boxes 1-3+5+6 all ✅ pass. Box 7 visuel — Walid signaled UX issues but they are orthogonal to 6.9b classifier scope. **4 findings surfaced during smoke gate are ALL pre-existing (NOT 6.9b regressions)**: (a) 🚨🚨 Cartesia silent stall — pipeline freezes ~30% of calls on multi-frame Tina responses, REPRODUCED in call_id=149 + call_id=151 (Cartesia inchangé depuis Story 6.6 — bug latent), (b) 🟠 silence ladder ticks during bot speaking — `patience_tracker._run_silence_ladder` doesn't cancel on `BotStartedSpeakingFrame` (pre-existing depuis Story 6.4), (c) 🟠 `_LADDER_IMPATIENCE_AT=3.0` hardcoded constant trop agressif UX-wise (pre-existing Story 6.4), (d) 🟡 Waiter `clarify.success_criteria` misaligned with Tina's drink-clarify prompt (pre-existing scenario calibration). All 4 persistées dans `deferred-work.md` sous heading "Deferred from: Story 6.9b Pixel 9 smoke gate (2026-05-26)". Bundled into next story 6.13 hardening per Walid 2026-05-26 ask "Si les stories d'après ne règlent pas ce souci, il va falloir le faire dans cette épique". 6-13-epic-6-prelaunch-hardening: ready-for-dev — prioritised AHEAD of 6.10/6.11/6.12 to stabilise pipeline before further story work. Previously 2026-05-22 Phase A.5 landed via `/bmad-dev-story` interactive session — Groq Llama 3.3 70B migration complete; flipped `in-progress → review`. Smoke gate (Task 6) Walid-owned per Story 6.5 D6 deploy-gate convention. Bench history: 6 reports under `_bmad-output/implementation-artifacts/calibration-tests/` covering Qwen-via-OpenRouter (local + VPS), DashScope direct (Qwen 3.5 + 3.6 Flash, local + VPS), Groq Llama 3.1 8B (local + VPS), Groq Llama 3.3 70B (local + VPS). The load-bearing report: `classifier_benchmark_2026-05-22T09-29-19Z.json` (VPS Groq 70B = 121 ms p50 / 320 ms p95 / 98.7 % accuracy / 0 false positives vs Qwen-prod 587 ms / 859 ms / 94.7 % / 3 FPs — winner on every axis, +10 €/mois at 100-user MVP scale). Lever 1 (skip OpenRouter proxy via DashScope direct) was tested and ABORTED — provided 0 gain (Qwen hosted in Singapore, OpenRouter overhead ~10 ms not the spec's hypothesized 100-300 ms). The spec's strategic frame "Qwen 1046 ms median → concept dead approaching" did not hold empirically — Qwen-prod measured at 587 ms p50, comfortably under PRD 800 ms target. Migration to Groq is justified by accuracy gain (98.7 % vs 94.7 %, 0 false positives vs 3) + Story 6.12 unblock (sync-verdict-everywhere viable with sub-300 ms classifier) rather than raw latency necessity. **Modifications**: `server/config.py` (added `groq_api_key: str` required + flipped `classifier_model` default `qwen/qwen3.5-flash-02-23` → `llama-3.3-70b-versatile` + `extra: "ignore"` on model_config), `server/pipeline/exchange_classifier.py` (full module docstring rewrite + `_OPENROUTER_URL/MODEL` → `_PROVIDER_URL/MODEL` to Groq + kwarg `openrouter_api_key` → `api_key` for provider-neutrality + dropped `reasoning: {enabled: false}` since Llama has no thinking mode + persistent-client lifecycle UNCHANGED preserving all Story 6.9 Deviations #5-#10), `server/pipeline/bot.py` (wiring `api_key=settings.groq_api_key, model=settings.classifier_model`), `server/tests/conftest.py` (added `GROQ_API_KEY: "test-groq"` to TEST_ENV_VARS), `server/tests/test_config.py` (added `groq_api_key` required + flipped classifier_model default test from Qwen baseline to Groq winner + flipped env-override test direction Qwen→Groq to Groq→Qwen rollback), `server/tests/test_exchange_classifier.py` (`_make_classifier` updated to `api_key="test-key"` + `test_init_raises_on_empty_api_key` updated + the smoke-witness test rewritten as `test_classify_posts_to_groq_with_openai_compat_shape` to lock the Groq URL+model+no-reasoning contract — pre-migration this was the Story 6.3/6.6 OpenRouter smoke witness, now serves as Story 6.9b migration witness), `server/tests/test_checkpoint_manager.py` (5 occurrences of `ExchangeClassifier(openrouter_api_key=...)` bulk-replaced to `api_key=`), `server/tests/test_bot_pipeline_wiring.py` (added `api_key=settings.groq_api_key` source-text assertion + test-fixture construction updated), `deploy/.env.example` (repositioned `GROQ_API_KEY` from benchmark-only block to required-prod slot + updated `CLASSIFIER_MODEL` comment for Groq default + Qwen rollback path + updated `DASHSCOPE_API_KEY` comment to "Lever 1 exploration, kept for slot"), `server/scripts/benchmark_classifier.py` (added during Phase A.5: `load_dotenv()` for server/.env auto-load, retry-on-429 with Retry-After honoured, 3 extra provider configs `groq_llama_3.3_70b` + `qwen3.5/3.6_flash_via_dashscope`, top-level `enable_thinking: false` flag for DashScope which the OpenAI-SDK-only `extra_body` convention silently drops). Tests: server 375 unchanged (Phase A.5 swapped tests in-place rather than adding net new). Pre-commit gates GREEN: ruff check + ruff format + pytest 375 + flutter analyze + flutter test 373. **Phase B remaining for Walid** (5 items): (a) rotate the GROQ + DASHSCOPE + OPENROUTER keys that leaked into chat history today, (b) provision new GROQ_API_KEY on `/opt/survive-the-talk/.env`, (c) Task 2.3 cosmetic memory update `project_story_6_9b_classifier_latency_slash.md` §1 with real numbers, (d) Task 6 7-box Smoke Test Gate on Pixel 9 Pro XL (expect Groq p50 ~120ms / p95 ~320ms confirmed in journalctl with `LATENCY_PROBE=1`), (e) Task 6.5 `deferred-work.md` line 451 re-tag + flip `review → done`. Previously 2026-05-21 Phase A complete via `/bmad-dev-story`. Phase A: Settings.classifier_model refactor (retires DW line 450) + EXCHANGE_CLASSIFIER_PROMPT compression (~600-700 → ~340 tokens preserving all 6 principles + P21 markers) + 5 principle-regression tests + benchmark_classifier.py harness (~460 LOC, 4 providers, Deviation #1 recommendation logic) + 75-sample synthetic corpus + 5 harness smoke tests + 3 .env.example slots. 7 ACs + 7 up-front deviations + 7-box Smoke Test Gate. Scope: benchmark Groq Llama 3.1 8B + Cerebras Llama 3.1 8B + Anthropic Haiku 4.5 vs Qwen 3.5 Flash on ≥50 labeled past-call transcripts (AC1 + new `scripts/benchmark_classifier.py` + corpus fixture), migrate ExchangeClassifier to winner preserving Story 6.9 persistent-client lifecycle (AC2, Deviation #6), activate streaming where measured gain ≥100 ms (AC3, Deviation #2), compress EXCHANGE_CLASSIFIER_PROMPT ~600-900 → ~300-400 tokens (AC4, Deviation #3) validated by 6 principle-regression tests (default-to-MET + intent-over-literal + synonym/brand + fragmented + restatement + current-objective-only), target p50 ≤300ms / p95 ≤500ms / no single classify >800ms on real prod traffic (AC5). Up-front Deviations: #1 empirical provider selection (cost matrix informational, abort if no provider beats Qwen on BOTH latency AND accuracy → fall back to Story 6.12 option F visual-first), #4 5-scenario re-calibration OUT OF SCOPE for in-progress→review (Walid-owned manual follow-up; retags `deferred-work.md` line 451), #6 persistent-client lifecycle from Story 6.9 (Deviations #5-#10) is sacred — every existing test_exchange_classifier.py invariant survives migration, #7 consecutive-None backstop N=5 stays in place as safety belt. Retires `deferred-work.md` line 450 (hardcoded model id → Settings.classifier_model). Strategic frame (must-read before dev): collapses 3 structural problems into one fix — restores Story 6.8 AC5 universal ≤2000ms ceiling (retracted by Story 6.9 D2 for terminal turn), collapses Deviation #5 verdict=None soft-lock window, unblocks Story 6.12 architectural decision (sync-verdict vs visual-first feedback). Hard prerequisite — 6.9 done ✅. Blocks Story 6.10, 6.11, 6.12 (all 3 depend on classifier reliability + latency). Full context: `memory/project_story_6_9b_classifier_latency_slash.md`. Test target: server +5-6 net new (5 principle-regression in test_exchange_classifier.py + 1 harness smoke in test_benchmark_classifier.py) → ≥369. Client 373 unchanged. Awaiting `/bmad-dev-story` invocation.
  6-11-noisy-environment-detection: done  # 2026-05-30 DONE — Pixel 9 smoke gate validated by Walid ("test OK, on peut valider"). Required TWO smoke-fixes after the first device tests: (smoke-fix #1) the noisy exit line was queued behind the in-flight character reply + flushed by interruptions → push an InterruptionFrame before it (scoped to noisy_environment); (smoke-fix #2, Walid's idea) a LOUD CONTINUOUS parasite kept interrupting the line every ~1.5s → new `pipeline/input_gate.py` InputGate wired first (after transport.input(), before STT), armed by EnvironmentMonitor on detection to MUTE the mic (drops audio/VAD/interruption/transcription frames, the STTMuteFilter set) so the line plays uninterrupted + the parasite stops polluting the LLM/classifier. Tina's line also reworded for clarity. Final validated flow: loud parasite → detect → mute mic → full exit line → call_end noisy_environment → "Background voice was too loud" screen → gifted. Honest limitation (not a blocker): diarization-based detection can MISS when the parasite dominates and becomes cumulative-primary (the patience hang-up backstops that); robust voice-isolation is post-MVP per Story 6.9. Server 412→488 tests across the story+fixes; client 399. All gates green; deployed bbafa30. **Also shipped same-day (separate commit 4ca78fe, NOT part of 6.11): checkpoint judge default-to-MET → default-to-UNMET** — the multi-goal judge was passing EVERY checkpoint regardless of input (call_id=203/204); now requires genuine accomplishment while keeping B1 tolerance. Previously 2026-05-30 /bmad-dev-story COMPLETE — `ready-for-dev` → `review`. Parasitic-background-voice detection via Soniox speaker diarization. NEW `server/pipeline/environment_monitor.py` (~205 LOC, EnvironmentMonitor FrameProcessor): observes finalized user TFs, reads per-token speaker ids from `TranscriptionFrame.result` (Deviation #6 — NOT `frame.metadata` as the spec assumed; verified against installed pipecat 0.0.108 soniox parser which sets `result=self._final_transcription_buffer`), primary speaker = cumulative-token-dominant (the user), a turn is parasitic if any non-primary speaker ≥3 tokens, fires ONCE when ≥2 of last 4 turns parasitic (Deviation #2 grace period), pushes `env_warning` envelope DOWNSTREAM + calls `patience_tracker.schedule_noisy_environment_exit()`. `bot.py`: `enable_speaker_diarization=True` on Soniox (AC1, zero extra cost), EnvironmentMonitor wired BEFORE emotion_emitter (raw-TF observation, Story 6.6 Dev #5 rule), `hang_up_line_noisy_environment` threaded into PatienceTracker. PatienceTracker: `_REASON_NOISY_ENVIRONMENT` + `_VALID_REASONS` 4→5-tuple, `hang_up_line_noisy_environment` kwarg, `schedule_noisy_environment_exit()` (mirrors schedule_completion, NO survival_pct override → meter-ratio per Deviation #8), exit-line branch in `_run_hang_up`. `prompts.py`: `NOISY_ENVIRONMENT_EXIT_LINE_DEFAULT` constant (scenario-agnostic, AC5). `scenarios.py`: loads `exit_lines.noisy_environment` → fallback to constant. `the-waiter.yaml`: Tina's override; other 4 scenarios inherit default. `schemas.py`: `EndCallIn.reason` Literal +`noisy_environment`. `routes_calls.py`: `noisy_environment` added to `_GIFT_ANY_DURATION_REASONS` (Deviation #7 — NOT a new `_compute_gifted()` helper which never existed; always-gifted/no-duration-gate per Deviation #3 BUT kept under the shared 3/day quota ceiling as the self-limiting anti-abuse backstop). Client: `env_warning_payload.dart` (value class), `DataChannelHandler.onEnvWarning` + `env_warning` case (defensive parse), `noisy_environment_banner.dart` (amber `AppColors.warning` — Deviation #10, the spec's `statusWarning` token doesn't exist; `Icons.volume_off`, dark-on-amber, SizedBox.shrink when null), wired into CallScreen as top overlay below the checkpoint HUD via `_envWarningNotifier` (UI-only-on-State pattern), `showsNotice` routes `noisy_environment` to the notice screen, `CallEndedNoticeScreen` 5th variant ("Background voice was too loud" + "Got it" CTA) via new `NOISY_ENVIRONMENT` code in `EmpatheticErrorScreen` (icon/title/body — Deviation #11). Task 7.3 N/A (Deviation #9 — no client `CallEndPayload` reason Literal exists; call_end reason is a free String end-to-end). Tests: server 449→476 (+27 net: 12 test_environment_monitor + 3 patience_tracker + 2 scenarios + 1 prompts + 2 calls + 2 bot_wiring + ordering); client 390→399 (+9 net: 3 env_warning routing + 1 payload + 4 banner + 1 notice variant). Pre-commit gates ALL GREEN: ruff check + ruff format + pytest 476 + flutter analyze (No issues) + flutter test 399. Smoke Test Gate (Tasks 12.4 deploy + 7-box gate, 12.5 `review → done`) reserved for Walid per Story 6.5 D6 — REAL parasitic voice (YouTube near phone) needed; the detection + exit-line + refund paths are unexercised by unit tests on a live call. Awaiting `/commit`. Previously 2026-05-21 Story 6.11 spec drafted post Story-6.9 cocktail-party limitation analysis. Scope — detection of parasitic background voices via Soniox V4 native speaker diarization (zero extra cost — just `enable_speaker_diarization=True`), in-character sarcastic exit line (generic `NOISY_ENVIRONMENT_EXIT_LINE_DEFAULT` constant in prompts.py + per-scenario YAML override via `exit_lines.noisy_environment` mirroring Story 6.6 pattern), gifted=True automatic refund (no duration gate, user can't control their environment), client banner widget overlaid on Rive character during exit-line playback, 5th CallEndedNoticeScreen variant. New file `pipeline/environment_monitor.py` (~150 LOC) — EnvironmentMonitor FrameProcessor observes user TFs, tracks sliding window of last 4 turns, triggers on ≥2-of-4 turns with non-primary speaker ≥3 tokens each (early-warning + grace period to avoid false positives). Emits new `env_warning` envelope + calls new `PatienceTracker.schedule_noisy_environment_exit()`. Pipeline wiring — EnvironmentMonitor BEFORE EmotionEmitter (raw TF observation, mirror Story 6.6 Dev #5). 5 up-front deviations — #1 generic line top-level constant overridable per scenario, #2 detection requires 2-of-4 turns not instant (false-positive guard), #3 gifted=True automatic for noisy_environment (no <30s gate unlike character_hung_up/inappropriate), #4 two distinct envelopes (env_warning during call + call_end with new reason), #5 server-side only detection (zero mobile audio analysis). Hard prerequisite — Story 6.10 done (goal-based dialogue). Test counts — server +12-15 new (target ≥360), client +5-8 new (target ≥385). 7-box smoke gate — baseline calm no-false-positive + YouTube positive detection + refund verified + per-scenario exit line + banner UI + 2 scenarios coverage + clean logs. Scheduled order — 6.9 close → 6.10 implement → 6.11. Awaiting Walid sign-off.
  6-13-epic-6-prelaunch-hardening: done  # 2026-06-01 review → done (Walid approval — pre-launch pipeline hardening pass closed; code committed at 8d44cf2). Previously 2026-05-27 /bmad-code-review COMPLETE on range 436144f..HEAD (3 adversarial layers). 2 decisions resolved + ALL 5 patches applied (P2 incl.) + 8 deferred + 6 dismissed. P2 AC4 widget test SHIPPED (`app_test.dart`) — and EXPOSED + FIXED a real prod bug: the 401 toast used `Overlay.of(navigatorKey.currentContext)` which threw (Navigator's Overlay is a child, not ancestor of that context) and the exception was swallowed by the interceptor's catch → toast never rendered; fixed via new `AppToast.showInOverlay(OverlayState)` + `navigatorKey.currentState?.overlay`. Client tests now 382. Status back to `review` (all review findings resolved; only the Walid-owned Pixel 9 smoke gate remains for `review → done`, and the P1 silence-ladder fix needs silence-path smoke re-validation). P1 CRITICAL: stage-2 silence prompt self-cancelled the ladder (AC2 BSF branch missing `_self_speaking` guard → anger+hang-up never fired) — FIXED + regression test `test_stage2_prompt_BSF_does_not_self_cancel_the_ladder`. P3 warm-up task retained via `_BACKGROUND_TASKS`. P4 `cartesia_api_key` defaulted to "". P5/D1 401 guard made static + auto-reset on `onResponse` (fixes per-instance double-toast + post-relogin silent-loop) + 2 tests. D2 prod_snapshot.sqlite REGENERATED from live VPS (real post-010 shape, replaces hand-edit). P2 AC4 widget test (HIGH) LEFT AS ACTION ITEM (full-app widget test = judgment/flakiness). Gates GREEN: ruff + ruff format + pytest 416 + flutter analyze + flutter test 381. review → in-progress (P2 action item + P1 changes silence-ladder runtime behaviour → needs Walid silence-path smoke re-validation). Awaiting /commit (one amended commit per Walid convention). Previously 2026-05-26 dev-story COMPLETE, in-progress → review. All 7 ACs landed across server + client. New file `server/pipeline/tts_watchdog.py` (~190 LOC, TTSWatchdog FrameProcessor for AC1) + 6 new tests in `server/tests/test_tts_watchdog.py`. AC2: PatienceTracker grows `BotStartedSpeakingFrame UPSTREAM` cancel branch + cross-reference contract test for direction parity (mirror of the BSF-stopped contract test). AC3: full per-difficulty plumbing — `ladder_impatience_seconds` becomes REQUIRED kwarg on `PatienceTracker.__init__`; `_DIFFICULTY_PRESETS` updated (easy=4.5/medium=3.5/hard=2.5); `_PATIENCE_OVERRIDE_KEYS` extended to 8 keys; new range validator `0.5 ≤ x ≤ 10.0` in `resolve_patience_config`; all 5 scenario YAMLs grow `ladder_impatience_seconds: null` override; new migration `010_scenarios_ladder_impatience.sql` (ALTER TABLE ADD COLUMN REAL); schemas/queries/seed/routes_scenarios all extended to mirror the silence_prompt_seconds plumbing; `tests/fixtures/prod_snapshot.sqlite` updated locally (migration applied + schema_migrations row inserted) so the lifespan-replay test passes against the production-shape baseline. AC4: new file `client/lib/core/api/auth_interceptor.dart` (~80 LOC, cross-cutting Dio 401 handler with global-callback pattern + re-entry guard) + 6 unit tests; `ApiClient` registers the interceptor first in the chain; `App.initState` wires the closure to call `_tokenStorage.deleteToken()` → `ResetAuthEvent` → `AppToast` via `_router.routerDelegate.navigatorKey`; `_AppState._tokenStorage` field hoisted out of the AuthBloc-construction branch so the closure can dereference it regardless of injection path. AC5: both `logger.debug` calls + the TODO comment block deleted from `exchange_classifier.py`; PII surface closed. AC6: Waiter YAML `clarify.success_criteria` rewritten to accept 3 categories (dish variation / re-confirmation / drink answer with synonyms) + new YAML-asserting test in test_scenarios.py. **6 up-front deviations** all preserved (no new deviations introduced during dev). Test counts: server 376 → 394 (+18 net new across test_tts_watchdog ×6, test_patience_tracker ×6, test_scenarios ×5, test_bot_pipeline_wiring ×1); client 373 → 379 (+6 net new in auth_interceptor_test). Pre-commit gates ALL GREEN: ruff check + ruff format + pytest 394 + flutter analyze + flutter test 379. Smoke gate boxes 1+2+4+5+6+7+8+9 (deploy + Cartesia happy-path observation + silence ladder pause + 4.5s impatience anchor + Waiter clarify drink answer + 3-call survived + manual JWT invalidate + journalctl PII grep) sit with Walid for `review → done` per Story 6.5 D6 deploy-gate convention. Box 3 (Cartesia watchdog synthetic-Stop on stall) self-validated via the new server unit test. Awaiting `/commit`. Previously 2026-05-26 Story 6.13 spec drafted post Story 6.9b Pixel 9 smoke gate. Bundles all 🚨 + 🟠 HIGH-priority items surfaced during smoke gate that 6.10/6.11/6.12 do NOT naturally cover. Scope (7 ACs): AC1 Cartesia silent-stall watchdog (timeout + synthetic TTSStoppedFrame to unblock pipeline — fixes the 30% call-killing freeze reproduced in call_id=149 + 151), AC2 silence ladder pause on BotStartedSpeakingFrame (fixes "Are you still there?" firing during bot speech), AC3 _LADDER_IMPATIENCE_AT configurable per difficulty (easy=4.5s, medium=3.5s, hard=2.5s), AC4 auth 401 silent loop fix (Dio interceptor — must-fix-before-MVP-launch from Story 5.5), AC5 remove verbose classifier debug logs (Story 6.9b review D6 carry-over), AC6 Waiter clarify YAML criteria audit + amend, AC7 smoke gate 3-calls-survived on Pixel 9 Pro XL (calm + café + off-topic) zero freeze. PRIORITIZED ahead of 6.10 to stabilise pipeline before goal-based-dialogue rewrite — 6.10 will smoke-test on a buggy pipeline otherwise. Hard prerequisite: Story 6.9b done ✅. Awaiting Walid /bmad-dev-story invocation.
  6-10-goal-based-dialogue: done  # 2026-05-28 UI REFONTE (pre-smoke-gate, folded in via amend): Walid moved the checkpoint HUD fully to Flutter — Rive `.riv` no longer renders checkpoints. Deleted `checkpoint_stepper_canvas.dart` (Rive stepper) + `checkpoint_hint_bubble.dart` (+ tests); added `checkpoint_step_hud.dart` — top gradient box (accent #00E5A0 → transparent), inline check left of step text, shows ONLY the current step, check animates outline→green on completion + swipe-up to next step, out-of-order shows the just-completed step checked then returns to active pending. Server `checkpoint_advanced` envelope now carries full `hints` list (+`goals_met_indices`+`index`) so the widget animates locally; `CheckpointSnapshot` reshaped (hints/metIndices/justFlippedIndex). Deviation #8 (Rive count-renderer) SUPERSEDED by Deviation #9 (full Flutter HUD). Memory `feedback_hud_overlays_are_rive.md` reversed for checkpoints. Animation feel + gradient contrast need Walid on-device validation. Gates green: ruff + pytest + flutter analyze + flutter test. Previously same day: /bmad-dev-story COMPLETE — goal-tracking rewrite (linear _index → `_goals` dict + `classify_multi` one-LLM-call multi-goal verdict). CheckpointManager judges every turn against ALL pending goals; any-order success; off-topic-only failure (AC8). New `EXCHANGE_CLASSIFIER_MULTI_PROMPT` + `classify_multi`/`_parse_multi_classifier_output`/`_post_for_content` (shared HTTP/failure-mode helper, `classify` kept as legacy wrapper). Dynamic system_instruction `base + COHERENCE_CHARTER + REMAINING_GOALS_BLOCK + SUGGESTED_FOCUS_BLOCK` recomposed at init + every flip via 3 shared module helpers also used by bot.py (no drift). Envelope `data` gains `goals_met_indices` (full author-order met set); `index` now = just-flipped goal's index; one envelope per flip. Patience: one apply_exchange_outcome(success=any_flip) per turn; partial credit = success; all-None = neutral infra failure (consecutive-None backstop N=5 + Deviation #7 terminal-turn sync classify preserved, redefined `met_count+1>=total`). **NEW Deviation #8** — client stepper is Rive count-based (`lastCheckIndex`), NOT the Flutter per-circle widget the spec's AC7 assumed (post-6.7 pivot + `feedback_hud_overlays_are_rive.md`); payload carries full `goalsMetIndices` (forward-compat) but `call_screen.dart` drives the fill from `goalsMetIndices.length` (met COUNT, correct for out-of-order); per-circle non-sequential rendering deferred to a Walid-owned .riv change. Existing scenario YAMLs unchanged (Deviation #3); calibration re-tune (survival rates rise) deferred to 6.10b/6.11 (Deviation #5). Tests: server 416→438 (+22), client 384→388 (+4). Gates green: ruff check+format + pytest 438 + flutter analyze (No issues) + flutter test 388. Smoke gate (Task 14, call_id=137 drift replay) Walid-owned for review→done per Story 6.5 D6. Awaiting /commit. Previously 2026-05-20 Story 6.10 spec drafted post Story-6.9 smoke test analysis (call_id=137 hangup). Architecture shift — `CheckpointManager` moves from linear state machine (`self._index` advances 0→1→2→...) to goal tracking (`self._goals: dict[id, "pending"|"met"]`). Multi-goal classifier (1 LLM call returns set of met goals via structured JSON). Dynamic system_instruction recomposes after each flip — `base + COHERENCE_CHARTER + REMAINING_GOALS_BLOCK + SUGGESTED_FOCUS_BLOCK`. Patience semantics change — `apply_exchange_outcome(success=any_goal_met)` per turn (not per-goal); fail only on truly off-topic turns. Existing scenario YAMLs unchanged. Client envelope extended backward-compat — adds `goals_met_indices: list[int]` so stepper renders non-sequential fills. 7 up-front deviations — #1 architectural rewrite (~25 tests touched), #2 classify_multi method, #3 YAMLs unchanged, #4 dynamic system_instruction, #5 patience semantics shift (calibration impact documented), #6 envelope extends not breaks, #7 Deviation-7 terminal-turn pattern preserved. 14 tasks across 5 phases — server classifier+state model, server envelope+bot.py, server tests (rewrite ~25 + ~10 new), client stepper non-sequential, pre-commit + smoke gate. Expected test counts — server 341→~350, client 373→~376. 7-box smoke gate replays the call_id=137 drift case (must complete with reason=survived). Calibration re-tune is a separate post-Story task (6.10b or 6.11) because survival rates will go up under new architecture. Hard prerequisite — Story 6.9 done (smoke test café pending). Awaiting Walid sign-off on spec before dev starts.
  6-9-dtln-noise-suppression: done  # 2026-05-21 review → done. `/bmad-code-review` complete on cumulative diff 28a67c4..89bd10c. 3 parallel adversarial layers (Blind Hunter 17 + Edge Case Hunter 19 + Acceptance Auditor 5) → triage 5 decision-needed (all resolved live with Walid), 22 patches (all applied), 3 deferred, 8 dismissed. Decisions: D1→consecutive-None counter N=5 (P16); D2→AC5 retract for terminal turn + latency-probe regression (P17, P18, explicit dette temporary, unwound by queued Story 6.9b); D3→8s wall-clock EndpointWatchdog (new file P19); D4→Deviation #10 documenting EXCHANGE_CLASSIFIER_PROMPT rewrite + default-to-MET regression net (P20, P21); D5→DTLN steady-state denoise test across 20 chunks (P22). Patches: iscoroutinefunction cleanup guard, DTLN NaN/Inf + idempotent re-start + CancelledError + empty-or-odd-length bytes + N-consecutive-failure disable, ExchangeClassifier _closed flag + lock-guarded close() + RuntimeError handling, reset_mock + strengthened persistent-client assertion + AC5 enable=True direction + close-during-in-flight + closed-state classify tests. Spec amends: AC5 retract for terminal turn, Deviation #10, File List reconciliation (orig/amend/review tags), Deviation #5 mitigation rephrased to "infra-billing pressure + silence ladder + consecutive-None backstop". Follow-up stories queued: 6.9b "Classifier Latency Slash" (benchmark Groq/Cerebras/Haiku 4.5, streaming, prompt compression, target ~200-300ms — IMMEDIATE post-6.9), 6.12 "Reactive Character Mood" (fix async cause-effect mismatch — post-6.9b). Tests: server 343 → 363 (+20 net new across 4 test files + new test_endpoint_watchdog.py with 6 tests). Pre-commit gates GREEN: ruff check + ruff format + pytest 363 + flutter analyze + flutter test 373 unchanged. New review-patch code (consecutive-None counter, EndpointWatchdog, hardening) is inert on happy-path calls; smoke gate for these failure-mode paths rides with Story 6.9b smoke gate. Café smoke (the happy path) was already validated 2026-05-21 by Walid. Previously POST-SMOKE-TEST AMENDS (uncommitted, pending /commit + /bmad-code-review): 4 reliability deviations added on top of initial commit 0e1ba96 (smoke test surfaced 2 prod bugs — call 138 "Pasta" lost on OpenRouter HTTP timeout draining patience unfairly, call 142 "Cola" lost on Soniox interim-TFs-forever freeze). Amends — #5 verdict=None patience-NEUTRAL (was -15 conservative fallback per Story 6.6; now log-only, infra failure must NOT punish user), #6 HTTP timeout 0.8s→1.5s + outer 1.0s→2.0s (partial revert of Story 6.8 AC4 latency tightening; persistent client #7 compensates median latency loss), #7 persistent httpx.AsyncClient with double-checked-locking _get_client() + idempotent close() + CheckpointManager.cleanup calls classifier.close (saves ~150ms TCP+TLS handshake per call), #8 SonioxSTTService vad_force_turn_endpoint=False (delegate endpoint detection to Soniox native neural VAD instead of Silero — fixes the interim-TFs-forever case from continuous low-level audio). Plus VAD tuning revert (confidence/min_volume/strength all back to baseline after experimental 0.85/0.7/0.8 didn't help cocktail-party case). Test 7 INVERTED (`test_classifier_None_does_not_advance_and_does_not_drain_patience` now asserts `apply_exchange_outcome.assert_not_called()`). 2 new tests for persistent client lifecycle. Server 341→343. Café smoke test re-validated by Walid post-amends — DTLN noise suppression confirmed working in noisy environment. Cocktail-party / parasitic-voice case (YouTube near phone) acknowledged as out-of-scope for noise-suppression DSP — deferred to Story 6.11 (Noisy Environment Detection — spec drafted 2026-05-21 same session). See 6-9-dtln-noise-suppression.md "Post-Smoke-Test Amends (2026-05-21)" section for detailed adversarial review concerns: concurrency (double-checked locking), resource leaks (classifier.close call ordering), failure modes (verdict=None abuse vector trade-off), backwards compat (test 7 inversion impact), Soniox endpoint detection edge cases, and HTTP+outer timeout cumulative impact on Story 6.8 AC4 latency goal. Previously 2026-05-20 Story 6.9 created + dev complete + flipped `in-progress → review`. New file `pipeline/dtln_audio_filter.py` (~140 LOC) — `DTLNAudioFilter(BaseAudioFilter)` wraps livekit-plugins-dtln (Aloware MIT, ~4 MB ONNX, server-side DTLN noise suppression). `bot.py` imports + instantiates + threads via `LiveKitParams(audio_in_filter=audio_in_filter)`. `pyproject.toml` adds `livekit-plugins-dtln>=0.1.5,<0.2.0`; `uv.lock` regenerated (livekit 1.1.3→1.1.8, livekit-protocol 1.1.3→1.1.9, livekit-agents 1.5.11 added, onnxruntime + opentelemetry stack + grpcio + av + sounddevice transitively pulled). 4 up-front deviations — #1 server-side denoising not client-side (vs Krisp $$/Picovoice $6K-year/DeepFilterNet iOS-gap rejected after web research), #2 calling `_process` directly is the public hook per rtc.FrameProcessor abstract base, #3 strength=0.5 default for natural timbre (full-strength sounds thin), #4 passthrough on every error (init crash, mid-call exception, stop-after-error) per server/CLAUDE.md "better than broken". Tests — server 329 → 341 (+12 net new — 11 DTLN unit tests covering round-trip/disabled-passthrough/init-failure-passthrough/FilterEnableFrame-toggle/filter-exception-passthrough/stop-releases-resources/strength-clamped + 1 wiring source-text guard). Pre-commit gates green — ruff check + ruff format + pytest 341 + flutter analyze + flutter test (zero Flutter changes). 6-box Smoke Test Gate reserved for Walid post-deploy (baseline calm + café-bondé real noise test + optional A/B + server CPU + journalctl clean). Awaiting `/commit` + VPS deploy + smoke gate.
  6-8-post-checkpointmanager-scenario-calibration: done  # 2026-05-21 review → done (rattrapage statut oublié post-amend). Originally 2026-05-20 dev-story code complete — `in-progress` → `review`. New file: `pipeline/latency_probe.py` (~85 LOC, env-var gated LatencyProbe FrameProcessor — opt-in via LATENCY_PROBE=1 for smoke-gate AC3 measurement, zero noise in prod). Modified `prompts.py` (+COHERENCE_CHARTER ~200 tokens, 5 conversation-memory rules); `checkpoint_manager.py` (new required `coherence_charter` kwarg + 3-piece swap composition `base + charter + segment` + WARN-on-duplicate guard for AC15 box 3); `bot.py` (import COHERENCE_CHARTER + LatencyProbe + TextFrame + OutputAudioRawFrame; load scenario data early; 3-branch initial_system_prompt composition; `user_speech_timeout 1.8→0.6` with full tuning-context comment + VAD audit block; thread coherence_charter into CheckpointManager; wire 2 LatencyProbe instances); `exchange_classifier.py` (_CLASSIFIER_TIMEOUT_SECONDS 2.0→1.0 + _HTTP_TIMEOUT_SECONDS 1.8→0.8 + docstring update); `the-waiter.yaml` (explicit Menu inventory block + `drink` success_criteria rewrite accepting Coke/Pepsi/Sprite synonyms + `clarify` success_criteria rewrite accepting "as I said before" + inline call_id=118 comments); `test_checkpoint_manager.py` (+`coherence_charter` to `_make_manager` default "CHARTER." + 3 direct constructions updated + 2 system_instruction equality assertions updated + 2 new tests: `test_coherence_charter_appears_in_every_system_instruction_swap` AC8 + `test_warn_on_duplicate_charter_in_composed_prompt` AC15 box 3); `test_bot_pipeline_wiring.py` (+COHERENCE_CHARTER import assertion + coherence_charter="CHARTER." in direct construction + new `test_coherence_charter_threaded_to_checkpoint_manager_and_llm_settings` AC8 wiring test); `test_exchange_classifier.py` (docstring 2.0→1.0). 3 new deviations: #5 LatencyProbe ships as opt-in prod code (not test fixture) per zero-redeploy smoke-gate ergonomics; #6 bot.py initial_system_prompt is 3-branch composition (YAML-first / env-var-fallback / SARCASTIC absolute fallback — all 3 append charter); #7 WARN-on-duplicate guard sits inside _classify_and_advance (catches duplication at the swap moment, not a separate post-write step). Tests: server 325 → 329 (+4 net new), client 373 unchanged (zero net Flutter per Dev Notes). Pre-commit gates green: ruff check + ruff format + pytest + flutter analyze + flutter test. Manual items reserved for Walid post-deploy: empirical user_speech_timeout floor tuning (Task 1.3), LATENCY_PROBE=1 LLM→TTS gap measurement (Task 3.2-3.3), 4-scenario inventory design (Task 8.3), Cartesia voice audition+selection (Task 10), Pass A+B calibration 5 scenarios × 2 = 10 device calls on Pixel 9 Pro XL (Tasks 11+12), 14-box Smoke Test Gate (Task 14). Awaiting `/commit`.
  6-14-tts-audio-jitter-resilience: done  # 2026-06-01 review → done — Walid Pixel 9 smoke gate PASSED (Cartesia voice validated fluid, no "voix rallongée"; checkpoint flips OK). /bmad-code-review landed 2 patches (min-playout-delay [0,2000] boot validator + Cartesia error-logger substring pre-filter so audio chunks skip json.loads — the latter found during a post-deploy "tick feels slow on UI" investigation: server logs proved the URGENT checkpoint_advanced emits mid-speech, Story 6.10 fast-path intact; residual lag = pre-existing async classifier-vs-reply mismatch = Story 6.12, NOT a 6.14 regression) + 5 deferred (InboundAudioStatsLogger dev-only edge cases + room-reuse) + ~12 dismissed, zero AC violations. Deployed a90cbcf (amended one-story-one-commit + force-push). Gates ALL run by Claude: ruff clean + pytest 501 + flutter analyze clean + flutter test 404. Previously 2026-06-01 /bmad-dev-story COMPLETE — in-progress → review. Track 2 (Cartesia made the CODE default in config.py; FreshContextCartesiaTTSService + CARTESIA_FRESH_CTX deleted; new always-on ErrorLoggingCartesiaTTSService logs the documented `cartesia_ws_error` schema at the WS boundary before pipecat's audio_context_available guard can drop it). Track 1 AC2 (server-side jitter buffer: new pipeline/livekit_tokens.py attaches LiveKit min_playout_delay room-config to BOTH call tokens — flutter_webrtc 1.3.0 has NO client knob — env LIVEKIT_MIN_PLAYOUT_DELAY_MS default 200). Track 1 AC1 (client InboundAudioStatsLogger logs jitter/jitterBufferDelay/concealedSamples for before/after measurement, fail-safe). AC3 (ElevenLabs frame levers reviewed + documented, no change). 3 deviations (server-side jitter fix vs spec's client-side guess; launch default already decided by Walid's device A/B; kept token fn names to avoid test churn). GATES ALL GREEN: ruff clean; **full server pytest 497 passed** (incl. new test_livekit_tokens ×6 + test_calls + test_scenarios; -W error clean after JWT secret ≥32B); flutter analyze clean; flutter test 404 (399→404, +5 logger; fixed 15 call_screen failures via fail-safe diagnostic). (Env note: `from livekit import api`→`import aiohttp` initially deadlocked in the dev sandbox on Defender cloud-scanning aiohttp's `_http_parser.pyd` — NOT a code issue/regression, routes_calls already imported it pre-6.14; resolved by warming Defender's verdict, then full suite ran in 183s. See memory/feedback_sandbox_livekit_import_hang.md.) Device smoke gate (AC4.3 Cartesia re-test + 6-box AC6 + empirical 200ms tune) reserved for Walid per Story 6.5 D6. Previously 2026-05-30 spec drafted (Walid ask — the recurring "voix rallongée"/slow-Tina is a stop-ship problem). ROOT CAUSE diagnosed call_id=198: network-jitter receiver-side time-stretch (WebRTC NetEq stretches audio to fill bursty-packet gaps). Server logs CLEAN on the affected call (0 TTS context churn, 0 errors, 0 stalls, 7 normal turns); `DTLN_ENABLED=0` A/B never helped in prior tests → NOT server pacing → pure client-side network jitter. ElevenLabs' larger frames amplify it vs Cartesia's smaller frames (matches Walid's "didn't have this with Cartesia"). **2 tracks:** (1) PROVIDER-AGNOSTIC client jitter-buffer/playout-delay tune (~150-400ms, the launch-blocker fix — bigger receiver buffer absorbs jitter, trades a little latency for smooth playback; validate against WebRTC inbound-audio stats jitterBufferDelay/concealedSamples/insertedSamplesForDeceleration + keep ≤2s PRD ceiling); (2) CARTESIA RE-EVAL post-incident behind the existing `TTS_PROVIDER` flag — **Cartesia support reply 2026-05-28 (Ege Tinmaz) confirms the 2026-05-26 freeze was a RESOLVED platform incident** (status.cartesia.ai/incidents/1j04yfp4048k; both our calls-156/157 reproductions were INSIDE that window → the reason we left Cartesia is largely invalidated), no client pacing needed, the `FreshContextCartesiaTTSService` workaround was unnecessary/counter-productive (remove), documented error schema `{"type":"error","status_code":int,"error":str}` to log. Re-test Cartesia (no freeze ×3 + stretching A/B vs ElevenLabs) → decide launch default. 6 ACs, 6 tasks, device smoke gate (jitter only reproduces on real mobile link). Client-only for Track 1 likely; Track 2 touches tts_factory + Cartesia path + docs. No migration. IMMEDIATE experiment available (Walid's call): flip `TTS_PROVIDER=cartesia` (env+restart, reversible) to A/B today. Awaiting Walid `/bmad-dev-story`.
  6-15-automated-scenario-calibration-harness: review  # 2026-06-01 /bmad-dev-story COMPLETE, in-progress → review. Built a full text-driven scenario VALIDATION ENGINE (Walid reframe). NEW: scripts/calibration_engine.py (reuses prod classify_multi + pure advance_goals/step_patience + compose_goal_system_instruction + YAML loaders; only transport is replaced by text), scripts/calibrate_scenario.py (yes/no verdict + exit code, smart sweep skipping unchanged-PASS via validation-ledger.json behaviour-hash + ENGINE_VERSION, --force/--golden-only/--generate-golden), scripts/simulate_conversation.py, tests/fixtures/golden/waiter_easy_01.json (reviewed worked example), tests/test_calibration_engine.py (28 mocked-LLM tests incl. lenient-judge-fails-the-net AC2 proof + no-reimplementation AC1 guard). REFACTOR (no prod behaviour change, 96 prod tests green): advance_goals+GoalAdvance in checkpoint_manager.py, step_patience in patience_tracker.py, CHARACTER_TEMPERATURE/MAX_TOKENS in llm_provider.py. Golden = universal off-topic seed (zero-authoring, always gating) + auto-gen+review per-checkpoint cases. Calibration N=10 cooperative (band from difficulty: easy 60-80/med 35-55/hard 15-35, ±5=warning) + off_topic guardrail (must not complete). Copy-pasteable failure markdown for AI agents (AC11). Docs: server/CLAUDE.md §6. Gates: ruff clean; safe non-livekit subset 184 passed. **review → done reserved for Walid**: run calibrate_scenario live w/ GROQ_API_KEY (confirm Waiter in-band + --generate-golden review the 4 non-Waiter fixtures) + full pytest before /commit (sandbox livekit import hang). All 13 ACs delivered. Previously 2026-05-30 spec drafted (Walid ask, motivated by the same-day "judge passes EVERY checkpoint" bug that took several manual device calls to catch). Text-driven automated scenario testing via AI agents — NO app/voice loop. Automates the existing manual calibration (Story 3.0 score_transcript tooling + Story 6.8 per-scenario success-rate bands + Story 6.9b benchmark_classifier corpus/reuse-prod-paths precedent). **Sacred principle**: harness MUST reuse the SAME prod code paths (classify_multi + EXCHANGE_CLASSIFIER_MULTI_PROMPT + build_main_llm + checkpoint-advance logic + resolve_patience_config); bypasses ONLY the transport layer (Soniox/TTS/LiveKit) since text legitimately replaces it. **Two modes**: (1) DETERMINISTIC golden net — per-checkpoint labeled (turn→expected-verdict) cases incl. off-topic NEGATIVES (must be unmet) + messy B1 POSITIVES (must be met); the regression guard that would've caught 2026-05-30 in seconds; cheap. (2) STOCHASTIC calibration — AI learner-agent (cooperative/hesitant/off_topic/minimal strategies, given briefing+character only NOT success_criteria) runs N conversations → completion-rate vs Story-6.8 target band (Waiter 60-90%, Mugger/Girlfriend 35-55%, Cop/Landlord 15-35%) → in_band/too_easy/too_hard verdict + off_topic-must-NOT-complete guardrail; report artifact under calibration-tests/. One `calibrate_scenario <id>` command (+ no-arg all-scenario sweep) = the step to run at each scenario creation (the broader goal: scale to many scenarios without hand-testing each). **Cost/determinism**: live-LLM runs NOT in default pytest (cost+flake, gated by key+flag like benchmark_classifier); harness logic has mocked-LLM unit tests that DO run; rates-over-N not single-shot; low temp. Likely Task-1 refactor: extract pure checkpoint-advance decision out of CheckpointManager.process_frame so prod+harness share it (no prod behaviour change, existing tests stay green). 8 ACs, 9 tasks, mostly self-validating (no device gate — the point). Server-only, no migration. Awaiting Walid `/bmad-dev-story`.
  6-16-scenario-builder: review  # 2026-06-02 /bmad-dev-story COMPLETE, in-progress → review. Built the scenario builder + generated the worked cop artifact. NEW: scripts/scenario_builder.py (expand_brief→draft_checkpoints→critique_and_repair[adversarial de-overlap + time-advance + judgeability]→pure build_base_prompt/assemble_scenario/validate_structure/scenario_to_yaml; reuses 6.15 LLM client; structural validation mirrors scenarios.py), scripts/build_scenario.py (CLI: fuzzy desc→YAML, --dry-run/--validate/--overwrite, refuses existing id, --validate chains 6.15 golden), scripts/build.cmd + scripts/calibrate.cmd (venv-python wrappers — fix Windows `python` Store-stub), tests/test_scenario_builder.py (14 mocked-LLM tests). WORKED ARTIFACT pipeline/scenarios/cop-interrogation-01.yaml ("The 8:30 Alibi", Detective Mercer, fingerprints on inside handle of forced door, 8:30 break-in, grey-hood witness, CCTV, Carver Street Boys) — 20 checkpoints generated by a 7-agent adversarial workflow; verifier confirms ALL 3 bars (20 distinct + advances-in-time + no-single-sentence-validates-3+); lexical-overlap heuristic found 0 collisions; loads through every prod loader (20 unique ids, patience_start 90 override, base_prompt valid); catalogue now 6 scenarios. **Honest caveat:** runtime engine is goal-based/any-order (6.10) so per-turn gating is enforced at authoring + caught empirically by 6.15 too_easy gate, NOT a runtime temporal lock. is_free:false, calibration PENDING. ruff clean; safe non-livekit subset 193 passed. **review → done reserved for Walid**: run calibrate.cmd cop_interrogation_01 live (GROQ_API_KEY) to confirm difficulty in-band + off_topic guardrail + tune patience_start; full pytest before /commit (sandbox livekit hang). All 10 ACs delivered. POST-LIVE-RUN (2026-06-02, Claude ran the tool on Walid's real GROQ key): fixed 3 things — (1) **PROD BUG**: `classify_multi` `max_tokens` was fixed 128 → too small for a 20-goal verdict → Groq HTTP 400 `json_validate_failed` on EVERY classify for the 20-checkpoint cop scenario (would break it in PROD too); fixed `_multi_max_tokens(n)=64+24n` in exchange_classifier.py + regression test + non-2xx body logging; (2) 429 storm = Walid's Groq FREE tier (30 req/min) — added ResilientJudge throttle (default 2.1s/call) + retry + `--throttle-ms`; (3) Windows emoji `UnicodeEncodeError` → `force_utf8_stdio()`. Also `load_llm_settings()` so tools need only GROQ_API_KEY (not full prod env). **Live proof:** `calibrate_scenario waiter_easy_01 --golden-only` → ✅ PASS (0×429, 0×400, exit 0). Cop golden-only works (20-goal classify verified) but ~3-4min on free tier; full calibration needs Groq Dev tier. ruff clean; safe subset 179 passed. Previously 2026-06-02 spec drafted — front half of the authoring loop (build → validate-via-6.15). Fuzzy free-text premise → AI expands into a rich brief (persona + time-ordered narrative arc + canonical facts) → drafts N checkpoints (default 20) → adversarial critique de-overlaps + enforces time-advance → assemble schema-valid YAML → structural validate (scenarios.py rules) → optional --validate chains into the 6.15 calibrate_scenario engine. NEW: scripts/scenario_builder.py (engine), scripts/build_scenario.py (CLI), .cmd wrappers (bypass Windows python Store-stub), tests/test_scenario_builder.py (mocked-LLM). Worked artifact = the cop/fingerprints interrogation (20 checkpoints). Goal-based engine is any-order (6.10) so AC4/AC5 enforced at authoring (critique) + empirically by 6.15 too_easy gate. Live generation gated behind GROQ_API_KEY (never imported by prod).
  6-17-full-auto-scenario-pipeline-and-voice: review  # 2026-06-02 /bmad-dev-story — built + review. Makes the builder full-auto incl. VOICE. Part A (PROD): bot.py now passes scenario metadata.tts_voice_id to build_tts_service(voice_id=) → _build_cartesia uses it (fallback CARTESIA_VOICE_ID) — before this the per-scenario voice field was STORED BUT IGNORED (every scenario spoke as the default British female). Part B (builder): scripts/scenario_builder.py fetch_cartesia_voices (live GET /voices, Cartesia-Version 2024-11-13; fields gender/language/country/description) + select_voice (LLM matches gender/accent/vibe, validates id in catalog) + cartesia_api_key on calibration_engine.LlmSettings/load_llm_settings; graceful no-key degradation (leaves null + reason). Part C: build_scenario threads voice→tts_voice_id; BuildResult.voice_id/voice_reason; CLI prints chosen voice. Part D: build_scenario --deploy (scp YAML to VPS + restart pipecat → re-seed → appears in app list). Cop given real voice (Cartesia "Ronald - Thinker" deep/intense male) in cop-interrogation-01.yaml. Cartesia catalog fetched live from VPS: ~36 EN voices (US/UK/AU) + es/hi/etc; accent fidelity bounded by catalog (no bespoke Mexican-accented English). Tests: test_tts_factory +1 (voice capture), test_scenario_builder +6 (fetch/match/pipeline). ruff clean; safe subset green (67 builder+engine+tts). CHARACTER-PHYSIQUE MATCHING (Walid: only 5 puppets, can't invent, scenario+voice must fit the face): added CHARACTER_PROFILES (5 puppets' fixed gender/age/look + voice hint from shipped scenarios) → builder injects the puppet's look as a VISUAL CONSTRAINT into the persona prompt (persona must match gender/age/look; only name/job/situation vary) + restricts voice to the puppet's gender (required_gender). INTERACTIVE WIZARD: scripts\new-scenario.cmd (double-click) → new_scenario.py asks character (shows 5 faces+looks)/idea/difficulty/name → builds + voice + AUTO-VALIDATE+REPAIR + offers deploy (no long command). AUTO BUILD→VALIDATE→REPAIR LOOP (Walid: "aller-retour intelligent jusqu'à cohérent"): build_and_validate_scenario builds → golden net (off-topic rejected on every checkpoint) → on fail repair_checkpoints_from_golden tightens only offending success_criteria → re-validate, up to max_repair_rounds (wizard 10 per Walid); wizard checks INCONCLUSIVE (rate-limited all-unsure = false pass) BEFORE VALIDÉ; prints ✅ VALIDÉ / ⚠️ residual (vrai souci) / ⚠️ INCONCLUSIVE; "Fini" only after the loop. This is the off-topic-correctness net (free-tier OK); difficulty-band calibration still needs Dev tier. pytest is NOT for authoring scenarios (only gates code deploy). +5 tests (repair/loop/profiles/voice-gender/summary). **"Appears in my list" needs the VPS to have the 6.17 prod code too (voice wiring + 6.16 _multi_max_tokens) — rides commit→CI deploy = Walid /commit gate (full pytest must pass first; dev sandbox can't run it — livekit hang).** review→done + /commit + on-device test = Walid.
  epic-6-retrospective: optional

  # Epic 7: Post-Call Debrief & Learning
  epic-7: backlog
  7-1-build-debrief-generation-backend: backlog
  7-2-build-call-ended-overlay-transition: backlog
  7-3-build-debrief-screen: backlog
  7-4-build-pre-scenario-briefing-display: backlog
  epic-7-retrospective: optional

  # Epic 8: Monetization & Subscription
  epic-8: backlog
  8-1-integrate-storekit-2-and-google-play-billing: backlog
  8-2-build-paywall-screen-with-invisible-tier-design: backlog
  8-3-build-subscription-management-and-full-tier-enforcement: backlog
  epic-8-retrospective: optional

  # Epic 9: Offline Access & Data Sync
  epic-9: backlog
  9-1-build-local-cache-with-sqflite-for-scenarios-and-debriefs: backlog
  9-2-build-automatic-data-sync-on-network-availability: backlog
  epic-9-retrospective: optional

  # Epic 10: App Publication & Launch Readiness
  epic-10: backlog
  10-1-create-privacy-policy-terms-of-service-and-legal-compliance-pages: backlog
  10-2-provision-domain-dns-ssl-and-server-infrastructure: backlog
  10-3-configure-app-store-and-play-store-listings: backlog
  10-4-set-up-beta-testing-pipeline-and-analytics: backlog
  10-5-execute-final-launch-checklist-and-store-submission: backlog
  epic-10-retrospective: optional
