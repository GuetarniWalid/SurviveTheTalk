# Deferred Work

Items flagged during code review but postponed — each entry records where the review surfaced it and why it was not actioned at the time.

## Deferred from: code review of story-8.3 (2026-06-19)

_From the adversarial multi-agent review (8 hunters → dedup → per-finding verification). The clear money-correctness patches (F1/F4/F5/F13/F14/F18) + the F6 a11y fix were handled in-review; F3 (Apple renewal key) routed to Walid as decision-needed. The items below are real but not actionable in 8.3 — the webhook/billing ones are also store-deferred (Apple/Google live billing blocked on 8.1 D4 + Story 10-4)._

- **🟡 Webhook buyer-binding gap — no `appAccountToken`/`obfuscatedExternalAccountId` (F15, MEDIUM).** The webhook resolves the affected account from whichever user FIRST submitted the store artifact to `/verify` (first-submitter-wins), inheriting the binding gap `/verify` itself documents (`PURCHASE_CONFLICT`). A shared/leaked artifact means lifecycle benefits/downgrades land on the submitter, not the real buyer. Real fix = mint+send `appAccountToken` (iOS) / `obfuscatedExternalAccountId` (Google) at purchase, bind to `user_id`, assert on verify + prefer token→user in the webhook. Spans `/verify` + device IAP flow + storage = a future story. [`server/api/routes_subscription_webhooks.py:90,207`] (edge layer, 8.3 review)
- **🟡 Manage drawer can't distinguish cancelled-future from renews-future + shows "Active until {past date}" during the sweep race (F7, LOW).** `_renewalLine` infers intent from a bare date; needs a server `auto_renew`/`cancelled` boolean on `UserProfileOut` (Apple `DID_CHANGE_RENEWAL_STATUS` / Google state). Bundle with the on-device billing gate. Cosmetic/trust only — no entitlement impact. [`client/lib/features/subscription/views/manage_sheet.dart:304-317`] (edge layer, 8.3 review)
- **🟢 Google RTDN authenticated only by a URL query secret `?token=` (F12, LOW).** Pre-prod hardening: switch to the reserved Pub/Sub OIDC bearer + `aud == google_pubsub_audience` check (field already declared, unused). No live exposure today (no Caddy access log, forgery re-validates to a no-op, webhook 503s until store config). [`server/api/routes_subscription_webhooks.py:263`, `server/config.py:162`] (blind+sec-server, 8.3 review)
- **🟢 Google `unreachable` RTDN marked processed + not retried (F17, LOW).** Same family as F1: an early-revoke RTDN landing during a Google API outage is lost, bounded by the expiry sweep to the remaining paid period. Resolved if F1's re-drive (don't mark unreachable as processed) is implemented; otherwise accept the bounded window. [`server/api/routes_subscription_webhooks.py:259,319`] (payments-logic, 8.3 review)
- **🟢 Card-tap/briefing path lacks the AC3 proactive 0-calls inert guard (F8, LOW).** Free user at cap who taps the CARD of a free scenario pushes the briefing + POSTs, gets a 403, then routes to the paywall — works via the server backstop, but via the round-trip the icon path avoids. AC3 scopes the inert affordance to the call ICON (satisfied); card-tap is intentionally tier-invisible browse. Post-MVP UX polish: add an early `callsRemaining==0` guard at the top of `_startCall`. [`client/lib/features/scenarios/views/scenario_list_screen.dart:494-510`] (auditor, 8.3 review)
- **🟢 Dual purchaseStream listeners fire a redundant second verify+complete per transaction (F9, LOW).** `PurchaseSyncService` (app-lifetime) + `SubscriptionBloc` (sheet-lifetime) both consume the same broadcast stream while the paywall is open. Fully absorbed by the server's idempotent `/verify` (UNIQUE token + `_respond_existing`) and swallowed double-`completePurchase`. Accepted coupling, already documented in-code. Optional: gate the bloc's verify when the service is present. [`client/lib/features/subscription/services/purchase_sync_service.dart:51-94`] (client, 8.3 review)
- **🟢 `onEntitlementChanged` refresh dropped when the hub isn't in `ScenariosLoaded` (F10, LOW).** Broadcast `StreamController` with no buffering; an entitlement flip during Loading/Error is dropped. Bounded + self-heals (the next `/scenarios` load that re-mounts the hub carries the correct tier; server enforcement is authoritative regardless). Optional latch hardening. [`client/lib/features/scenarios/views/scenario_list_screen.dart:211-232`] (client, 8.3 review)
- **🟢 Google webhook 403/503 on token mismatch → unbounded Pub/Sub redelivery (F16, LOW).** Intended security posture (reject, don't accept an unauthenticated body). Ops/observability item: at store-wiring send a Google `testNotification` to confirm the push `?token=` matches, and alert on a sustained rate of the "bad/missing verification token" WARNING / "TOKEN unset" ERROR. Entitlement correctness independently covered by the expiry sweep. [`server/api/routes_subscription_webhooks.py:270-290`] (edge, 8.3 review)

## Deferred from: code review of story-6.29 (2026-06-11)

_The bounded-wait + sanitizer + mood co-generation implementation is sound (auditor: What-NOT-to-do 0 violations, Deviations D1-D4 match the code exactly, no pipecat base-attribute shadowing, AC5 crediting byte-untouched). This is the one out-of-diff residual._

- **🟢 `SCENARIO_CHARACTER` is a dead producer since the EmotionEmitter retirement.** `routes_calls` still sets the env var and `_PARKED_JOB_ENV_KEYS` (`server/pipeline/bot.py:925`) still forwards it to parked pool bots, but nothing reads it server-side anymore (the 6.29 wiring test `test_bot_no_longer_reads_scenario_character_env_var` pins the non-read). Cleanup = remove the producer in `routes_calls`, drop the key from `_PARKED_JOB_ENV_KEYS`, re-pin/retire the test — touches files outside the 6.29 diff, zero prod risk meanwhile. (blind hunter, 6.29 formal review)



_Charter rules 6-8 + the mood-tag directive change every scenario's character behaviour; ENGINE_VERSION bumped 4→5 so the ledger marks all scenarios stale. The story-blessed gate is `--golden-only` (judge-side regression net on live Scout); the cooperative-band half stays quota-walled, same situation as the 6.27 entry below._

- **🟡 AC4's full-band live calibration not run — ALL scenarios are stale under ENGINE_VERSION 5.** Same Groq free-tier daily-cap wall as the 6.27 entry (one band run ≈ N=10 conversations × 6 scenarios on the walled free tier). The charter additions + the trailing `<mood:...>` tag (now stripped by `sanitize_reply_text` inside `simulate_conversation` — golden==prod) could shift cooperative completion rates inside/outside the difficulty bands. The next `calibrate_scenario.py` sweep (deliberate budgeted action, post-quota-reset or paid tier) must run the full band; the ledger forces it automatically via the 4→5 bump. Files: `server/scripts/calibration_engine.py` (ENGINE_VERSION), `server/pipeline/prompts.py` (charter + directive), `calibration-tests/validation-ledger.json`. (story 6.29 AC4)
- **🟡 PRE-EXISTING (surfaced, not caused, by 6.29): permissive opening criteria on `mugger_medium_01` (STABLE golden FAIL) + `cop_hard_01` (borderline/flaky).** The 4→5 bump forced the FIRST fleet-wide golden run for these scenarios (no prior reports exist in git, no ledger entry). mugger `react` fails the universal off-topic seed STABLY (twice identical: "There are a lot of people here today." → judged `met`, must be `unmet`); cop_hard `respond` failed the first sweep ("I think the traffic was terrible this morning." → met) then PASSED a `--force` re-run — verdicts flipping across runs proves the criteria sits exactly on the seed boundary. Root cause is the YAML authoring, not the judge or any 6.29 code: both opening criteria literally allow "acknowledges … in any way / responds to the officer", so the lenient-by-design judge obeys them; 6.29 touched ZERO judge inputs (prompt/schema/model/criteria/seed all pre-date — AC5). Fix = tighten ONLY those two `success_criteria` (a scoped scenario-authoring pass — spawn-task chip filed 2026-06-10), then `--golden-only --force` both; the hash change re-stales their band calibration. The story-gating waiter fixture (the hand-authored, `reviewed: true` worked example) + 4 other scenarios PASS under ENGINE_VERSION 5. Diagnostics: `calibration-tests/calibrate_cop_hard_01_2026-06-10T17-23-02Z.md`, `calibrate_mugger_medium_01_2026-06-10T17-30-42Z.md`. Files: `server/pipeline/scenarios/the-cop.yaml`, `server/pipeline/scenarios/the-mugger.yaml`. (story 6.29 dev, golden sweep)

## Deferred from: code review of story-6.27 (2026-06-10)

_The `implies` back-fill engine/loader/builder/classifier-resilience work is correct (auditor: 0/7 ACs violated, no "What NOT to do" breach); 6 patches were applied in-review (dead-edge validator rule, `checkpoint_verdicts` hardening, `checkpoint_backfilled` journal line, retry-semantics pin test, manager-level envelope test, story bookkeeping). This is the one quota-walled residual._

- **🟡 AC2's full-band live calibration never completed — ALL scenarios are stale under ENGINE_VERSION 4.** The full `calibrate_scenario.py waiter_easy_01` band run was killed mid-off-topic-phase by the Groq free-tier DAILY token cap (the known walled-Dev-tier situation); the story-blessed `--golden-only --force` PASS (which DID exercise the new pure `implies` assertion + the 6.23 `requires` assertion on live Scout) is the recorded gate. Residual risk: the cooperative completion-rate bands (easy 60-80 etc.) are unvalidated against the new flip rule — for the waiter AND for the other 5 scenarios, all marked stale by the 3→4 ENGINE_VERSION bump. The next `calibrate_scenario.py` sweep (a deliberate budgeted action, after a daily quota reset or on a paid tier) must run the full band; the ledger will force it automatically. File: `server/scripts/calibration_engine.py` (ENGINE_VERSION), `calibration-tests/validation-ledger.json`. (acceptance audit)

## Deferred from: code review of story-7.1 (2026-06-09)

_Server-only debrief backend (Option A — bot generates at teardown, server serves). The implementation is strong and the prior dev self-review already fixed the one CRITICAL (`HesitationObserver._clock`). This independent pass found **0 shipping defects at the current architecture** — the headline idempotency item is latent (one-shot subprocess today) and was kept as a `patch`; these are the lower-priority residuals. The top item is the deploy-owed `prod_snapshot.sqlite` refresh._

- **✅ AC1 — refreshed `prod_snapshot.sqlite` — RESOLVED 2026-06-09 (post-deploy).** Deployed 7.1 (commit `2d25a63`, CI green, `/health` git_sha match) → migration 011 confirmed live on prod (`schema_migrations` carries `011_debriefs`; `debriefs` table has 13 real rows; `call_sessions.checkpoints_passed/total_checkpoints` + `scenarios.scenario_title` present). Ran `refresh_prod_snapshot.py` (snapshot now carries 011, FK 0 / integrity ok, PII scrubbed), `test_migrations` + full `pytest` **775** green against it, committed the refreshed snapshot. _Original deferral note:_ The diff did NOT commit a refreshed snapshot (it couldn't — `refresh_prod_snapshot.py` SSH-pulls live prod, which only carries migration 011 AFTER it deploys), and `test_migrations.py` was loosened from a snapshot-derived count to `len(MIGRATIONS_DIR.glob("*.sql"))` (the repo count) to stay green meanwhile. So AC1's "snapshot refreshed + committed alongside the migration" is unmet by commit `52a8a13` and MUST land right after the deploy applies 011. The migration replays clean against the *current* (011-less) snapshot, so the Story 5.1 regression class is still caught now. Acknowledged in Completion Note #2 — surfaced here so it is not lost before `review → done`. File: `server/tests/fixtures/prod_snapshot.sqlite`, `server/tests/test_migrations.py`. (acceptance audit)
- **🟢 A malformed LLM item on the non-strict fallback path drops the WHOLE debrief.** `_normalize_core` passes `errors[]`/`idioms[]`/`hesitation_contexts[]` through with only a list-ness check (no per-item validation), so a structurally-wrong item makes the write-time `DebriefOut.model_validate` fail → the debrief is skipped → `GET` serves `DEBRIEF_NOT_READY` forever (no regeneration). This is graceful by design (no 500/crash — the write-time gate is deliberate), but per-item validate-and-drop in `_normalize_core` would salvage the rest of the debrief. Scout's strict `json_schema` avoids this on the happy path; only the fallback parse path is exposed. File: `server/pipeline/debrief_generator.py` (`_normalize_core`, `_clamp_areas`). (edge-case + blind hunter)
- **🟢 `busy_timeout` contention → progress + counts silently lost.** The teardown writes (`upsert_user_progress` / `set_call_checkpoint_counts` / `insert_debrief`) run inside the bot's outer `try/except` (`bot.py:814`). If the API holds the sqlite write lock past the 5 s `busy_timeout`, the `OperationalError` is swallowed and the whole teardown (progression bump + checkpoint counts + debrief row) is dropped with only a log line. Low probability (the writes are quick, single-row), and the spec accepts bot↔API concurrency as safe under `busy_timeout` — consider a short `OperationalError` retry around the write block. File: `server/pipeline/debrief_teardown.py` (the second `get_connection()` block). (edge-case)
- **🟢 `DEFAULT_END_REASON = "user_hangup"` mislabels a fully-completed call.** When `PatienceTracker.call_end_reason` is None — the tracker did not drive the end (user hang-up, network drop, OR a scenario the user *completed*) — the bot feeds the debrief LLM `reason="user_hangup"`. The hero `survival_pct` is unaffected (it is checkpoint-derived), but the system prompt's framing tone keys on the reason, so a survived-by-completion call is narrated as a hang-up. Consider emitting a distinct `completed` reason on full completion. Files: `server/pipeline/debrief_teardown.py:43`, `server/pipeline/bot.py:819`. (blind hunter)
- **🟢 Hesitation timing is an approximation (FR12).** Two sanctioned-but-imperfect points: (a) `_merge_hesitations` pairs the LLM's `hesitation_contexts` with the backend-measured durations strictly BY INDEX (AC5-specified) and trusts the model to return them in gap order — a reordered/short list mis-pairs context↔duration; (b) the gap is measured server-side from frame timing (`BotStoppedSpeakingFrame`→`UserStartedSpeakingFrame`), which includes downlink + client playback + uplink RTT, so the reported `duration_sec` is systematically OVER-stated vs. the human-perceived pause (and the >3 s threshold can trip on round-trip latency). Both are inherent to the chosen Decision-2 approach. The smoke gate should confirm a deliberate >3 s silence reports a plausible duration. Files: `server/pipeline/debrief_assembly.py` (`_merge_hesitations`), `server/pipeline/hesitation_observer.py`. (blind hunter)
- **🟢 Minor robustness (fallback / edge, low impact).** `brief_personality` sentence-splits on `[.!?]\s+`, so a persona opening with an abbreviation or decimal ("Det.", "Mr.", "8:30 p.m.") truncates the `Character personality:` line mid-sentence in the LLM user message; `_parse_debrief_output`'s first-`{...}` fallback (`find("{")`/`rfind("}")`) can over-capture on a large body containing literal braces in `user_said` quotes (inherited verbatim from `exchange_classifier`, strict-mode-avoided). Files: `server/pipeline/debrief_teardown.py:46,63`, `server/pipeline/debrief_generator.py:390-396`. (blind hunter)

## Deferred from: code review of story-6.23 (2026-06-08)

_The reactive-gating engine/harness/loader/builder are correct; the formal review confirmed zero shipping defects. This is the one cosmetic residual._

- **🟢 Harness empty-judgeable branch runs `advance_goals({})` while prod early-returns.** On a turn where every pending beat is reactive-and-gated, prod `_classify_and_flip_goals` early-RETURNS (`if not judgeable: return`, `checkpoint_manager.py:955`) before touching `advance_goals`. The harness instead sets `verdicts = {}` then falls through to `advance_goals(goals, {})` (`calibration_engine.py:644-647`). Behaviorally identical — empty verdicts = a flip no-op with `outcome="neutral"` and the meter unchanged — so the validated property (goal-state + meter + completion rate) cannot fork; and the branch is unreachable in shipped data (the only `requires` scenario, cop, has a proactive tail). Optional exact-parity tidy: `continue` instead of `advance_goals({})` so the harness mirrors prod's early-return textually at the "load-bearing coupling". Not a real golden==prod fork. (blind hunter, 6.23 formal review)

## Deferred from: code review of story-6.17 (2026-06-02)

_The per-scenario voice + wizard + auto-repair + `--deploy` are dev/authoring tooling (the one PROD path is the voice wiring in `tts_factory.py`/`bot.py`, which is correct + tested). The Acceptance Auditor verified all 7 ACs on the on-disk code. Two patches were applied (wrong-gender-voice fallback; CLI `--validate` INCONCLUSIVE guard). These are the residual hardening items. The `--deploy` item is the top operational concern, carried from the 6.16 review._

- **🟡 `--deploy` has no safety rails.** `build_scenario.py --deploy` (and the wizard's deploy offer) scp the YAML to the live release dir as `root@167.235.63.129` (`StrictHostKeyChecking=no`) + `systemctl restart pipecat.service`, with NO confirmation, NOT gated on the golden/`--validate` result (you can deploy an off-topic-accepting scenario), no backup of the overwritten file, and a partial-deploy (scp ok / restart fail) leaves a new YAML on prod with the old process. `scp`/`ssh` missing → an uncaught `FileNotFoundError` traceback after the YAML is already written. Trusted-operator dev tool + the YAML also lives in the repo, but one keystroke = unconfirmed root prod deploy. Files: `server/scripts/build_scenario.py` (`_deploy_to_vps`, the `--deploy` block), `server/scripts/new_scenario.py` (deploy prompt). (blind hunter + edge-case + acceptance audit)
- **🟢 A stale/deleted/typo'd Cartesia voice id 4xx's every call for that scenario.** `select_voice` validates the chosen id against the catalog AT GENERATION time, but a baked-in `tts_voice_id` that Cartesia later removes (or a hand-edited typo) reaches `_build_cartesia` unchecked → Cartesia 4xx on every TTS call → the character is mute the whole call, with no startup probe and no 4xx→`CARTESIA_VOICE_ID` fallback. Files: `server/pipeline/tts_factory.py` (`_build_cartesia`), `server/scripts/scenario_builder.py` (`select_voice`). (blind hunter + edge-case)
- **🟢 `opening_line` is not validated.** `validate_structure` checks the checkpoints/base_prompt/difficulty but NOT `opening_line`; the auto-builder emits `(brief.get("opening_line") or "").strip()` which can be `""` → `bot.py` falls back to a generic `_DEFAULT_OPENING_LINE` ("Hello? Can you hear me?") for that character — a softer recurrence of the exact bug 6.17 fixed. A non-string `opening_line` (hand-edited YAML) would reach `TTSSpeakFrame(non-str)`. Add `opening_line` (non-empty str) to `validate_structure`. Files: `server/scripts/scenario_builder.py` (`validate_structure`, `assemble_scenario`), `server/pipeline/bot.py`. (blind hunter + edge-case)
- **🟢 Auto-repair loop ignores structure mid-loop + can false-pass on empty checkpoints.** `validate_and_repair` loops on `golden.passed` only; a repair round that returns an empty/unparseable array → 0 seed cases → `golden.passed=True` (a FALSE pass), and any structural regression introduced mid-loop is ignored until the wizard's post-loop `structural_problems` check catches it (so a non-wizard caller would not). No monotonic-progress early-exit either: a stuck repair burns all `max_repair_rounds` (wizard default 10) × N judge calls on the 30-rpm/100k-tpd free tier. File: `server/scripts/scenario_builder.py` (`validate_and_repair`, `repair_checkpoints_from_golden`). (blind hunter + edge-case)
- **🟢 Wizard infinite re-prompt loop on stdin EOF.** `_ask` returns the default (`""`) on `EOFError`; the `while not character/description/name` loops then re-prompt forever (CPU peg) when stdin is closed/piped-empty. Fine for the double-click console; bad for any non-interactive invocation. Break/exit on repeated EOF. File: `server/scripts/new_scenario.py` (`_ask` + the input loops). (blind hunter + edge-case)
- **🟢 `fetch_cartesia_voices` caps at 100 with no pagination + assumes a `data` envelope.** The signature exposes `max_voices` but the HTTP `limit` is hardcoded to 100 and the cursor/next-page is ignored, so a matching voice past the first 100 mixed-language results is invisible (→ empty gender pool → default voice); a non-`data` response envelope yields a silently empty catalog. ~36 English voices fit in 100 today. File: `server/scripts/scenario_builder.py` (`fetch_cartesia_voices`). (blind hunter + edge-case)
- **🟢 A bad Cartesia key (401/403) is swallowed.** The `try/except Exception` around fetch+select collapses an expired/invalid key, a network error, and a JSON-shape change all into the same `voice_id=None` + a `voice_reason` string the user may scroll past → the scenario silently ships with the default British-female voice and no clear "your key was rejected" signal. Voice is best-effort (AC3), but conflating "no key" with "key rejected" is avoidable. File: `server/scripts/scenario_builder.py` (`build_scenario` voice block). (blind hunter)
- **🟢 Test hardening.** `test_factory_uses_scenario_voice_id_else_default` stubs only the non-instrumented Cartesia class (the `CARTESIA_INSTRUMENT=1` branch is uncovered) and never asserts an empty-string `voice_id` falls back; the de-hardcoded YAML-oracle seeding/listing tests (`test_scenarios.py`/`test_queries.py`) read the live scenarios dir as their own oracle (a malformed generated YAML raises `KeyError` inside the test helper, and the count assertion is tautological w.r.t. the seeder). Files: `server/tests/test_tts_factory.py`, `server/tests/test_scenarios.py`, `server/tests/test_queries.py`. (blind hunter)
- **🟢 Prompt-injection on the free-text premise.** The wizard's idea/`description` is injected verbatim into `EXPAND_PROMPT`/`VOICE_MATCH_PROMPT` with no delimiting/escaping, so a crafted idea can steer the generators (the JSON parser is tolerant by design). Low impact (trusted operator, dev tool). Files: `server/scripts/new_scenario.py`, `server/scripts/scenario_builder.py`. (blind hunter)

## Deferred from: code review of story-6.15 (2026-06-02)

_The validation engine is dev-only tooling (gated out of `pytest`, never imported by prod — AC6), so these robustness gaps have no prod runtime impact. They affect operator UX (a crash mid-run, an ugly traceback) and the strength of the offline guard. The one substantive item is the first — the golden net tests one goal in isolation while the 2026-05-30 bug was a multi-goal over-eager flip._

- **🟡 Golden net evaluates one goal in isolation, not the multi-goal turn the 2026-05-30 bug occurred in.** `run_golden` classifies each case with a SINGLE pending goal (deliberate, "so the verdict is unambiguous"), but the incident this net memorializes was an off-topic line flipping a goal *while several `success_criteria` were presented together*. A judge correct in isolation but over-eager under multiple pending goals would PASS the golden net yet still ship the bug. The multi-goal path IS partially exercised by `run_calibration` (all pending goals) + the off_topic guardrail, so it's a strength gap, not a hole. Consider a multi-goal golden mode. File: `server/scripts/calibration_engine.py:803` (`run_golden`). (blind hunter)
- **🟡 Simulated turn-1 sends the character LLM a system-only message + fabricates the opener.** `simulate_conversation`'s first iteration has an empty `dialogue`, so the character model is called with only a system prompt (some OpenAI-compatible endpoints 400 on a system-only array), and prod uses a *scripted* per-scenario opening line (Story 6.17 / commit `035751d`) the harness doesn't replay — so the simulated open differs from prod, weakening "green run ⇒ prod behaves" for turn 1. File: `server/scripts/calibration_engine.py:585-601`. (blind hunter)
- **🟡 `LlmSettings` duck-types `config.Settings` with hand-copied model defaults — silent drift risk.** The engine's lightweight `LlmSettings` hardcodes `character_model`/`classifier_model` defaults "kept in sync" only by a comment, and passes itself to prod's `resolve_llm_*` helpers. If a `Settings` default changes (or a resolver reads an attribute `LlmSettings` lacks), the dev tool silently validates a *different* model than prod, or crashes only at live-run time (no pytest coverage). Add a sync-guard test asserting defaults match `config.Settings`. File: `server/scripts/calibration_engine.py` (`LlmSettings`, `build_live_clients`). (blind hunter + acceptance audit)
- **🟡 `generate_golden_fixture` parsing is brittle and drops cases silently.** A response with `"positives": null` iterates `None` → `TypeError` mid-run; a non-string `character_line`/utterance is stored as-is (later `AttributeError` in `_escape_format_braces` once reviewed+run); a parse failure for one checkpoint contributes zero cases with no warning, so a fixture can be written with fewer cases than checkpoints and still be flipped `reviewed: true`. `--generate-golden` only, output is human-reviewed before gating. File: `server/scripts/calibration_engine.py:901+` (`generate_golden_fixture`, `_parse_generator_output`). (blind hunter + edge-case)
- **🟢 Ledger write is non-atomic + silently resets on corruption.** `save_ledger` does `path.write_text(...)` (no temp+rename) once per scenario in the sweep; an interrupt mid-write leaves half-written JSON, and `load_ledger` then catches `JSONDecodeError` → `{}`, silently discarding every prior PASS → a full (paid) re-validation. Low impact (re-validation is safe). Use atomic replace + maybe a `.bak`. File: `server/scripts/calibration_engine.py:1220` (`save_ledger`). (blind hunter)
- **🟢 Judge HTTP client may leak if `ExchangeClassifier` has no `close`/`aclose`.** `ResilientJudge.close()` is a best-effort `getattr(inner, "close", None)`; the judge client is built unconditionally (even on `--generate-golden`, which never classifies). If the inner classifier exposes no close method, its httpx/AsyncOpenAI pool is never closed. Dev tool, process exits anyway. File: `server/scripts/calibration_engine.py` (`ResilientJudge.close`, `build_live_clients`). (blind hunter)
- **🟢 `ResilientChat` retries every exception + throttle is not concurrency-safe.** The retry wraps a bare `except Exception` (comment says "transient e.g. 429") so a deterministic 4xx burns the full backoff before failing and hides the real error; and `_last_call` is a read-modify-write across `await` with no lock, so a future `asyncio.gather` over conversations would defeat the throttle (429 storm). Sequential use today hides both. File: `server/scripts/calibration_engine.py` (`ResilientChat.chat`). (blind hunter)
- **🟢 `effective_max_turns = max(max_turns, len(checkpoints)+8)` silently raises a user's `--max-turns`.** A flag documented as a maximum is overridden upward (to avoid a false `too_hard` on many-checkpoint scenarios) with no log line, so a cost-conscious operator setting a smaller value silently gets a longer, more expensive run. Add a one-line notice when the override kicks in. File: `server/scripts/calibration_engine.py:1076`. (blind hunter)
- **🟢 CLI prints the multi-line Markdown diagnostic to stdout alongside one-line verdicts.** On FAIL the full report is `print`ed to stdout (also saved to `.md`) intermixed with `PASS/FAIL` lines, so a consumer parsing stdout line-by-line chokes. The AC9 CI contract is the *exit code* (correct), so this is cosmetic; route the diagnostic to stderr or the file only. File: `server/scripts/calibrate_scenario.py:223`. (blind hunter)
- **🟢 `compute_scenario_hash` is computed twice per scenario per sweep.** Once in the skip-check (`_amain`) and again inside `_validate_one`, each re-reading + re-parsing the YAML; besides the wasted I/O, reading at two instants means the gate-deciding hash and the ledger-recorded hash can disagree if the file changes mid-sweep. Compute once and thread it through. Files: `server/scripts/calibrate_scenario.py:176,73`. (blind hunter)
- **🟢 `force_utf8_stdio()` is best-effort but the first emoji `print` can still crash.** If `reconfigure` is unavailable (a captured/piped non-reconfigurable stream), the swallowed failure leaves cp1252 and the first `✅`/`❌` print raises `UnicodeEncodeError` — the "never fatal" function just defers the crash to result-printing time. File: `server/scripts/calibration_engine.py` (`force_utf8_stdio`). (blind hunter)
- **🟢 Optional `calibration.target_band` override is documented but never wired from YAML.** `band_for_difficulty`/`evaluate_calibration` plumb a `band_override`, but `run_calibration` never passes it and nothing reads `calibration.target_band` from the YAML — an author who sets it is silently ignored. Either wire it or drop the claim from Deviation #3 / the engine comment. File: `server/scripts/calibration_engine.py:1058` (`run_calibration`). (acceptance audit)

## Deferred from: code review of story-6.16 (2026-06-02)

_The scenario builder is dev-only tooling (gated out of `pytest`, never imported by prod). The one PROD change is the `_multi_max_tokens` fix (low-risk, only raises the multi-goal classifier token ceiling). These are authoring-quality + operator-UX gaps; the Story 6.15 calibration is the empirical backstop for the quality ones. The `--deploy` items are Story 6.17 functionality living in 6.16-attributed files — revisit in the 6.17 review._

- **🟡 Generated cop artifact uses compound `PASS if (a) … AND (b) …` success_criteria.** Several `cop-interrogation-01.yaml` checkpoints (and a 3-pillar close) ask the judge to confirm a conjunction. The goal-based judge returns ONE met/unmet per goal, so a conjunction is handled as a whole (harder, not unrepresentable) — it can make those beats hard to pass. Walid's live `calibrate_scenario cop_interrogation_01` (too_hard verdict) is the backstop. File: `server/pipeline/scenarios/cop-interrogation-01.yaml`. (blind hunter)
- **🟡 `critique_and_repair` silently no-ops on unparseable critique output.** `if arr: current = sanitize(arr); return current` — if the critique LLM returns prose/truncates/empty, the un-de-overlapped draft is returned as if audited, with no signal. The lexical-overlap heuristic is advisory only (printed, not added to `structural_problems`, doesn't block the write). So the AC4/AC5 de-overlap "guarantee" is best-effort; the 6.15 `too_easy` gate is the real backstop. File: `server/scripts/scenario_builder.py` (`critique_and_repair`). (blind hunter + acceptance audit)
- **🟢 `_multi_max_tokens` has no upper ceiling + a weak regression test.** `64 + 24·max(1,n)` is unbounded — a 100-checkpoint scenario would request 2464 tokens (fine for Scout's ~8k ceiling, but uncapped). The test asserts only monotonicity + `≥500`, not that 544 actually holds a 20-key verdict JSON (the real bug). Fine for realistic N; cap + value-assert would harden it. File: `server/pipeline/exchange_classifier.py:134-142`, `server/tests/test_exchange_classifier.py`. (blind hunter + edge-case)
- **🟢 non-2xx classifier body logged at WARNING (`response.text[:300]`).** For the `json_validate_failed` 400 this fix targets, the body is the model's failed generation (the verdict JSON), not user input — low PII risk. Other 4xx error bodies are unverified and could echo request content. Consider debug-level or scrubbing. File: `server/pipeline/exchange_classifier.py:440`. (blind hunter + edge-case)
- **🟢 `parse_json_array`/`parse_json_object` use a greedy outermost-bracket span.** `text.find("[")`/`rfind("]")` (and `{`/`}`) grab the outermost brackets in the whole response — stray brackets in LLM prose before/after the array (or a `]`/`}` inside a string) yield an invalid slice → `None` → the step raises or the critique round is dropped. Brittle on the most failure-prone surface (LLM free text). File: `server/scripts/scenario_builder.py:783`. (blind hunter + edge-case)
- **🟢 `expand_brief` trusts every brief key blindly.** It only checks `brief is None`; a parseable brief missing `arc`/`canonical_facts`/`opening_line` yields a scenario with no canonical facts + empty opening line + checkpoints drafted from a spineless brief — and `validate_structure` checks structure, not richness, so it passes every gate. Silent quality collapse with a green check; calibration catches a hollow scenario. File: `server/scripts/scenario_builder.py` (`expand_brief`, `build_base_prompt`). (blind hunter)
- **🟢 `sanitize_checkpoints` dedupe can mangle ids on degenerate output.** `foo, foo, foo_2` → the 2nd `foo`→`foo_2`, then the real `foo_2`→`foo_2_2`; the `beat_{i+1}` fallback (list index) can collide with a real `beat` id. Order-dependent, not collision-proof; low frequency. File: `server/scripts/scenario_builder.py:331-357`. (blind hunter + edge-case)
- **🟢 `suggest_patience_start` is internally incoherent with its docstring.** At fail_penalty 25 a 20-checkpoint hard scenario gets 90 → absorbs exactly 3 misses, same as a 3-beat one; the `+(n-12)·2` long-scenario bump is swamped by a single 25-pt miss. It's a seed value calibration tunes, but the "sizes to absorb misses by length" justification doesn't hold. File: `server/scripts/scenario_builder.py:420-432`. (blind hunter)
- **🟡 `--deploy` (Story 6.17) pushes to prod root with no safety rails.** `build_scenario.py --deploy` scps the YAML to the live release dir as `root@167.235.63.129` (`StrictHostKeyChecking=no`) + restarts pipecat, with NO confirmation prompt, NOT gated on `--validate`/golden passing (you can deploy a golden-FAILING, LLM-generated scenario), no backup of the overwritten file. Trusted-operator dev tool + the YAML also lives in the repo, but one keystroke = unconfirmed root prod deploy. Revisit in the 6.17 review. Files: `server/scripts/build_scenario.py:41-67,156-171`. (blind hunter + edge-case)
- **🟢 `--out` can clobber an unrelated existing YAML.** The existing-id refusal only checks `scenarios._SCENARIO_INDEX`; an explicit `--out path/to/existing.yaml` is written unconditionally (no `--overwrite` check on the raw path). File: `server/scripts/build_scenario.py:129-135`. (edge-case)
- **🟡 Story 6.17 code lives in 6.16-attributed files — review under 6.17.** The auto build→validate→repair loop (`validate_and_repair`, `build_and_validate_scenario`, `repair_checkpoints_from_golden`, `max_repair_rounds=10`) + voice selection (`fetch_cartesia_voices`, `select_voice`) are Story 6.17 functionality in `scenario_builder.py`/`build_scenario.py`, reached via `new_scenario.py`. Their edge cases (repair-round id drift changing the validated goal set; voice-catalog shape; many-call cost) belong to the 6.17 review. Files: `server/scripts/scenario_builder.py`, `server/scripts/build_scenario.py`. (blind hunter + edge-case + acceptance audit)

## Deferred from: code review of story-6.14 (2026-06-01)

_All from the `InboundAudioStatsLogger` AC1 diagnostic (dev-only, gated by `kLogInboundAudioStats`) + the AC2 server-side jitter buffer. Low prod impact — the diagnostic is off-by-default-intent and the call model is a single remote audio track (tina-bot)._

- **🟡 `InboundAudioStatsLogger` track-churn data correctness.** Null or reassigned `track.sid` (keyed by `identityHashCode` fallback) can double-attach the same track → duplicate log lines + a `_prevConcealed` baseline split across two keys → bogus `(+delta)`. There is no `TrackUnsubscribedEvent` handler, so a mid-call unsubscribe leaves stale `_trackCancels`/`_prevConcealed` and a same-sid re-subscribe is skipped (`containsKey → return`) → the diagnostic silently stops. A cumulative-counter reset prints a negative `(+delta)`. Corrupts/duplicates the very AC1 `concealedSamples` delta the logger exists to measure. Dev-only diagnostic, rare in the 1-remote-track call model. File: `client/lib/features/call/services/inbound_audio_stats_logger.dart` (`_attach`, `formatStatsLine`). (blind + edge-case review)
- **🟡 `InboundAudioStatsLogger.dispose()` vs concurrent `_attach`.** A `TrackSubscribedEvent` firing during the `await roomCancel()` window can mutate `_trackCancels` mid-iteration (Dart `ConcurrentModificationError`) or register a listener after the dispose snapshot that then leaks past teardown. Mitigated (room listener cancelled first) but not airtight. Dev-only. File: `client/lib/features/call/services/inbound_audio_stats_logger.dart` (`dispose`). (blind + edge-case review)
- **🟡 livekit_client `monitorStats()` self-cancels the stats timer permanently on the first transient `false`.** The SDK's `startMonitor` kills the periodic stats timer the first time `monitorStats()` returns false (transient `getStats()` failure / receiver briefly inactive). After that NO more `AudioReceiverStatsEvent` fires → the AC1 logger goes silent for the rest of the call with no restart, giving a partial before/after capture. SDK limitation; needs a re-arm if the diagnostic is kept long-term. File: `client/lib/features/call/services/inbound_audio_stats_logger.dart`. (edge-case review)
- **🟢 `kLogInboundAudioStats = true` ships the AC1 diagnostic ON in prod** while its own doc-comment says "off-by-default keeps journals clean post-launch." Intentional for the smoke-gate before/after capture; flip to `false` once the AC1 measurement is recorded. File: `client/lib/features/call/services/inbound_audio_stats_logger.dart:23`. (acceptance audit)
- **🟡 Room-reuse silently no-ops the AC2 jitter buffer.** If a room with the same `call-<id>` name survives a prior crashed/incomplete session, LiveKit keeps the ORIGINAL room's config (possibly no `min_playout_delay`) and the new tokens' config is ignored — the SFU never emits the `playout-delay` extension, the before/after A/B shows "no change," and there is no signal that the config was a no-op. Validate fresh-room on the device gate. Files: `server/api/routes_calls.py` (`initiate_call`), `server/pipeline/livekit_tokens.py`. (edge-case review)

## ⚠️ MUST-FIX BEFORE MVP LAUNCH (HIGH-PRIORITY HOLDS)

- **🚨 Voice pipeline must be resilient to poor connections — audio jitter playout** *(surfaced 2026-05-27 during Story 6.13 Pixel 9 smoke gate on 5G; Walid flagged PRE-MVP: "beaucoup de user auront une mauvaise connexion et si on peut grandement améliorer l'expérience c'est tout benef")*
  - **Symptom:** on every call over 5G, Tina's voice "bugs" — syllables stretch/elongate then snap back to normal speed, intermittently. All words are pronounced in full (so it's time-stretching, NOT packet loss/garbling). Reproduced on call_id=170 (2026-05-27). Sounds broken; poor UX.
  - **Why it's high-priority:** a large fraction of MVP users will be on mobile/poor connections. WebRTC's receiver-side jitter buffer (NetEq) time-stretches audio to cover late/uneven packets then accelerates to recover — exactly this artifact. If common-case connections sound broken, the product feels broken.
  - **Root cause / evidence:** `TTS_AUDIO_DEBUG=1` capture showed the SERVER audio is clean — `sample_rate=24000` mono, consistent, healthy amplitude (10k–26k / 32767), no errors, VPS CPU low (2 cores, load ~0.15). The notable finding: ElevenLabs (via pipecat) delivers each utterance as **1–2 GIANT `OutputAudioRawFrame`s** (up to ~4.6 s / 221 KB in a single frame) instead of a stream of small frames. The output transport must then pace that large block out to WebRTC in real time; on a jittery 5G link the receiver stretches during jitter then catches up. Not a sample-rate or data-corruption bug (those would be constant, not intermittent).
  - **Investigation 2026-05-27 (corrects the original hypotheses):**
    - ❌ "Smaller ElevenLabs frames" is a DEAD END — the LiveKit output transport re-paces audio into ~10 ms WebRTC packets in real time regardless of the TTS frame size, so shrinking ElevenLabs frames does not change playout.
    - ❌ "Increase the client jitter buffer" is a DEAD END in this stack — `livekit_client 2.6.4` + `flutter_webrtc 1.3.0` expose NO audio jitter-buffer / playout-delay knob (only read-only metrics). It is fully auto-managed by native NetEq (which is exactly what's stretching/recovering). Forcing a bigger buffer would need a native SDK fork AND would add ~100-200 ms of CONSTANT latency per turn — directly harming the project's #1 battle (latency). NOT worth it.
  - **A/B RESULT 2026-05-28 — server-pacing RULED OUT, confirmed pure 5G network jitter.** Ran the `DTLN_ENABLED=0` A/B on 5G: the glitch PERSISTED (this time a mid-word *skip* instead of a stretch — both are NetEq adaptations: stretch = decelerate to cover late packets, skip = accelerate/conceal to catch up). DTLN is the only heavy LOCAL-CPU consumer on the box (STT/LLM/classifier are network I/O), so its removal ruling out the change means the asyncio loop is NOT the bottleneck → it's the 5G link. DTLN re-enabled (`DTLN_ENABLED=1`). **Bonus diagnostic:** Walid noticed the character's MOUTH follows the glitch. That's EXPECTED + reassuring — Story 6.3b moved lip-sync client-side, computed from the actual PCM buffer feeding the speaker (`FormantVisemeAnalyzer` on the playback thread), so the mouth is structurally locked to whatever audio plays, glitch included. It's a symptom of the audio, not a separate bug, and confirms lip-sync can't drift.
  - **Right fix (revised after A/B):** there is NO latency-free server fix — it's the network. Levers, in order: (1) connection-quality nudges — recommend WiFi/headset at onboarding, show a "weak connection" indicator; (2) accept it as an inherent WebRTC-over-marginal-5G behavior (every real-time voice app has it); (3) NOT the jitter buffer (unavailable in the SDK + costs ~100-200 ms latency). The earlier "DTLN off the loop" idea is moot (DTLN ruled out), though moving DTLN to a thread is still a minor CPU-headroom win worth doing opportunistically. Secondary infra lever: more VPS cores (costs money, not latency) — unlikely to help since the loop isn't the bottleneck.
  - **Wrong fix to avoid:** trading latency for a bigger jitter buffer (it's not even exposed, and latency is the #1 battle), OR shrinking TTS frames (no effect — transport re-paces).
  - **Touched by:** `server/pipeline/tts_factory.py` (ElevenLabs `Settings` / output format / streaming), `server/pipeline/bot.py` (`LiveKitParams` audio_out), pipecat `ElevenLabsTTSService`.
  - **Suggested home:** dedicated pre-MVP voice-pipeline resilience pass (pair with the endpointing item below). Promotes part of `memory/project_post_mvp_voice_ux_tuning.md` from post-MVP to pre-MVP.
  - **Tests:** capture `TTS_AUDIO_DEBUG` per-frame cadence before/after; A/B WiFi vs 5G if a stable network is available.

- **🚨 Voice pipeline must be resilient to poor connections — interim speech that never finalizes → dead air** *(surfaced 2026-05-27 during Story 6.13 Pixel 9 smoke gate on 5G; same PRE-MVP priority)*
  - **Symptom:** on a marginal 5G call (call_id=171, 2026-05-27) the silence ladder reached stage 3, then at 14:46:09 the mic captured ~3 interim words (`User started speaking ... interim_transcription=True`) which CORRECTLY cancelled the ladder — but that speech **never finalized into a complete turn**. The bot never responded, the ladder never re-armed → ~23 s of dead air → the user hung up manually (`reason=user_hung_up`). The character appears frozen.
  - **Why it's high-priority:** on poor connections, choppy mic audio routinely produces interim transcriptions that don't "endpoint". When that dead-locks the conversation with no recovery, the experience dies — and it's a common-case failure on mobile.
  - **Root cause / evidence:** Soniox emitted interim transcriptions (`num_spoken_words` 1→3, `interim_transcription=True`) but NO finalized `TranscriptionFrame` followed and no `Generating TTS` afterwards. The `EndpointWatchdog` (Story 6.9, 8 s wall-clock backstop) should force-finalize a stuck interim into a turn, but it produced no bot turn here — needs verification that it actually fires in prod and recovers the turn. Same class as the Story 6.9 "Soniox interim-TFs-forever" case (mitigated then by `vad_force_turn_endpoint=False`).
  - **Right fix (proposed):** (1) harden endpointing on marginal audio — ensure `EndpointWatchdog` force-finalizes a stuck interim into a real turn AND verify it runs live; (2) after a user-speech cancel that yields **no** bot turn within N seconds, **re-arm the silence ladder** so the call can still progress/hang-up instead of dead-air; (3) optionally a recovery "still there?" path on prolonged dead air.
  - **Wrong fix to avoid:** assuming every `UserStartedSpeakingFrame` leads to a finalized turn — on poor links it often doesn't.
  - **Touched by:** `server/pipeline/endpoint_watchdog.py`, `server/pipeline/patience_tracker.py` (re-arm after a no-turn cancel), `server/pipeline/bot.py` (Soniox / VAD config).
  - **Suggested home:** same pre-MVP voice-pipeline resilience pass as the audio-jitter item above.
  - **Tests:** drive an interim-only transcription (no finalize) through a real pipeline and assert the `EndpointWatchdog` forces a turn OR the ladder re-arms within N seconds (no dead-air).

- **🔴 Silent 401 / `AUTH_UNAUTHORIZED` handling — cross-cutting auth gap** *(surfaced 2026-04-29 in code review of Story 5.5; flagged HIGH-PRIORITY by Walid)*
  - **Symptom:** When the JWT expires, the server returns HTTP 401 with `code: 'AUTH_UNAUTHORIZED'`. The client has NO interceptor for this — every feature bloc that hits the API translates it through `_classifyApiCode` (or its local equivalent) and the user sees a generic "try again" error. Retrying does nothing — the token is dead. The user is stuck in a loop until they kill and restart the app, and even then there's no flow to re-authenticate gracefully (auth router redirects assume a clean cold-start, not a session-expiry mid-flow).
  - **Why it's high-priority:** before MVP launch, sessions WILL expire (default JWT TTL × N days × real-world users). Without this, a non-trivial fraction of returning users hits a dead-end loop on day-30+ and churns silently.
  - **Right fix (proposed):** add a `Dio` interceptor in `client/lib/core/api/api_client.dart` that catches `response.statusCode == 401`, clears `TokenStorage`, dispatches `AuthEvent.SessionExpired`, and lets `GoRouter`'s `refreshListenable` redirect to `/auth`. The bloc-level error mapping (`_classifyApiCode`) then never sees a 401 — it's intercepted upstream.
  - **Wrong fix to avoid:** adding an `AUTH_REQUIRED` slot to each feature bloc's error state (would couple auth to every feature, leaks the abstraction, multiplies maintenance).
  - **Touched by:** `client/lib/core/api/api_client.dart` (add interceptor), `client/lib/features/auth/bloc/auth_bloc.dart` (handle `SessionExpired`), `client/lib/core/router/app_router.dart` (verify refresh listenable wiring), and any feature bloc that currently catches `ApiException` (audit they no longer expect AUTH codes).
  - **Suggested home:** dedicated story in Epic 8 (Monetization & Subscription) or earlier if MVP closed-beta exposes the gap. Should land BEFORE the public-launch milestone.
  - **Tests:** integration test that pumps a 401 mid-app-flow and asserts the user lands on `/auth` with the previous nav stack preserved (so they can return to where they were after re-auth).

- **🚨 Perceived latency exceeds PRD kill criterion** *(surfaced 2026-05-19 during Story 6.7 manual smoke test on Pixel 9 Pro XL by Walid)*
  - **Symptom:** call_id=118 transcript timestamps showed consistent 2-3s perceived latency between user speech end and Tina's first audio byte across 7 conversational turns. In terminal-zone turns (patience < 25, Dev #7 synchronous classifier path) latency jumped to ~4-5s. The PRD `Functional & Non-Functional Requirements > Performance` row: target <800ms, hard ceiling <2s, "consistently >2s → concept is dead". Observed values are at OR ABOVE the kill line.
  - **Why it's high-priority:** Walid (2026-05-19) explicitly stated the whole app is at stake. Adversarial-conversation UX depends on real-time feel; the PRD framed this as gate 1 of MVP viability. The Phase 0 PoC validated sub-800ms latency in isolation; Epic 6's CheckpointManager / EmotionEmitter / ExchangeClassifier pipeline + a 1.8s speech timeout dragged it over the line.
  - **Smoking-gun root cause:** `server/pipeline/bot.py:125` — `SpeechTimeoutUserTurnStopStrategy(user_speech_timeout=1.8)`. Pipecat waits 1.8s of silence before declaring "user turn done" and triggering the LLM. This single line ALONE is at the 2s ceiling with zero margin. Layered on: VAD `stop_secs=0.8` (may stack), STT finalize ~0.5s, LLM TTFT, TTS TTFA, WebRTC RTT. Total = ~3s typical, 4-5s in terminal zone.
  - **Right fix (proposed, rough priority order):**
    1. Drop `user_speech_timeout` 1.8 → 0.6-0.8s with UX validation (single biggest win).
    2. Audit VAD `stop_secs=0.8` for redundancy with above; aim for net ~0.8s silence-to-turn-end.
    3. Verify LLM→TTS streaming overlap is enabled end-to-end (instrument first-byte timestamps per processor). If accumulation anywhere, we eat full LLM gen time before TTS starts.
    4. Move Dev #7 synchronous classifier off the critical path (cache parallel-path verdict; or shorten 2.0s timeout → 1.0s; or accept disjointed exit-line UX for early launch).
    5. Profile contention between EmotionEmitter / ExchangeClassifier / main LLM call — all hit OpenRouter on every user turn.
    6. Verify geographic locality (Hetzner EU vs Soniox/OpenRouter/Cartesia endpoints) — transatlantic RTT could be eating budget.
  - **Wrong fix to avoid:** scoping this to a single scenario tweak. The fix is architectural (pipeline params + streaming verification + classifier path).
  - **Touched by:** `server/pipeline/bot.py` (VAD + speech timeout), `server/pipeline/checkpoint_manager.py` (Dev #7 sync path), `server/pipeline/emotion_emitter.py` + `server/pipeline/exchange_classifier.py` (OpenRouter contention).
  - **Suggested home:** Promote Story 6.8 from YAML calibration to architectural latency fix + scenario tuning bundle. Land before MVP closed-beta.
  - **Tests:** end-to-end smoke gate that asserts perceived latency ≤ 1500ms (FR45 alert threshold) over a happy-path Waiter run; instrumented per-processor first-byte timestamps logged for diagnosis.
  - **Memory:** see `memory/feedback_latency_kill_criterion_exceeded.md` for full lever analysis.

- **🚨 Conversation coherence enforcement at scenario level, not system level** *(surfaced 2026-05-19 during Story 6.7 manual smoke test by Walid)*
  - **Symptom:** call_id=118 surfaced 3 coherence failures from Tina (Waiter): (a) forgot the user's confirmed "Coke" order 70s later and asked again, (b) re-asked the main course after confirming "Okay, grilled chicken", (c) hallucinated 3 different drink menus across turns (Coffee/tea/water/soda → Coke/Sprite/Ginger Ale → Water/juice/cola/coffee).
  - **Why it's high-priority:** Walid (2026-05-19) explicitly: "le gardien ne doit pas être au niveau de chaque scénario mais plutôt au niveau général parce qu'ensuite quand je vais générer des scénarios à la chaîne je n'aurai pas le temps de vérifier chaque microinteraction". The current architecture forces every scenario author to re-discover coherence safeguards in their `prompt_segment` strings — non-scalable. The whole-app character coherence is unreliable.
  - **Root cause:** Story 6.6's `CheckpointManager._classify_and_advance` (Deviation #2) swaps `llm._settings.system_instruction` wholesale on every checkpoint advance with `base_prompt + prompt_segment[current_checkpoint]`. Any conversation-memory or menu-consistency directive in the previous segment is overwritten. Qwen3.5 Flash (small, fast model) does not weight LLMContext message history strongly enough to override a fresh system directive emphasizing the new objective.
  - **Right fix (proposed):** introduce a system-wide `COHERENCE_CHARTER` constant in `server/pipeline/prompts.py`. The CheckpointManager swap becomes: `base_prompt + "\n\n" + COHERENCE_CHARTER + "\n\n" + prompt_segment`. Charter (~150-300 tokens) covers: order memory (explicit list of confirmed items, forbid re-asking), menu consistency (quote menu verbatim from base, never paraphrase or augment), acknowledgment continuity ("got it" = locked), no-contradiction rule (never deny or re-question a prior confirmation).
  - **Alternatives explored:** (a) tool-call approach where LLM calls `confirm_order_item(item)` recording to session state, then a "you have already confirmed: [...]" preamble auto-injects each turn — higher dev cost, more robust; (b) post-LLM validator that checks for prior-turn contradictions and regenerates — adds latency. Option (charter) is cheapest and likely sufficient for Qwen3.5 Flash.
  - **Wrong fix to avoid:** adding coherence directives to each scenario YAML's `prompt_segment` strings individually — exactly the per-scenario coupling Walid rejected.
  - **Touched by:** `server/pipeline/prompts.py` (new `COHERENCE_CHARTER` constant), `server/pipeline/checkpoint_manager.py:__init__` + `_classify_and_advance` (the swap composition).
  - **Suggested home:** Story 6.8 must INCORPORATE this charter design alongside the latency fix — they're co-dependent (the charter adds tokens to every prompt, which adds LLM TTFT).
  - **Tests:** end-to-end smoke that drives a scenario through 3+ checkpoint advances and asserts (via transcript diff) that the LLM never re-asks an already-confirmed item.
  - **Memory:** see `memory/feedback_coherence_must_be_system_wide.md` for full analysis.

## Deferred from: code review of story 6-3b-improve-lip-sync-from-word-to-syllable-level (2026-05-12)

- **Latent: no defensive `data.size < frames * stride` check in the playback callback** — the outer `try { ... } catch (t: Throwable)` would catch an `ArrayIndexOutOfBoundsException` if libwebrtc ever delivers a truncated buffer, but silently (logged at error level only). Trust in the SDK contract for now; revisit if the catch ever fires in production logs. Touched: `client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioClockChannel.kt` (`onWebRtcAudioTrackSamplesReady`).
- **Brittle coupling to flutter_webrtc plugin internals** — the entire pivot rests on reflecting into `methodCallHandler.playbackSamplesReadyCallbackAdapter` via `Class.getDeclaredField`. The class import `com.cloudwebrtc.webrtc.audio.PlaybackSamplesReadyCallbackAdapter` and the field name are plugin-internal (not part of the public API). Any flutter_webrtc minor-version bump that renames or restructures either would break the analyzer silently — the catch in `tryAttachCallback` swallows the `NoSuchFieldException` and the mouth simply never animates, with no surfaced error beyond a log line. Mitigations: pin the flutter_webrtc version in `pubspec.yaml` (already pinned at 2.6.4 per memory note), add a smoke-gate item that re-verifies attach success on every plugin upgrade, and consider upstreaming a public-API request to expose a `PlaybackSamplesReadyCallback` registration hook. Touched: `client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioClockChannel.kt` (`tryAttachCallback`).

- **🟡 Audio↔viseme phase offset is unmeasured and uncompensated — current sync is "good enough" but not optimal** *(surfaced 2026-05-12 via critical follow-up assessment after the /bmad-code-review of 6-3b; flagged HIGH-VALUE-BUT-NOT-MVP-BLOCKING)*

  ### Background

  Story 6.3b ships client-side viseme generation by hooking flutter_webrtc's `PlaybackSamplesReadyCallback` and analysing each PCM chunk on the WebRTC audio thread. The architectural premise is "**structural sync**" — because the analyzer runs on the same thread that hands bytes to the speaker, the viseme cannot drift in time relative to the audio. This premise is correct.

  However, "cannot drift" ≠ "phase-aligned". There is an inherent, **unmeasured and uncompensated** phase offset of order **20-100 ms** between the moment a phoneme is *heard* and the moment the corresponding mouth shape is *visible*. Walid validated lip-sync visually on Pixel 9 Pro XL and it looked correct — that's because (a) the human perceptual threshold for lip-sync error is ~80-100 ms (broadcast standard EBU R37 tolerates ±60 ms audio-lead, ±125 ms audio-lag), (b) the 80 ms hysteresis floor masks micro-jitter, (c) on a stylised 2D character at conversational speed the brain forgives a lot.

  We're under the perceptual threshold today. We are *not* optimal.

  ### Four sources of phase offset (additive)

  1. **AudioTrack output latency (the dominant term, currently ignored)** — `PlaybackSamplesReadyCallback` fires when bytes are *enqueued* into the WebRTC playback pipeline. The Android `AudioTrack` buffers those bytes before they reach the DAC; on Pixel-class devices this is typically **20-80 ms** depending on audio path (low-latency vs default), sample rate, and current system load. The viseme emitted at sample-write-time is therefore **early** relative to the sound the user actually hears by that amount. We never query `AudioTrack.getTimestamp()` or `getLatency()` to know the offset, so we can't correct for it.

  2. **Main-thread post jitter** — `mainHandler.post { sink.success(v) }` lands the viseme on the next UI frame at vsync. At 60 Hz that's a uniformly-distributed **0-16 ms** delay depending on where in the cycle we tossed the runnable. Adds variance, not bias.

  3. **Flutter EventChannel + binary messenger** — `~1-3 ms` to cross the native→Dart isolate boundary via the platform messenger. Bias.

  4. **Rive state machine transition** — `setVisemeId` triggers an interpolation from the current pose to the target pose over the duration configured inside the `.riv` file (typically 60-100 ms for "snappy" transitions, 150-200 ms for "soft"). The peak of the visual mouth shape lags the `setVisemeId` call by half the transition duration. Bias.

  Net: **~20-100 ms variable offset, dominated by AudioTrack buffering**, with sign that depends on how much (4) Rive transition compensates against (1) AudioTrack lead. Today we don't measure, so we don't know if we're net-early or net-late, only that the *magnitude* is plausibly under the perceptual threshold.

  ### Three viable improvements, ranked by ROI

  **A — `AudioTrack.getTimestamp()` compensation [~80% of the gain, ~30% of the cost]**

  Instead of emitting visemes when bytes are *queued*, emit them when bytes are *played*. Mechanism:
  - In `AudioClockChannel.kt`, on each callback measure the gap between `samples.framePosition` (or the equivalent monotonic write counter) and the playback timestamp from `AudioTrack.getTimestamp(AudioTimestamp)`, smoothed over a 100-ms window.
  - Compute `delay_ms = (framesPending / sampleRate) * 1000` where `framesPending = writeIndex - timestamp.framePosition`.
  - Replace `mainHandler.post { ... }` with `mainHandler.postDelayed({ ... }, delay_ms.toLong())`.

  Caveats:
  - We don't own the AudioTrack — flutter_webrtc's JavaAudioDeviceModule does. Accessing it requires another reflection round-trip (the field `webRtcAudioTrack` inside `MethodCallHandlerImpl`, similarly private). This compounds the "brittle coupling" risk already flagged above.
  - `AudioTrack.getTimestamp` returns false on some devices / audio paths; need a fallback to `getLatency()` (deprecated but functional) or a fixed 40-ms default offset.
  - The hysteresis floor (80 ms) and the delay (~40-60 ms typical) sum to noticeable visemes-on-rests at end-of-utterance — would need to verify on-device.

  **B — Vsync-aligned post via `Choreographer.postFrameCallback` [~10% of the gain, ~10% of the cost]**

  Replace `mainHandler.post { ... }` with `Choreographer.getInstance().postFrameCallback { ... sink.success(v) }`. The viseme then lands exactly on the next UI frame instead of "somewhere in the next 16 ms". Eliminates jitter source (2) entirely. Negligible risk — Choreographer is documented public API and exists since API 16.

  **C — Lookahead buffering [~moderate gain, ~moderate cost — deferred]**

  Buffer the last N chunks (say N=3 ≈ 30 ms) and emit the viseme of `chunk[t-N]` with a delay of `N × chunk_duration`. This means the analyser sees 30 ms of audio context per decision (more stable classification, less micro-flicker) AND we're emitting the viseme N×10 ms *earlier* in wall-clock time than today, partially compensating (1). Worth it only if A is rejected.

  **D — Native Rive rendering [non-viable today]**

  Move Rive rendering to the Android side via Rive's Android runtime, bypassing the Flutter EventChannel hop entirely. Eliminates (3) and possibly (4) if we control transition timing natively. This is a **massive architectural refactor** — would require rewriting `RiveCharacterCanvas` as a platform view, lose the Flutter widget tree integration, and break every existing test. Not worth pursuing unless A+B+C prove insufficient.

  ### Why this is not MVP-blocking

  - On-device validation showed acceptable lip-sync subjectively.
  - The dominant offset (AudioTrack buffer) is within human perceptual tolerance (<80 ms on Pixel 9 Pro XL low-latency audio path; cellular call audio is also subject to ~20-40 ms acoustic-echo-cancellation latency, comparable order).
  - The hysteresis floor (80 ms) already masks the variance.
  - Walid's explicit on-device validation is the bar that closes the smoke gate.

  ### Why it's worth a follow-up story before public launch

  - "Good enough" today on Pixel 9 Pro XL is not a guarantee on older / mid-tier Android devices where AudioTrack buffer latency can reach **150-200 ms** (e.g. some Samsung A-series, low-end Xiaomi). On those devices we may cross the perceptual threshold without ever knowing — App Store reviewers and critical YouTube reviewers will catch it.
  - The fix is bounded and measurable: instrument the offset, apply A+B, re-instrument, prove the offset dropped. ~1-2 days of work.

  ### Suggested home

  A dedicated story **6.3c** (or post-Epic 6 follow-up) titled "Audio↔viseme phase compensation via AudioTrack.getTimestamp + Choreographer post". Should land **before** public launch, ideally **before** the closed beta on heterogeneous devices.

  ### Acceptance criteria for the follow-up

  - Add a debug instrumentation mode (toggleable via Settings or a build flag) that logs, every 1 s, the measured AudioTrack offset and the cumulative viseme→display latency. Disable in release.
  - Implement (A) compensation; verify median offset drops by ≥ 30 ms compared to baseline on three device classes (flagship like Pixel 9, mid-tier like Pixel 6a, low-end like a Samsung A14 or BlueStacks).
  - Implement (B) Choreographer post; verify standard deviation of the offset drops to under 5 ms (vs ~8 ms today from the uniform 0-16 ms jitter).
  - No regression on Walid's subjective on-device visual on Pixel 9 Pro XL — the lip-sync must look as good or better.
  - No new tests beyond an instrumentation unit test asserting offset values plumb through; the actual quality is a smoke-gate observation.

  ### Touched (when actioned)

  - `client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioClockChannel.kt` — read AudioTrack timestamp, replace `mainHandler.post` with `postDelayed`, Choreographer post.
  - `client/lib/features/call/services/viseme_scheduler.dart` — possibly nothing if we keep the EventChannel contract; possibly an optional `instantaneousOffset` field for debug overlay.
  - New `client/android/app/src/test/kotlin/.../AudioClockOffsetTest.kt` if we can test the offset calculation purely (likely yes — it's arithmetic).

## Deferred from: code review of story 6-3-implement-emotional-reactions-and-lip-sync-via-data-channels (2026-05-01)

- **VisemeEmitter primary+rest emitted back-to-back; client ignores `timestamp_ms`** — server pushes the primary viseme then a rest follow-up in the same async tick; the client `setVisemeId` writes immediately on receive. Result: mouth opens then snaps shut on the same wire-tick. Already scheduled in **Story 6.3b** (sprint-status note: word-level lip-sync rated catastrophic on smoke; syllable-level fix shipping next). Touched: `server/pipeline/viseme_emitter.py`, `client/lib/features/call/services/data_channel_handler.dart`.
- **`_VOWEL_GROUPS` `'o'` substring rule too broad** — single-letter `'o'` matches "not", "for", "to", "go" etc. and dominates everything that doesn't have an earlier hit. Heuristic concern; revisit alongside Story 6.3b syllable-level rewrite. Touched: `server/pipeline/viseme_emitter.py:_PRIORITY`.
- **`_word_duration_ms = max(80, len(word) * 60)` arbitrary** — no source for the 60ms/char or 80ms floor. Mismatches real Cartesia per-word durations: short word ("a") → 80ms, long word ("understanding") → 780ms while audio may take 400ms. Revisit alongside 6.3b. Touched: `server/pipeline/viseme_emitter.py`.
- **`load_scenario_metadata` ignores cache** — re-reads YAML on every `initiate_call`; `load_scenario_prompt` IS cached, asymmetric. Acknowledged tradeoff in inline comment; no benchmark provided. Add `@lru_cache` if prod metrics show file IO pressure on the FastAPI event loop. Touched: `server/pipeline/scenarios.py`.
- **VisemeEmitter has no `finalized` check on `TTSTextFrame`** — pipecat 0.0.108 only emits via `_add_word_timestamps` (always finalized), so not active. Revisit if Cartesia integration changes or if a pipecat upgrade introduces interim TTS frames. Touched: `server/pipeline/viseme_emitter.py`.
- **`intensity` defaulting differs server vs. client (`0.5` vs `0.0`)** — server's `_parse_classifier_output` clamps null intensity to 0.5; client's `_onEmotion` coerces non-num to 0.0. Future contract drift if either side relaxes validation. Touched: `server/pipeline/emotion_emitter.py` ↔ `client/lib/features/call/services/data_channel_handler.dart`.
- **`BlocConsumer.listenWhen` does not fire on initial state** — production `CallBloc` always starts in `CallConnecting` so live behavior is fine; concern is hypothetical for future code reuse with seeded `BlocProvider.value` (e.g., screen restoration after hot-restart). If a future path seeds the bloc with `CallConnected`, `_dataChannelHandler` is never built and emotion/viseme envelopes drop silently. Mitigation if surfaced: also check the seed state in `initState` via `context.read<CallBloc>().state`. Touched: `client/lib/features/call/views/call_screen.dart:184-209`.
- **`disgust_hangup` emotion classifiable before Story 6.4 wires hang-up** — `EMOTION_CLASSIFIER_PROMPT` instructs the model to map abusive/sexual/threatening input to `disgust_hangup`, but no hang-up logic ships in 6.3 (Story 6.4 owns it via `PatienceTracker` / `RemoteCallEnded`). Until 6.4 lands, the character flashes the disgust expression while the abusive caller keeps talking. **Resolution 2026-05-01: accepted as-is — pre-MVP, no real users; abusive-input edge case has zero exposure until public launch. Story 6.4 (next on the sprint plan) will wire the actual hang-up before MVP ships.** Touched: `server/pipeline/prompts.py`, `server/pipeline/emotion_emitter.py`.

## Deferred from: code review of story 6-2-build-call-screen-with-rive-character-canvas (2026-04-30)

- **`IncomingCallScreen` overflow guard missing** — the dial surface in 6.2 is now wrapped in `LayoutBuilder + SingleChildScrollView + IntrinsicHeight` (Story 5.4 pattern) for 320×480 + textScaler 1.5 viability, but the originating `IncomingCallScreen` is NOT. Same overflow risk on small viewports + large text scalers. Already noted as follow-up cleanup in Story 6.2 Conscious-choice #5; add a test mirroring the 320×480 textScaler 1.5 case. Touched: `client/lib/features/call/views/incoming_call_screen.dart`.
- **`SemanticsService.announce` deprecation has no version target** — `incoming_call_screen.dart:113` uses `// ignore: deprecated_member_use` with the rationale "Suppress until a Flutter bump lands" but no Flutter version, no `// TODO(walid):`, no tracking link. Will silently rot through Flutter upgrades. Add a tracking marker tied to a specific Flutter version target. Pre-existing from Story 4.5, not introduced by 6.2.
- **`_buildErrorBody` overflow guard symmetry** — `call_screen.dart:279-310` does NOT use the same `LayoutBuilder + SingleChildScrollView + IntrinsicHeight` wrap as `_buildDialSurface`. A long error reason at 320×480 + textScaler 1.5 will overflow. Apply the same wrap pattern.
- **PopScope.canPop transition not regression-tested** — the `BlocConsumer` builder/listener frame-ordering that makes Deviation #3's fix work is load-bearing but no test exercises the `CallConnected → CallEnded → PopScope.canPop becomes true → maybePop fires` chain. If a future flutter_bloc upgrade splits listener/builder ordering, the bug returns silently. Add a regression test next time the area is touched.
- **`_hostWithMockBloc` test seam is hand-mirrored** — `call_screen_test.dart:81-…` mirrors `CallScreen`'s widget tree to inject a `MockCallBloc`. The mirror drifts every time the real screen evolves (acknowledged in the file's own comment). Refactor to an injectable bloc factory at next test-suite touch.
- **AC9 fallback-tap test bypasses prescribed `MockCallBloc` seam** — `call_screen_test.dart:1688-1716` uses real `CallBloc` + `MockRoom` and asserts `room.disconnect()` was called. Functionally equivalent (the disconnect proves the event ran) but not the literal AC9 prescription ("Use the existing `MockCallBloc` pattern from Story 6.1"). Refactor next time the test is touched.

## Deferred from: code review of story 4-5-build-first-call-incoming-call-experience (2026-04-23)

- **Bot subprocess never reaped** — `server/api/routes_calls.py:63-76` fires `subprocess.Popen` and never tracks it. Real lifecycle (terminate on call-end, zombie cleanup) belongs to Epic 6.4 / 7.1 via `POST /calls/{id}/end`.
- **`CallPlaceholderScreen` has no LiveKit timeout / reconnect / disconnect-event handler** — `call_placeholder_screen.dart:34-60` silently hangs on "Connecting to Tina…" if the room never comes up. Spec scopes real call UX (including error recovery) to Epic 6.2 Story 6.2.
- **Mic permission revoked between onboarding and `/call` not user-guided** — `call_placeholder_screen.dart:44-58` catches the failure and shows a generic "Couldn't connect" without offering a path back to settings. Epic 6.2 owns the real mic-error UX.
- **No rate-limit / per-user in-flight guard on `/calls/initiate`** — `routes_calls.py:33-101` allows unbounded subprocess spawns per user. Post-MVP infrastructure concern (middleware / Redis / idempotency key).
- **Migration `002_calls.sql` has no explicit `ON DELETE` policy on `user_id` FK** — defaults to `NO ACTION`, which blocks user deletion when call rows exist. Intentional (preserves audit trail) but undocumented; re-visit when user deletion / GDPR erasure lands.

## Deferred from: code review of story 5-1-build-scenarios-api-and-database (2026-04-24)

- **`run_migrations()` + `executescript` breaks outer `BEGIN IMMEDIATE` atomicity** — `server/db/database.py:82-104` wraps each migration in `BEGIN IMMEDIATE`, re-checks `schema_migrations`, then calls `db.executescript(sql)` which silently COMMITs the outer transaction before running the script. Consequence: (1) the `INSERT INTO schema_migrations(...)` is no longer atomic with the DDL — a crash mid-script can leave the DB partially migrated with no tracking row, (2) the "workers serialise via the lock" claim in the docstring is broken for any migration whose script takes >1 statement. Pre-existing in 001/002; amplified by 003-006 which each carry their own `BEGIN;/COMMIT;`. Root fix = rework `run_migrations` to split statements instead of using `executescript`, or switch to `isolation_level=None` + explicit savepoints.
  - **When to address**: before the first migration that (a) runs multiple DDL statements that MUST be atomic with `schema_migrations` insertion (e.g. a data migration where partial application is dangerous), OR (b) deploys with >1 uvicorn worker. The current VPS runs a single worker, so the multi-worker race is latent.
  - **Trigger check**: any future story that adds a migration performing data transformation (not just schema DDL) — grep the migration for `UPDATE` / `INSERT INTO` against existing tables. Also: the day `deploy/pipecat.service` or `gunicorn/uvicorn` invocation grows a `--workers N` flag with N>1.
  - **Owner/Route**: Architecture (Winston) — plan a `server/db/database.py` refactor, verify with the `prod_snapshot` test harness added in Story 5.1 (Change Log entry 4).

- ~~**No `PRAGMA busy_timeout` on `get_connection()`**~~ — **CLOSED 2026-04-28** by Story 5.3 code review. The TOCTOU race fix in `routes_calls.initiate_call` (`BEGIN IMMEDIATE` around cap-check + INSERT) introduced a contending writer on `/calls/initiate`, tripping the trigger. `await db.execute("PRAGMA busy_timeout = 5000")` was added to `get_connection()` alongside the `foreign_keys` pragma. The original deferred fix shape was applied verbatim — see `server/db/database.py:43-47`.

### How these items get surfaced
Both items live in `server/db/database.py` — any future story that edits that file should re-read this section first. Consider adding a comment at the top of `database.py` pointing here ("See `deferred-work.md` §Story 5-1 before touching this file") so nobody rewrites the migration runner without picking them up.

## Deferred from: code review of 5-1-CI-deploy (2026-04-24)

The CI deploy pipeline (GitHub Actions → VPS) went through adversarial review against the ten trap axes specified in the review prompt. BLOCKERs (DB write perms, `migrate-to-releases.sh` path bug, migration-guard) and MAJORs (host-key TOFU, sudoers wildcard, trigger path filter, unit auto-sync, env ownership, `/health` git_sha hardening) were fixed inline. The items below are real, but not blocking the first `gh workflow run`. Each entry records when it becomes worth doing.

- **`migrate-to-releases.sh` copies `.venv` with possibly-broken entry-point shebangs** — `cp -a /opt/.../repo/server` preserves the existing `.venv`. `.venv/bin/python` is a symlink → `/usr/bin/python3.12` ✓, so the ExecStart invocation works. But `.venv/bin/ruff`, `.venv/bin/alembic`, etc. have absolute shebangs pointing at the *old* `.venv` path. The bridge release's `.venv` is therefore a "ghost" — any code path that invokes entry-points (not just `python main.py`) would silently run the old binaries. The first real CI deploy rebuilds `.venv` from scratch via `uv sync`, so this only affects the bridge window.
  - **Fix shape**: add `sudo -u deploy ~deploy/.local/bin/uv sync --frozen --no-dev` inside `$RELEASE_DIR/server` in `migrate-to-releases.sh`, after the `chown -R` and before the symlink swap.
  - **When to address**: only if the bridge window ever runs anything beyond `python main.py`. Today it doesn't. Re-evaluate the day something in main.py imports a package that shells out to a venv-bin script.
  - **Trigger check**: if a PR introduces `subprocess.run([".venv/bin/..."])` or similar in main.py or its import chain.

- **`uv` is installed via `curl ... astral.sh/uv/install.sh | sh`, no version pinning** — `deploy/setup-vps.sh` step 6. If astral ever changes the install path (e.g. from `~/.local/bin/uv` to `~/.local/share/uv/bin/uv`), the workflow's `$HOME/.local/bin/uv` reference in the "Install Python deps" step breaks silently.
  - **Fix shape**: `curl -LsSf https://astral.sh/uv/<VERSION>/install.sh | sh` with a pinned `<VERSION>`, kept in sync with the `uv` version used in the test job (`astral-sh/setup-uv@v3` auto-picks latest).
  - **When to address**: first time a CI deploy fails with "uv: command not found" on the VPS. Also worth doing preemptively on the next major uv release if astral's changelog mentions path changes.
  - **Trigger check**: grep `astral-sh/setup-uv` in `.github/workflows/` and correlate its version with `setup-vps.sh` install script.

- **`xargs -I{} rm -rf {}` in release pruning is not safe against filenames with spaces/newlines** — `.github/workflows/deploy-server.yml` "Prune old releases" step. Release dirs are git-SHA-keyed (7 hex chars), safe today. Only a risk if someone manually creates a release dir with an unusual name.
  - **Fix shape**: replace with `find $VPS_RELEASES -mindepth 1 -maxdepth 1 -type d | sort -r | tail -n +4 | xargs -r -d '\n' rm -rf --`, or a while-read loop.
  - **When to address**: if anyone ever creates a release dir by hand (e.g. during ops recovery) — prune becomes unpredictable. Cheap one-liner to swap in pre-emptively.

- **Pruning may delete the only rollback target if a healthcheck falsely passes** — workflow's "Prune old releases" runs on `if: success()`. If a deploy swapped the symlink, healthcheck passed on the NEW release, but the release is actually buggy in a way that only manifests later (logs, batch jobs, WebRTC flow), the prune has kept only the last 3 releases including the bad one. Rollback to N-4 is impossible via symlink swap.
  - **Fix shape**: keep last 5 (or configurable via env var), or gate prune behind an "acknowledge deploy success" delay (e.g., only prune on the NEXT successful deploy, which implies the current one stayed healthy).
  - **When to address**: first post-deploy incident where rollback was needed but the target was already pruned. Or when adding real user load (Epic 7+).

- **Backup disk growth** — workflow creates `db.pre-<sha>.sqlite` each deploy, retains 14 days. At today's ~50KB DB × 30 deploys/day × 14 days = 21MB ✓. When DB reaches 100MB (realistic post-MVP), same cadence = 42GB, which approaches Hetzner small-VPS disk budget.
  - **Fix shape**: replace `-mtime +14` with `ls -1t | tail -n +N` (keep last N regardless of age), OR add a size-cap (keep backups under X GB total).
  - **When to address**: when `du -sh /opt/survive-the-talk/backups` crosses 1GB, OR before a growth story (e.g., call transcripts in DB).
  - **Trigger check**: any migration that adds a new large table (transcripts, audio metadata, call logs).

- **pipecat.service has no `StartLimitBurst` / `StartLimitIntervalSec`** — `Restart=on-failure RestartSec=5s` without burst limit means a boot-broken service crashes-loops forever. The workflow's 5-attempt healthcheck window (15s) can coincidentally catch a "starting" window before the next crash and report green.
  - **Fix shape**: add `StartLimitBurst=5` and `StartLimitIntervalSec=60` under `[Service]`, plus `StartLimitAction=none`. Combined with the healthcheck step, a repeatedly-failing service cleanly signals "down".
  - **When to address**: first incident where a buggy release crash-looped and healthcheck passed ambiguously. Also: cheap, can be in the same PR as the `/health` `git_sha` hardening.
  - **Trigger check**: any PR that edits `deploy/pipecat.service` should pick this up opportunistically.

- **No SSH connection multiplexing in the workflow** — the deploy job opens 6+ separate SSH sessions (verify guard, backup, rsync, deps-install, unit-sync, swap, restart, journalctl-on-fail). Each pays ~2s handshake. OpenSSH `ControlMaster=auto` + `ControlPersist=10m` would cut ~10s off each deploy.
  - **Fix shape**: add an `~/.ssh/config` step that sets `ControlMaster=auto ControlPath=/tmp/cm-%r@%h:%p ControlPersist=10m` for the VPS host, early in the job.
  - **When to address**: when deploy latency becomes a developer-experience pain (probably once per-push cadence > a few per hour, or when Epic 6+ has many rapid iteration cycles).

- **`deploy/backup.sh` is an unused placeholder** — predates the CI workflow's inline `sqlite3 .backup`. Not called by anything, not on cron.
  - **Fix shape**: either delete the file, or wire it up to a systemd timer for a nightly non-deploy backup (orthogonal to the pre-deploy backup done by the workflow).
  - **When to address**: during a cleanup pass, OR when a "daily cold backup independent of deploy cadence" becomes a real need (e.g., for GDPR 30-day retention).

- **`deploy/Caddyfile` / `deploy/caddy.service` are not part of the CI pipeline** — the narrower path filter in the workflow trigger (§MAJOR #7 fix) excludes them correctly, but that also means Caddy changes require manual SSH + `systemctl reload caddy`. Not currently documented.
  - **Fix shape**: either (a) add a one-sentence note in `deploy/README.md` "How Caddy changes are applied", OR (b) extend the pipeline to scp+reload Caddy similarly to pipecat.service.
  - **When to address**: first time someone edits `Caddyfile` and forgets to reload Caddy on the VPS.

- **Deploy summary step has no context when healthcheck fails** — `Deploy summary` runs `if: always()`, prints release id and commit. But when the healthcheck fails, the user sees the journalctl dump inline AND the summary separately; ordering in the GH UI can be confusing.
  - **Fix shape**: fold the journalctl dump into a `if: failure()` "Deploy diagnostics" step AFTER the healthcheck, and always print `systemctl status pipecat.service | head -10` in the summary for success-path visibility.
  - **When to address**: first time a deploy fails and the GH UI is hard to read. Cheap UX polish.

### How these items get surfaced
Most of these live in `.github/workflows/deploy-server.yml`, `deploy/setup-vps.sh`, or `deploy/pipecat.service` — any future story that edits deploy infrastructure should re-read this section first. If the "Known gaps" section in `deploy/README.md` grows, sync it here (or inversely — the README "Known gaps" is already a bridge to this file).

## Deferred from: code review of story 5-2-build-scenario-list-screen-with-scenariocard-component (2026-04-27)

- **Test fixtures duplicated across scenario test files** — `_build({...}) Scenario` factory and `registerFallbackValue(const LoadScenariosEvent())` are copy-pasted across `client/test/features/scenarios/bloc/scenarios_bloc_test.dart`, `client/test/features/scenarios/views/scenario_list_screen_test.dart`, `client/test/features/scenarios/views/widgets/scenario_card_test.dart`, and `client/test/app_test.dart`. Cheap fix when a 4th caller appears: extract `client/test/features/scenarios/_helpers.dart` exporting `buildScenario({...})` + `setupScenariosFallback()`.
  - **When to address**: when adding the next test file that needs the same fixture, OR during an Epic 5/6 test pass.
  - **Trigger check**: any new file under `client/test/features/scenarios/` or `client/test/features/debrief/` that re-declares `_build`/`buildScenario`.

- **`scenarios_bloc_test.dart` spam-tap test uses microtask ordering + 200ms hard-coded `wait`** — `client/test/features/scenarios/bloc/scenarios_bloc_test.dart:131-141`. The test relies on `await Future<void>.delayed(Duration.zero)` to "yield" between the two `bloc.add` calls, plus a 200ms `wait` for assertions to snapshot the post-delay state. Microtask ordering and timer precision aren't guaranteed across Dart SDK / `bloc_test` versions — flake risk on slow CI runners.
  - **Fix shape**: replace the delayed-repo-answer pattern with an explicit `Completer<List<Scenario>>` the test resolves after dispatching the second event; assert the second `bloc.add` short-circuits before completing the first.
  - **When to address**: first time the test flakes in CI. Or during an Epic 5 test-quality pass.
  - **Trigger check**: any change to `IncomingCallBloc._onAccept` spam-tap pattern (which this test mirrors) — bring both into the new pattern together.

- **No localization (i18n) for hardcoded English strings in scenarios feature** — `client/lib/features/scenarios/views/scenario_list_screen.dart` ("Tap to retry"), `client/lib/features/scenarios/views/widgets/scenario_card.dart` ("View debrief", "Tap phone to call", "Best:", "attempts", "Not attempted", "in progress", "completed"), `client/lib/features/debrief/views/debrief_placeholder_screen.dart` ("Debrief placeholder — scenario X (Story 7.x)", "Back to scenarios"). Project-wide debt; this story does not introduce the issue but does extend the surface.
  - **Fix shape**: introduce `flutter_localizations` + `intl` + `lib/l10n/app_en.arb` (initially English-only), refactor strings to `AppLocalizations.of(context).xxx`. Same approach for auth/onboarding/call screens already shipped.
  - **When to address**: before Epic 10 store submission (i18n is a prerequisite for non-EN markets), OR if a French-speaking beta tester explicitly requests it.
  - **Trigger check**: any PR that adds a new user-visible string anywhere in `client/lib/features/**/views/`.

- **`ScenariosLoaded` with an empty list renders a blank `ListView` with no message** — `client/lib/features/scenarios/views/scenario_list_screen.dart:54-73`. Server currently seeds 5 scenarios, so the empty case is unreachable in MVP, but a future tier-filtered query or a maintenance window could legitimately return `[]` and the screen would silently render blank with no copy.
  - **Fix shape**: add `if (scenarios.isEmpty) return _EmptyView();` branch with a centered Text ("No scenarios available yet — pull to refresh") + retry tap area mirroring `_ErrorView`.
  - **When to address**: when any feature introduces a code path where `GET /scenarios` can legitimately return `[]` (tier filter, geo-gate, A/B test, maintenance mode). Or as part of the Story 5.4 content-warning work if it leads to a "no scenarios match your filter" path.
  - **Trigger check**: any change to `server/api/routes_scenarios.py` that adds query parameters or conditional WHERE clauses.

- **Back-press / back-swipe on `/call` still backgrounds the app** — `client/lib/features/call/views/call_placeholder_screen.dart` and `client/lib/app/router.dart`. Walid raised this during Story 5.2 review (2026-04-27): swiping back from the call screen on Android does NOT bounce off `PopScope(canPop: false)` — the system goes straight to `moveTaskToBack` and the app drops to background. Verified on a Pixel 9 Pro XL (Android 14) via on-device adb logcat tracing.
  - **🟢 RESOLUTION DECIDED 2026-04-29 — see [ADR 003 — Call-Session Lifecycle](../planning-artifacts/adr/003-call-session-lifecycle.md).** Option A (raw LiveKit) retained over Option B (CallKit/ConnectionService wrapper) based on outgoing-only flow + immersive-conversation mental model + low native-dep tolerance + Windows-only dev setup. Story 6.1 ships a 3-tier back-press strategy: (1) push the call screen via root `Navigator.push` to detach from `_GoRouterRefreshStream` (the untried 5th mitigation, hypothesis-driven); (2) LiveKit `AndroidAudioServiceConfiguration` foreground service for audio keepalive; (3) iOS `UIBackgroundModes: [audio]`. Smoke test in §"Smoke test for Story 6.1" of ADR 003 is the Definition-of-Done gate. If Tier-1 empirically fails on Pixel 9 Pro XL, UX-DR10 is reinterpreted as "session preserved on background, return via foreground-service notification" — the ADR documents this fallback explicitly.
  - **What was tried during the review session and why each failed**:
    1. **`PopScope(canPop: false)` on the Scaffold root** — kept (it's harmless), but on its own the Flutter back-callback flapped between Flutter's and Android's default callbacks during the predictive back gesture. The logs showed `setTopOnBackInvokedCallback` toggling every ~500ms; the Android default callback was the one in charge when the gesture committed → `moveTaskToBack`.
    2. **Manifest `android:enableOnBackInvokedCallback="true"`** — opted into Android 13+ predictive back. Did not change the flapping behaviour.
    3. **Native `OnBackInvokedDispatcher.registerOnBackInvokedCallback(PRIORITY_OVERLAY, ...)` from `MainActivity.kt`** — Walid's logcat showed `startBackNavigation` still using the priority `-1` Android default callback at swipe time; my registered overlay callback was apparently never invoked. Either the registration silently failed, or Flutter's `FlutterActivity` overrides the dispatcher in a way that bypasses my callback.
    4. **AndroidX `OnBackPressedDispatcher.addCallback(this, OnBackPressedCallback(true) { … })`** — required switching `MainActivity` from `FlutterActivity` to `FlutterFragmentActivity` to access `onBackPressedDispatcher`. The build compiled but the user reported the same backgrounding behaviour after rebuild and on-device test.
  - **Probable root cause**: a known interaction between Flutter's embedding, go_router's `CustomTransitionPage`, the `_GoRouterRefreshStream(authBloc.stream)` listenable, and Android's predictive back system. The `PopScope` (or any back interceptor we register) is being torn down or de-prioritised at the moment the gesture commits. None of the four standard mechanisms held in our setup. Other apps (WhatsApp/FaceTime/native dialer) solve this — but with full native call APIs (CallKit on iOS, ConnectionService on Android) that own the audio session lifecycle and back behaviour together.
  - **Decision (Walid 2026-04-27)**: revert all native + manifest changes from the review session, keep only `PopScope(canPop: false)` (best-effort, harmless), defer the real fix to Story 6.1.
  - **Files reverted in the review session**: `AndroidManifest.xml` (removed the `enableOnBackInvokedCallback` flag), `MainActivity.kt` (back to the empty default), deleted `client/lib/core/system/back_press_blocker.dart`, removed the `BackPressBlocker.setBlocked(...)` calls from `CallPlaceholderScreen`'s `initState`/`dispose`. The `PopScope(canPop: false)` wrapper stays in `CallPlaceholderScreen.build`.
  - **Why this is acceptable for shipping Story 5.2**: the `/call` route is still a placeholder (the real WebRTC + Rive call screen is Story 6.1+). When the user accidentally backgrounds the app today, `_CallPlaceholderScreenState.dispose()` cleanly calls `room.disconnect()` — no resource leak. Re-opening the app cold-starts onto `/scenarios` (no broken state). The UX is sub-optimal but not a regression — the same back-swipe behaviour existed on `/call` before Story 5.2 (it just wasn't visible because the previous root route was a static placeholder, not a real screen reachable from a card tap).
  - **Fix shape for Story 6.1**: implementing CallKit (iOS) + ConnectionService (Android) is the right place for this. Those native call APIs:
    - Keep the audio session alive when the app backgrounds (currently LiveKit dies silently on background).
    - Surface a system "ongoing call" notification + lock-screen control.
    - Own the back-press behaviour at the OS level — back-swipe is naturally blocked while the call is ongoing because the OS treats it as a system-managed phone call.
    - Plus: a `flutter_callkit_incoming` (or similar) package wraps both APIs in a single Dart surface; that's likely the path of least resistance.
  - **When to address**: Story 6.1 (build-call-initiation-from-scenario-list-with-connection-animation) MUST decide on the call-session lifecycle approach (raw LiveKit vs. CallKit/ConnectionService wrapper) before wiring the real call screen. The back-press blocking falls out of that decision.
  - **Trigger check**: any change to `client/lib/features/call/views/` should re-read this entry first. If a PR introduces a real call screen (Rive + active WebRTC + hang-up logic), the back-press blocking + audio-session-keep-alive must ship together with it.

- **No widget test for `PopScope(canPop: false)` on `CallPlaceholderScreen`** — same file. A widget test was attempted but cannot pass: `_CallPlaceholderScreenState.initState` calls `Room()` from `livekit_client`, which constructs an `Engine` + `TTLMap` with a 15-second periodic timer that `room.disconnect()` does not cancel; the test framework reports "Pending timers" at end-of-test. Refactoring the screen to accept an injected `Room` factory would unblock the test.
  - **Fix shape**: Story 6.1 refactors this screen with the real Rive canvas + a proper Room factory injection point. Then write the test (pump the screen with the fake Room, assert that `tester.binding.handlePopRoute()` does not change the visible screen).
  - **When to address**: Story 6.1, alongside the call-screen refactor.
  - **Trigger check**: any change to `client/lib/features/call/views/call_placeholder_screen.dart` should pick this up.

- **`_Avatar.errorBuilder` returns `SizedBox.shrink()` and relies on the parent `Container` for size + colour** — `client/lib/features/scenarios/views/widgets/scenario_card.dart:98`. Today this works because the parent `Container(width: avatarSmall, height: avatarSmall, color: AppColors.avatarBg)` clips to the gray circle even when the image collapses. But the errorBuilder makes no local size guarantee — any future refactor that moves the size constraint onto the `Image.asset` (e.g. removing the `Container` in favour of `SizedBox.fromSize` + `DecoratedBox`) would silently break the missing-asset visual fallback.
  - **Fix shape**: `errorBuilder: (_, _, _) => const SizedBox(width: AppSpacing.avatarSmall, height: AppSpacing.avatarSmall)` so the contract lives in the errorBuilder itself, not in the surrounding widget tree.
  - **When to address**: any PR that touches `_Avatar` or extracts a shared `CharacterAvatar` widget. Cheap one-liner — can be folded into the next visual-asset PR (e.g. when a 6th scenario is added).
  - **Trigger check**: grep for `_Avatar` or `assets/images/characters/` in any incoming diff.

### How these items get surfaced
Three of the original four items (test fixtures, spam-tap delay, i18n) are test/code hygiene that will resurface during the next story that edits `client/test/features/scenarios/` or adds new user-visible strings. The placeholder back-button entry is self-evicting — Story 7.x replaces the screen wholesale. The two new entries (empty-list state + avatar errorBuilder) trigger only on specific structural changes — see each item's "Trigger check" line.

## Deferred from: code review of story 5-3-build-bottomoverlaycard-and-daily-call-limit-enforcement (2026-04-28)

- **`meta.calls_remaining` goes stale after `/calls/initiate` succeeds** — `client/lib/features/scenarios/bloc/scenarios_bloc.dart`. The bloc loads once (guarded by `state is ScenariosLoading`); BOC reads cached `state.usage`. After a successful call, the user returns to the list with the pre-call counter, but the server has already decremented. User sees "calls remaining: 1", taps a scenario, gets 403.
  - **Fix shape**: Story 6.x (call return handoff). When the call screen pops, dispatch a `RefreshUsageEvent` (or full `LoadScenariosEvent`) on the scenarios bloc — either via a route observer or a result-callback pattern.
  - **When to address**: Story 6.1 (call initiation) — the call entry path will introduce the round-trip; the return path lands wherever Epic 6 wires call-end → list.
  - **Trigger check**: any PR adding a real call screen behind the BOC tap (or replacing `PaywallSheet.show` with a true `/calls/initiate` invocation from the list).

- **`started_at` lex comparison silently breaks if any row uses non-Z UTC ISO format** — `server/db/queries.py:310-324`, `server/api/usage.py:_utc_day_start_iso`. `WHERE started_at >= ?` is a string compare. `_utc_day_start_iso` produces `"...Z"`; if `now_iso()` (or any future writer / SQL admin tool / migration backfill) produces `"...+00:00"` or microsecond-precision, the order flips (`+` < `Z` lexicographically). A paid user with 3 calls today that all happen to be `+00:00` rows would read as 0 used → cap bypass.
  - **Fix shape**: (a) add a CHECK constraint on `call_sessions.started_at` enforcing the `Z` suffix, OR (b) do the comparison via `datetime.fromisoformat(...)` Python-side, OR (c) add a server-startup integrity check that scans `call_sessions` for non-Z formats and refuses to boot.
  - **When to address**: before any code path other than `now_iso()` writes `call_sessions.started_at` (admin tools, migration backfills, second persistence helper), OR before tier-transition logic that depends on per-row temporal ordering.
  - **Trigger check**: any PR introducing a new `INSERT INTO call_sessions` statement (grep `call_sessions`); any migration that touches `call_sessions.started_at`.

- **`count_user_call_sessions_total` ignores tier-transition history** — `server/api/usage.py:42-50`. Free path counts ALL lifetime sessions regardless of when they happened. A user upgraded to paid (3 lifetime calls already used) → unsubscribed → reverts to free → hard-capped at 0 forever. Conversely, a fresh paid upgrader carries their free history. FR21 doesn't currently address tier-transition boundaries.
  - **Fix shape**: Story 8.x will introduce a `users.tier_changed_at` (or per-tier-period sessions buckets). The cap-policy must then count "calls since the current tier started", not lifetime.
  - **When to address**: Epic 8 (StoreKit 2 / Google Play Billing) — the moment a user can leave free tier, this becomes user-visible.
  - **Trigger check**: any PR adding a `tier` column transition (UPDATE users SET tier = ...) outside test fixtures, or any migration introducing tier metadata.

- **`compute_call_usage` raises `ValueError` for tier ∉ {free, paid}, swallowed by broad 500 catch-all** — `server/api/usage.py:46-52`, `server/api/routes_calls.py:136`. A future migration introducing `tier='trial'` (without plumbing it into `compute_call_usage` simultaneously) would surface as `CALL_PERSIST_FAILED 500` — misleading code (the call wasn't capped, no DB INSERT was attempted). Only matters when a third tier is added.
  - **Fix shape**: when adding a new tier value, audit `compute_call_usage` first; OR add a startup assertion that `users.tier` only contains values the policy module recognises; OR catch `ValueError` in the routes and emit a clearer error code.
  - **When to address**: Story 8.x or any earlier story that introduces a new tier value (e.g. trial / employee / admin).
  - **Trigger check**: any PR that changes the set of tier strings — grep for `"free"` / `"paid"` literals in `server/`.

- **Semantics for `paidExhausted` BOC variant lacks explicit "no action" affordance** — `client/lib/features/scenarios/views/widgets/bottom_overlay_card.dart`. The non-actionable variant has `button: false` (correct), but the composed semantic label `"No more calls today. Come back tomorrow."` is shaped identically to actionable variants. A blind user navigating with VoiceOver may swipe expecting an action.
  - **Fix shape**: add `Semantics(readOnly: true, ...)` for the non-actionable variant, or distinguish the announcement (`"Status: ..."` prefix). Polish, not a defect.
  - **When to address**: any A11y pass touching scenario-list semantics, or post-launch user feedback from screen reader users.
  - **Trigger check**: PR touching `BottomOverlayCard` Semantics or any A11y-focused work.

- **401 `AUTH_UNAUTHORIZED` is misleading code for orphaned-user path** — `server/api/routes_scenarios.py`. If a JWT is valid but the `users` row was deleted, the route raises `AUTH_UNAUTHORIZED`. The token IS valid; the account is gone. User signs in again, gets the same token, hits the same 401 — login loop. Not currently exercisable (no user-deletion path exists yet).
  - **Fix shape**: introduce `ACCOUNT_DELETED` (or `USER_NOT_FOUND`) error code; client clears the JWT cache on receiving it.
  - **When to address**: Story 10.x (GDPR erasure / account-delete flow), or earlier if any admin-side tool can delete `users` rows.
  - **Trigger check**: any PR that adds a `DELETE FROM users` path; any error-envelope change in `server/api/responses.py`.

- **`subprocess.Popen` rollback leaves LiveKit room and tokens minted (billing / cap-counter mismatch)** — `server/api/routes_calls.py:101-131, 170-181`. Pre-existing from Story 4.5. If token mint succeeds, INSERT succeeds, then Popen raises `OSError`, the rollback `DELETE` removes the DB row but the LiveKit-side artifacts remain. With Story 5.3's user-visible cap counter, the discrepancy becomes more visible: server's free-tier count is now 1 less than LiveKit's billing count.
  - **Fix shape**: Epic 6.4 (`POST /calls/{id}/end`) owns the LiveKit cleanup contract. Add explicit `livekit.delete_room(room_name)` to the Popen-failure rollback path.
  - **When to address**: Epic 6.4 — folds into the existing Popen lifecycle work.
  - **Trigger check**: any PR refactoring `routes_calls.initiate_call` rollback path or introducing real LiveKit room cleanup.

- **`CallUsage.fromMeta` accepts negative `calls_remaining` / `calls_per_period` without clamping** — `client/lib/features/scenarios/models/call_usage.dart`. A server bug or middlebox tampering returning `-1` would not be caught at the parse boundary. `hasCallsRemaining = callsRemaining > 0` evaluates false for negatives so the BOC shows the exhausted variant — current accessor surface degrades gracefully — but any future display that shows the raw integer would render garbage.
  - **Fix shape**: clamp `callsRemaining = max(0, json['calls_remaining'])` and assert `calls_per_period > 0` at parse time; throw `FormatException` to surface server-version skew rather than silently mask it.
  - **When to address**: any story that adds a UI surface displaying `calls_remaining` directly (e.g. account / settings screen).
  - **Trigger check**: any PR introducing a new widget that reads `CallUsage.callsRemaining` outside `BottomOverlayCard`.

- **Count queries do not filter on `call_sessions` status — orphan rows permanently burn lifetime quota** — `server/db/queries.py:295-322` (`count_user_call_sessions_total/_since`), `server/api/usage.py:42-50`. Both helpers `SELECT COUNT(*)` without any status filter. If FastAPI worker crashes / Popen fails / network blip occurs between INSERT and bot spawn, the row remains and consumes 33 % of a free user's lifetime quota for a call that never happened. The window is short (Popen comes immediately after INSERT) and the rollback path in `routes_calls.py:170-181` deletes the row on Popen failure, so this only fires on hard crashes (worker SIGKILL / OOM / panic). Lifetime cap of 3 means each orphan row is a 33 % burn — meaningful blast radius for a real user.
  - **Fix shape**: Epic 6.4 introduces `POST /calls/{id}/end`, which is the natural place to flip a status field. Add migration `008_call_sessions_status.sql` introducing `status TEXT NOT NULL DEFAULT 'pending'` with values `'pending' / 'completed' / 'failed'`. INSERT defaults to `'pending'`; `/calls/{id}/end` flips to `'completed'`; the cap-counter SELECT becomes `WHERE status IN ('pending', 'completed')`. Optionally add a janitor sweep that flips `'pending'` rows older than 1 hour to `'failed'` (covers the orphan-row case).
  - **When to address**: Epic 6.4 (call lifecycle endpoint). Doing it earlier means shipping a column with no writer that sets it to `'completed'` — dead code.
  - **Trigger check**: any PR that introduces `POST /calls/{id}/end` or any new write path on `call_sessions`. Also: if VPS worker crash logs show orphan-row cases in the wild before Epic 6.4 ships, escalate.
  - **Owner/Route**: Story 6.4 (or earlier if a real orphan-row report comes in). Migration + queries update + 1 new helper.

### How these items get surfaced
Three items (`meta.calls_remaining` staleness, `Popen`/LiveKit cleanup, tier-transition history) are blocked on specific upcoming epics (6, 6.4, 8) and will resurface naturally when those epics begin. The format-consistency, third-tier, and account-deletion items are dormant until a specific code path is added — each entry's "Trigger check" line names the file/grep that catches it. The two polish items (Semantics readOnly, `CallUsage` negative clamp) are cheap one-liners that can be folded into any future PR touching those files.

## Deferred from: code review of story-5.4 (2026-04-28)

- **Hardcoded layout literals in `content_warning_dialog.dart`** — fontSize 24/14/12, height ratios 29/24, padding 36/24/15/31, radii 42/18/999 inlined. AC6 hex-only test stays green, but Story 5.3 established `AppSpacing`/`AppTypography` token discipline. Cross-cutting design-token pass when the next sheet/dialog ships, or as a dedicated tokenisation PR. [client/lib/features/scenarios/views/widgets/content_warning_dialog.dart]
- **Rapid double-tap on phone icon → two sheets** — `_onCallTap` is async with no re-entry guard. Realistic risk low (Flutter's modal route generally swallows taps during transition), but a hostile-thumb double-tap could queue two `showModalBottomSheet` calls. Fix has design implications (StatefulWidget + busy flag, or disable the icon while in-flight). [client/lib/features/scenarios/views/scenario_list_screen.dart:138-148]
- **Daily call limit mid-dialog bypass** — if the Story 5.3 BOC counter exhausts while the sheet is open, "Pick up" still navigates to `/call`. Theoretical (only completing a call exhausts the counter, which dismounts the list screen). Re-check `state.usage` after `proceed=true` if a real bypass is observed. [client/lib/features/scenarios/views/scenario_list_screen.dart:138-148]
- **RTL locale — `WrapAlignment.end` aligns to left edge under Arabic/Hebrew** — no-op today (project is French-only per memory + Dev Notes "no localization"). Resurface when localisation lands. [client/lib/features/scenarios/views/widgets/content_warning_dialog.dart:208]
- **Android back-press / full-distance drag-down dismissal path untested** — `?? false` coercion at line 42 handles the null-result case, but no test pumps a back-press or threshold-crossing drag to verify the resolved value is `false`. Add when next touching dialog tests. [client/test/features/scenarios/views/widgets/content_warning_dialog_test.dart]
- **AA contrast claim unverified by automated test** — `app_colors.dart:43` comment claims `headsUpAccent on headsUpBg ≈ 4.6:1`; `#8F8621` on `#F5FFAD` actually computes around 4.0-4.5:1 depending on tool — borderline AA-large. No `theme_tokens_test.dart` enforcement. Add a contrast-ratio assertion when the next palette swap happens. [client/lib/core/theme/app_colors.dart:43]

## Deferred from: code review of 5-5-refactor-scenario-list-error-screen-with-empathetic-ux (2026-04-29)

- **retryCount increments unbounded** — by design per AC1 (no third copy tier); document only; close when localisation pass adds copy variants. [client/lib/features/scenarios/bloc/scenarios_bloc.dart:~62]
- **`previous = state` capture stale under future co-events** — moot today (single-event surface). Resurface when `RefreshScenariosEvent` or similar co-handler is added. [client/lib/features/scenarios/bloc/scenarios_bloc.dart:~27-30]
- **`_classifyApiCode` not unit-tested in isolation** — only bloc-integration coverage. Promote to a shared util + add unit tests when a second feature adopts the same error-mapping pattern. [client/lib/features/scenarios/bloc/scenarios_bloc.dart:~61-65]
- **Loading state renders blank screen for up to 15s after a retry tap** — pre-existing from Story 5.2; no spinner, no preserved error context. Add a `_LoadingView` with progress when UX feedback prioritises this. [client/lib/features/scenarios/views/scenario_list_screen.dart:~37-39]
- **No test asserts `BottomOverlayCard` absent during `ScenariosLoading`** — pre-existing coverage gap; current Loading test only asserts no `ScenarioCard`. Add when next touching the file. [client/test/features/scenarios/views/scenario_list_screen_test.dart:~129-144]
- **`SCENARIO_LOAD_FAILED` and broader server-code error mapping** — `_classifyApiCode` covers the four canonical client codes only; server emits ~6 5xx-class string codes (LIVEKIT_TOKEN_FAILED, BOT_SPAWN_FAILED, etc.). Bundle into a status-code-aware refactor in Epic 6+ when call/debrief flows add error surfaces. [server/api/routes_calls.py, server/api/routes_scenarios.py]
- **Empty/whitespace `ApiException.code` falls into UNKNOWN_ERROR** — defensive future-proofing; not exercised today. Add if a server change ships codes missing the `error.code` field.
- **Hardcoded tap coords `Offset(50, 100)` / `Offset(10, 10)` fragile under viewport refactors** — at 390×844 they correctly land outside the IconBadge, but a future SafeArea/header tweak could overlap. Replace with widget-finder + `tester.tap(find.byType(...))` next time the test is touched. [client/test/features/scenarios/views/scenario_list_screen_test.dart:~320]
- **`FilledButton` missing `foregroundColor`** — explicit white text/icon means rendering is correct, but ripple/highlight overlay falls back to theme `onPrimary` (may look wrong on accent green). Add `foregroundColor: AppColors.background` (or matching) when next polishing the button. [client/lib/features/scenarios/views/scenario_list_screen.dart:~351-360]
- **`FittedBox(scaleDown)` shrinks 24-px refresh icon below design at textScaler 3.0** — accessibility max iOS edge case; on 320-wide phone the cluster overflows and FittedBox scales the icon down to ~22px. Surface when MVP testing exposes accessibility regressions. [client/lib/features/scenarios/views/scenario_list_screen.dart:~285-308]
- **In-flight guard test body unchanged but renamed** — verify `verify(...).called(1)` still locks in single Loading→Error cycle when next touching the test file. [client/test/features/scenarios/bloc/scenarios_bloc_test.dart:~775]

## Deferred from: Story 6.1 (build-call-initiation-from-scenario-list-with-connection-animation, 2026-04-29)

- **🚨 iOS smoke test (Story 6.1 AC11)** — Tier-1 root-Navigator detach validated on Pixel 9 Pro XL (Android 14): predictive-back blocked, audio uninterrupted. iOS Control-Center-pull / lock-screen smoke test NOT executed because no iOS build pipeline exists yet (no `codemagic.yaml`, no Mac/Xcode access). **Owned by Epic 10 Story 10-4 (set-up-beta-testing-pipeline-and-analytics)** — set up CodeMagic, then retroactively run the iOS smoke for Story 6.1, 6.2, 6.4, 6.5 before MVP launch. Until then `UIBackgroundModes: [audio]` (already in `Info.plist:69-72` from Story 4.5) is the unverified-on-device claim. [_bmad-output/planning-artifacts/adr/003-call-session-lifecycle.md §Tier 3]
- **🚨 LiveKit `AndroidAudioServiceConfiguration` not exposed at 2.6.4** — ADR 003 §Tier 2 assumed this API installs an audio foreground service automatically. Verified absent in `livekit_client 2.6.4` (the plugin's `AndroidManifest.xml` is empty; only mediaProjection/screen-share has a service example). Manifest permissions (`FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MICROPHONE`) are pre-declared but inert. **Mitigation status:** Tier 1 alone is empirically sufficient on Pixel 9 Pro XL — back-press blocked, app stays foregrounded, audio uninterrupted. Tier 2 fallback ("session preserved on background, return via notification") is therefore unavailable today; if a future Android device defeats Tier 1, the upgrade path is `flutter_background` integration OR a `livekit_client` major bump that re-exposes the API. [client/android/app/src/main/AndroidManifest.xml:8-15]
- **Generic-error UX is silent on the scenario list** — non-NETWORK / non-CALL_LIMIT `ApiException` paths reset the `_initiating` flag in `try/finally` and return — no toast, no banner, no fade. Acceptable trade-off (only realistic case is a 5xx, rare) but a "did my tap register?" perception risk. AC6's spec'd 300ms `Curves.easeIn` opacity overlay was simplified to no-op because the user is already on the list. Resurface if MVP beta surfaces a "the call button is broken" complaint. [client/lib/features/scenarios/views/scenario_list_screen.dart:~190-205]
- **`POST /calls/{id}/end` cleanup contract** — explicitly out of scope per ADR 003 §"Exit-path orchestration". `CallBloc._onHangUpPressed` carries a `// TODO(Story 6.4)` marker. Story 6.4 owns the consolidated cleanup (Popen rollback, `call_sessions.status` migration, janitor sweep, counter `WHERE status IN (...)` filter). [client/lib/features/call/bloc/call_bloc.dart:~96]
- **`CallBloc._onCallStarted` minimum-1s hold race with HangUpPressed** — if the user taps hang-up during the 950ms `Future.delayed(remaining)` window, the bloc emits `CallEnded` (from the hangup handler) and may then emit `CallConnected` (from the racing onCallStarted handler) on top of it. The `if (emit.isDone) return` check only fires after `_onCallStarted` exits, not on a parallel hangup. Mitigation: the room is already disconnected by hang-up so the visual flicker is brief; CallScreen's listener pops on `CallEnded` not on subsequent emits, so the user-visible behaviour is correct. Add a `_canceled` flag if a regression test ever surfaces the visual flicker. [client/lib/features/call/bloc/call_bloc.dart:~67-80]
- **`Future.timeout` 5s timer not cancellable from outside the handler** — if the bloc is closed mid-`room.connect`, the 5s timer keeps ticking until either connect resolves or 5s elapses. Bloc `close()` returns to caller faster than the timer (which fires `TimeoutException` into a closed bloc — silently dropped via `if (emit.isDone) return`). Real-world impact: ~5s of zombie-timer memory after a fast app-close. Acceptable; revisit if profiling surfaces it.
- **Test stub `_stubCallScreen` in `scenario_list_screen_test.dart`** — the production `CallScreenBuilder` injection seam exists ONLY to keep tests fast and leak-free (real `Room()` constructor starts background timers from TTLMap + SignalClient). If a code reviewer prefers a NavigatorObserver-based assertion (no production seam), the seam can be removed once the LiveKit SDK exposes a way to construct a Room without timers, or once we wrap `Room` behind our own factory. [client/lib/features/scenarios/views/scenario_list_screen.dart:~26-36]
- **`call_bloc_test.dart` drives `streamCtrl.add(...)` directly because `EventsEmitter.emit` is `@internal`** — tests-only workaround documented inline. Fragile if `livekit_client` makes `streamCtrl` private in a future minor; tests would break compile-time which is loud, so acceptable. [client/test/features/call/bloc/call_bloc_test.dart:~163]
- **No screen-test for IncomingCallScreen → CallScreen root-Navigator push** — bloc-level test covers the path indirectly (it asserts `IncomingCallConnected` is emitted with the session). The widget-level listener that does the actual push has no direct test. Add when next touching `incoming_call_screen_test.dart`. [client/test/features/call/views/incoming_call_screen_test.dart]
- **`CallScreen.scenario` is unused in 6.1 (lint-suppressed)** — used by Story 6.2's Rive character variant. The `// ignore: unused_field` annotation in `call_bloc.dart:14` is honest but flags as a TODO seam. Resolves when 6.2 wires the canvas. [client/lib/features/call/bloc/call_bloc.dart:14]
- **Connecting-state visuals inherited from Story 4.5 placeholder, not validated as final UX** — AC7 explicitly said "reuse `_buildPulsingDots` and `_buildHangUpButton` from `call_placeholder_screen.dart` verbatim" because Story 6.2 will replace the surface with a Rive canvas. The "Connecting..." text + 3-dot loader is a transient state (~1-2s) carried over from the PoC. If the final visual differs, the change lands in Story 6.2 alongside the Rive canvas. [client/lib/features/call/views/call_screen.dart]
- **`CallScreen` test "renders ... + 3 pulsing dots" counts containers by shape+color — fragile** — refactoring the dots to use a custom widget would break the assertion. Add a `Key` to the dots row and switch to `find.byKey` when next touching the file. [client/test/features/call/views/call_screen_test.dart:~78-88]
- **`call_bloc_test.dart` uses `clearInteractions` then `verifyNever`** — works but is a code smell (the cleaner pattern is a local disconnect-call counter). Refactor when next touching the file. [client/test/features/call/bloc/call_bloc_test.dart:~165-172]

## Deferred from: code review of story 6-1-build-call-initiation-from-scenario-list-with-connection-animation (2026-04-30)

- **Connect/hang-up race emits `CallConnected` after `CallEnded`** — already documented above (line 220) as a known state-machine dirt with no user-visible impact (CallScreen's listener has already popped on `CallEnded`). Cosmetic; no patch needed unless a regression test surfaces the visual flicker. [client/lib/features/call/bloc/call_bloc.dart:~75-81]
- **`CallBloc._scenario` field unused with `// ignore: unused_field`** — Story 6.2 will read it for character variant. Same item as line 225; surfaced again by review for visibility. [client/lib/features/call/bloc/call_bloc.dart:13-14]
- **Test parametrised over fixed `["waiter_easy_01", "cop_hard_01"]` list — no full-file-set check** — adding a "every YAML in dir is loadable" generic test (`pytest.mark.parametrize` over the actual scanned directory) is Story 6.x housekeeping when scenario count exceeds 5. [server/tests/test_calls.py]
- **`call_bloc_test` couples to LiveKit `@internal` `streamCtrl` API** — already tracked above (line 223). Cost-benefit at one test site does not justify a production-API change.


## Deferred from: code review of story 6-4-implement-silence-handling-and-character-hang-up-mechanic (2026-05-13)

- **`_remoteEndPending` flag never reset (CallBloc)** — set in `_onRemoteCallEnded`, never cleared. Works only because bloc closes after `CallEnded`. If future story adds reconnect-without-close, will deadlock the next silence ladder. Mirror of dev OQ6. [client/lib/features/call/bloc/call_bloc.dart:~46,229]
- **`_publishPlaybackIdle` has no `turn_id` idempotency** — SCTP is reliable but duplicate or out-of-order client republishes will restart the server-side ladder for that turn. A monotonic `turn_id` would make this deterministic. [client/lib/features/call/views/call_screen.dart:~488-504]
- **`bot.py on_data_received` has no auth / participant check / rate-limit on the upstream `playback_idle` envelope** — a malicious or accidental client can spam `{"type":"playback_idle"}` to reset the silence ladder at will, defeating the patience mechanism. PoC acceptable; production hardening before launch. [server/pipeline/bot.py:~2528-2541]
- **`RemoteCallEnded.data` is `Map<String, dynamic>` — not deeply immutable** — debrief / telemetry consumers can mutate the map and corrupt analytics. Switch to `UnmodifiableMapView` or a typed payload struct when the consumer lands. [client/lib/features/call/bloc/call_event.dart:~224-228]
- **`DataChannelHandler` `bot_speaking_ended` envelope payload is dropped** — if the server ever wants to carry a turn index or message id, the client silently swallows it. Defensive future-proofing. [client/lib/features/call/services/data_channel_handler.dart:~292-300]
- **`_cancel_silence_timer` sets `self._silence_task = None` without awaiting** — cancelled task can still be at an `await` boundary; under tight contention with `_schedule_hang_up`, the cancelled ladder coroutine could still emit a stale stage 4 log after `_hang_up_in_progress=True`. Use `await asyncio.gather(prior, return_exceptions=True)` in the close path. [server/pipeline/patience_tracker.py:~2245-2249]
- **`_silence_task` re-creation race: `_cancel_silence_timer()` synchronously then `create_task(...)` immediately** — both tasks briefly alive; cancelled task may still call `_schedule_hang_up` before observing cancellation. Tests cover the happy path; race is theoretical. [server/pipeline/patience_tracker.py:~2235-2238]
- **`resolve_patience_config` accepts any override shape without validation** — `escalation_thresholds: "75,50,25,0"` (string) or `[]` (empty) silently corrupt config. Story 6.4 doesn't consume these fields; Story 6.6 will. Validate at next consumer landing. [server/pipeline/scenarios.py:~1606-1663]
- **`_PROMPT_PLAYBACK_TIMEOUT_SECONDS = 10.0` + 10 s `_remoteEndDrainTimer` + 8 s `_HANG_UP_CLIENT_DRAIN_TIMEOUT_SECONDS` have no unit tests exercising the timeout firing** — defensive code; only real-network regressions would surface a silent break. Mirror of dev OQ3. Add 3 timeout tests in a hardening pass. [server/pipeline/patience_tracker.py + client/lib/features/call/bloc/call_bloc.dart]
- **TTS-down during hang-up sequence has no observability** — if TTS service is unreachable, `BotStoppedSpeakingFrame` never fires, the 6 s `_speaking_done` timeout swallows the error and `call_end` fires regardless. No telemetry / log to differentiate "TTS failed" vs "TTS succeeded". [server/pipeline/patience_tracker.py:~481-495]
- **`CallBloc.close()` queue-drain race + `Future.delayed(_playbackDrainBuffer)` not cancellation-aware** — if the bloc is closed during the 500 ms drain wait, the delayed future still calls `_room.disconnect()`. Minor leak risk (room likely already disconnected). Mirror of dev OQ7. Use `Completer` + cancel pattern. [client/lib/features/call/bloc/call_bloc.dart:~270-292]
- **`handle_playback_idle` thread-safety contract is undocumented** — pipecat/LiveKit transports may dispatch `on_data_received` from a non-event-loop thread depending on version. `asyncio.create_task` from non-loop thread raises `RuntimeError`. Add a `call_soon_threadsafe` guard or document the threading invariant. [server/pipeline/bot.py:~1538-1539]
- **Stage 2 ladder anchor `_LADDER_IMPATIENCE_AT = 3.0` is hardcoded; `silence_prompt_seconds=3.0` on the hard preset collapses `stage2 = max(0, 0)` to zero** — stage 1 impatience and stage 2 verbal prompt fire back-to-back with no gap. Smoke gate only exercised easy. Per-difficulty timing scaling is dev DW1. [server/pipeline/patience_tracker.py:~2284]
- **Smoke gate only validated easy difficulty (The Waiter, 3 scenarios)** — medium / hard ladder timing not exercised on real device. Add a smoke pass per difficulty preset before MVP launch. [_bmad-output/implementation-artifacts/6-4-...md AC9]
- **`_DIFFICULTY_PRESETS` rows are mutable dicts with shared `escalation_thresholds` list reference** — `resolve_patience_config` does a shallow `dict(preset)` so two callers mutating the list corrupt the preset row. Use `copy.deepcopy(preset)` when 6.6 starts consuming the field. [server/pipeline/scenarios.py:~1563-1591]
- **`_self_speaking` orphaned-`playback_idle` race**: if the user speaks DURING the bot's silence-prompt playback, `_cancel_silence_timer` resets `_self_speaking=False`. The still-in-flight prompt's `playback_idle` arrives at the server and starts a fresh ladder — impatience fires 3 s later even though the user already responded. Subtle architectural race; dev DW5 acknowledges. Mitigation: server-side turn-id on `playback_idle` envelope, or skip-once flag on cancel. [server/pipeline/patience_tracker.py:~250-330]

## Deferred from: code review of story 6-5-build-voluntary-call-end-and-no-network-screen (2026-05-13)

- **Auth 401 silent loop extended into POST /calls/{id}/end fire-and-forget path** — `_endCallSilently` at `client/lib/features/call/bloc/call_bloc.dart:342-355` swallows `AUTH_UNAUTHORIZED` to `dev.log` with no session-expiry dispatch. Same cross-cutting gap as the MUST-FIX-BEFORE-MVP entry at top of this file — Story 6.5 adds another endpoint that participates in the silent-loop pattern. The fix is still the same Dio interceptor; no per-feature work needed here once the interceptor lands. Surface mention: don't forget to audit `/calls/{id}/end` POST after the interceptor ships (should require zero changes if interceptor is correct).
- **Janitor 1h horizon and 15min cadence are hard-coded module constants with no `Settings`/env-var override** — `server/api/app.py:1245-1246` and `server/db/janitor.py:22` (`ABANDONED_AFTER = timedelta(hours=1)`, `JANITOR_INTERVAL_SECONDS = 15 * 60`). Tuning requires a deploy. Acceptable for MVP single-VPS scale; lift to `Settings` when ops-tooling matures (post-MVP).
- **`dev.log` is a no-op in release builds → zero field telemetry on `/calls/{id}/end` failures** — `client/lib/features/call/bloc/call_bloc.dart:339-343`. The janitor's 1h sweep backstops cap-counter integrity, but the dev team has no signal on how often /end fails in production (rate, geography, error types). Wire to Sentry/Crashlytics when launch-readiness telemetry lands (Epic 10).
- **Double-tap on "Try again" CTA can pop two routes** — `client/lib/core/widgets/empathetic_error_screen.dart` `onRetry: Navigator.maybePop` has no debounce. Pre-existing in the widget since Story 5.5 (`scenario_list_screen.dart` extraction); Story 6.5 surfaced it because `NoNetworkScreen` now reuses the same widget. Not a 6.5 regression. Fix during a widget-hardening pass (debounce flag in `EmpatheticErrorScreen` state).

## Deferred from: code review of story 6-5 Déviation #28 follow-up (2026-05-15)

- **Pipeline integration test for `FrameProcessor` direction handling** — Déviation #28 was a Story 6.4 silent regression (PatienceTracker checked `BotStoppedSpeakingFrame` with `FrameDirection.DOWNSTREAM`, but pipecat 0.0.108 emits BSF UPSTREAM from `BaseOutputTransport`; the silence ladder was inert in prod for 2 days). The fix landed + a **contract test** that cross-references pipecat source vs. our source for direction parity. The contract test is fragile (source-text matching) and only covers the one specific frame (`BotStoppedSpeakingFrame`). A more robust safety net would be a **pipeline integration test** that wires `PatienceTracker` (and any other direction-sensitive `FrameProcessor`) into a minimal real pipeline with a concrete pipecat output transport (or a faithful mock), then drives trigger frames (e.g. `TTSStoppedFrame`) through and verifies the chain. The transport decides the direction; the test observes the outcome. Cost: ~½ day for clean setup (pipecat's `BaseOutputTransport` is abstract and needs `TransportParams` + sample_rate + lifecycle wiring — non-trivial mock). Pay-off: catches direction drift for ALL frames, not just BSF, and survives pipecat upgrades that change routing. Triggered: when a future Story adds another direction-sensitive `FrameProcessor` (e.g. Story 6.6 CheckpointManager), bundle this work in. Documented in `server/CLAUDE.md` §"Pipeline integration test" — point readers there.


## Post-MVP voice-pipeline UX tuning pass (planned by Walid, 2026-05-15)

> **Context.** Walid plans a dedicated UX-polish iteration AFTER the MVP ships (Epic 10) to tune the voice pipeline end-to-end. The items below are surfaced observations from Story 6.6 smoke testing — they are NOT 6.6 regressions, they're pre-existing quirks the new checkpoint system made more visible. Group them when the tuning pass starts so the work lands in one focused PR rather than scattered patches.

- **Bot response latency feels too long.** Walid's perceived UX during The Waiter smoke test: the gap between user finishing speaking and Tina responding is longer than ideal. Suspect contributors: Soniox STT finalization timeout (`SpeechTimeoutUserTurnStopStrategy(user_speech_timeout=1.8)` in `bot.py`), OpenRouter LLM TTFB (Qwen3.5 Flash, cold path), Cartesia TTS-WS connect/first-audio. Profile end-to-end on real cellular before tweaking — the cumulative budget is ~3-4 s and each segment has independent knobs. Acceptance criteria for the tuning pass: define a perceived-response-time target (Walid's call) and instrument time-to-first-audio measurement before optimizing.

- **Soniox STT transcription glitches on common food/restaurant vocabulary.** Story 6.6 Test 1 transcript captured "order" → "hold" (tour 1) and "grilled" → "grid" (tour 2). The classifier handled both gracefully (UNMET on the first, lenient pass on the second because "chicken" still made it through), but the visible UX is "I have to repeat myself sometimes". Options: (a) try Soniox v4 alternative model presets / vocabulary boost, (b) evaluate competing STT providers (Deepgram, AssemblyAI, Whisper-realtime), (c) accept it as a B1-learner-acceptable imperfection and rely on the classifier's leniency to absorb. Decision belongs to Walid post-MVP.

- **LLM (Qwen3.5 Flash via OpenRouter) drifts on prompt details.** Story 6.6 Test 1 caught Tina saying "we don't have fried" at tour 4 even though the YAML base_prompt explicitly lists `grilled chicken, fried chicken` in the menu. The LLM lost track of its own menu mid-conversation. Possible mitigations: (a) lower temperature (currently `0.7` in `bot.py` LLM settings), (b) re-anchor the menu at every prompt swap (CheckpointManager could inject the menu into each `prompt_segment` swap), (c) move to a stronger model for higher-fidelity prompt adherence (Qwen3-Max, Claude Haiku 4.5). Cost/quality tradeoff that needs Walid's voice once MVP is in users' hands.

- ~~**The 12 s wall-clock between LLM-response audio and the completion exit line.**~~ **RESOLVED 2026-05-18** in Story 6.6 post-deploy as Deviation #7 — Walid escalated after Test 4A surfaced the same gap on the hangup path. Fix: CheckpointManager now runs the classifier SYNCHRONOUSLY on terminal turns (meter about to zero OR last checkpoint) before forwarding the user frame; on confirmed-terminal verdicts it SUPPRESSES the frame so the LLM never produces a parallel response. Covers both hangup and completion paths. 5 new tests in `test_checkpoint_manager.py`. Same commit as Story 6.6.

- **Per-difficulty smoke gate coverage incomplete.** Story 6.6 was smoke-tested on easy (The Waiter) only. Medium (Girlfriend, Mugger) and Hard (Cop, Landlord) scenarios have NOT been driven end-to-end with the new classifier. Schedule a multi-difficulty smoke pass in the tuning iteration — different `success_criteria` phrasing across scenarios may surface classifier prompt-template issues that easy-tier doesn't.

- ~~**Meter-at-zero should optionally trigger character_hung_up.**~~ **RESOLVED 2026-05-18** in Story 6.6 post-deploy as Deviation #6: Walid chose option B (warning at meter ≤ 25 + hangup at 0) and asked for immediate implementation as part of the game's "trash" concept. Patch landed in `patience_tracker.py::apply_exchange_outcome` + new `_PATIENCE_WARNING_THRESHOLD` constant + new `patience_warning_line` constructor kwarg threaded through YAML `exit_lines.patience_warning`. 6 new tests cover the behavior. Same commit as Story 6.6.

## Deferred from: code review of story-6.6 (2026-05-18)

- **TTSSpeakFrame direction routing through LLM service unverified.** PatienceTracker (and now Story 6.6 warning emission) pushes `TTSSpeakFrame` DOWNSTREAM, expecting it to traverse `OpenAILLMService.process_frame` unchanged. No source-text contract test guards this routing. A pipecat upgrade that defers/drops downstream frames during in-flight inference would silently swallow warnings + exit lines. Edge Case Hunter, severity MED.
- **3 YAML re-parses per call init in scenarios.py.** `load_scenario_metadata` + `load_scenario_checkpoints` + `load_scenario_base_prompt` each re-read & re-parse the YAML on every call boot. ~10-30ms cumulative cold-cache cost. Cache the parsed dict once. Blind Hunter, severity LOW.
- **No alerting on sustained 429 from OpenRouter.** `_classify` logs a warning on non-2xx and returns None → conservative-fallback → every exchange is "failed" → meter drains in 4-5 turns → unjust character_hung_up. Needs an SRE-level signal (counter, log-level escalation, or circuit breaker). Blind Hunter, severity LOW.
- **`httpx.AsyncClient` constructed per-call inside `_classify` (no connection pooling).** Adds TLS handshake on every classification. Pre-existing pattern in EmotionEmitter — fix both together. Blind Hunter, severity LOW.
- **`_make_manager` brittle classify double-reassignment** (test refactor). The helper assigns `classifier.classify = _stub_classify`, then captures `real_classify = classifier.classify` (now the stub), then re-assigns to `_tracked_classify`. One accidental reorder during maintenance and the tracked-stub calls itself recursively. Blind Hunter, severity LOW.
- **`test_apply_exchange_outcome_False_floored_at_zero` couples meter-floor test to hangup flow.** Test asserts `tracker._patience == 0` but is forced to drive `BotStoppedSpeakingFrame` + drain to avoid leak warnings. Future refactor of either independently risks breaking both tests. Blind Hunter, severity LOW.
- **`checkpoint_advanced` envelope has no schema version field.** Story 6.7 will add a consumer; adding fields later means client can't tell old/new envelopes apart. Add `v: 1` now or document the design decision. Blind Hunter, severity LOW.
- **Pipeline-drive contract test (`test_checkpoint_manager_observes_finalized_TranscriptionFrame_via_real_pipeline_drive`) missing `UserTurnStrategies`.** The real `bot.py` wires VAD-based turn-end detection; the test uses the bare `LLMContextAggregatorPair(context)`. A future pipecat that hardens the aggregator to require turn-strategy gating would break prod but pass this test — same false-positive class as the Déviation #28 trap. Edge Case Hunter + Acceptance Auditor, severity MED.
- **`patience_warning_line` default is waiter-flavored (`"*sighs* Look, are you ordering or not?..."`).** A Mugger/Cop/GirlfriendKid scenario without YAML override would get this line — out of character. Make the default neutral or raise on missing YAML override. Blind Hunter, severity LOW.
- **`test_preemptive_completion_suppresses_user_frame_on_last_checkpoint` pre-sets `manager._index = 1`** bypassing the organic-advance path. Off-by-one in the `_index + 1 >= len()` predicate could pass tests that pre-set the state. Drive `_index` advance through real classifier verdicts instead. Edge Case Hunter, severity LOW.
- **`OutputTransportMessageFrame` direction hardcoded DOWNSTREAM in `_classify_and_advance`.** Other frames in this processor are direction-passthrough; the envelope is direction-explicit. If future routing changes filter envelopes by direction, the envelope disappears. Blind Hunter, severity LOW.
- **`_last_character_line` skips non-text multi-part messages silently.** `LLMContext._messages` supports `LLMSpecificMessage` (dataclass, not dict) and `content: list[parts]` where only `type=="text"` parts are read. A future multi-part assistant turn (image/audio) makes classifier see empty `last_character_line` with no log. Blind Hunter + Edge Case Hunter, severity MED.
- **AC3 #2 docstring in story-spec says "Reaching zero does NOT trigger hang-up here"** — Dev#6 inverted that. The actual code docstring on `apply_exchange_outcome` was updated; story-spec was NOT. Add an inline `<!-- 2026-05-18: amended by Dev#6 -->` comment in the spec for Story 6.7 dev. Acceptance Auditor, severity LOW.
- **`test_preemptive_hangup_suppresses_user_frame_when_meter_will_zero` relies on MagicMock truthy default `is_hanging_up`.** Comment in `_make_manager` exploits "Mock-by-default attribute access returns truthy, so `not pt._hang_up_in_progress` evaluates False, keeping existing tests on the normal async path." A pytest-mock upgrade that changes default attribute behavior silently flips the coverage. Blind Hunter, severity MED.
- **`LLMContextAggregatorPair(context)` API drift risk in tests.** The pipeline-drive test uses the default 1-arg constructor; pipecat upgrades changing the signature would break the test for the wrong reason, masking real direction drift behind a TypeError. Blind Hunter, severity LOW.
- **`asyncio.run(_drive())` inside pytest function vs future pytest-asyncio auto-mode.** If `pytest.ini` ever enables `asyncio_mode = auto`, the bare `asyncio.run` clashes with the auto-managed loop. Other Story 6.6 tests use `_run` helper with `new_event_loop` — same vulnerability, inconsistent style. Blind Hunter, severity LOW.
- **`_classify_and_advance` cancel-mid-flight envelope partial-execution.** If `asyncio.CancelledError` arrives after `self._index += 1` + `_settings.system_instruction =` but BEFORE `await self.push_frame(envelope)`, server advanced but client never received `checkpoint_advanced` — UI stuck on stale checkpoint. Extremely narrow window; current cancel sources don't trigger here, but unspecified. Edge Case Hunter, severity MED.
- **`LLMContext.get_messages()` returns the live mutable list (no defensive copy).** Single-event-loop world is safe today; if pipecat ever moves to threaded aggregators, `_last_character_line` would iterate a list being mutated by `add_message` and raise `RuntimeError`. Edge Case Hunter, severity LOW.

## Deferred from: code review of story-6.7 (2026-05-19)

- **`deferred-work.md` lines 396 / 406 not pointer-linked from Story 6.7 Completion Notes.** Spec Task 8 explicitly opted to document deferred-work lines 396/406 as RESOLVED in Story 6.7's Completion Notes only, leaving the historical record in deferred-work.md untouched per convention. Acceptance Auditor, severity LOW. As-designed; flagged for documentation discoverability only.
- **AnimatedSwitcher cross-fade test asserts `findsOneWidget` at 100ms during 250ms transition.** `checkpoint_hint_bubble_test.dart::AnimatedSwitcher cross-fades between hint texts` pumps 100ms then asserts only the second hint is found. The semantic the test name implies (both texts coexist during transition) is not verified — assertion passes because AnimatedSwitcher's default transition swaps widget identity at the start. Blind Hunter + Edge Case Hunter, severity LOW.
- **Bubble layout test depends on Inter font being registered at test runtime.** `long hint text wraps to multiple lines` test asserts `textBox.size.height > 45` with a hard-coded screen width of 320px. Flutter widget tests use a fallback font unless a `FontLoader` registers Inter; font metrics differ → height could change. Blind Hunter, severity LOW.
- **Test-ticker leak fragility if AnimatedSwitcher duration is ever bumped past 400ms.** Current test pumps a fixed 300ms after the 100ms intermediate frame. If duration is ever increased, residual Tickers leak → pytest framework "pending timers" failure. Edge Case Hunter, severity LOW.
- **`CheckpointHintBubble` does not defensively guard against `snap.total <= 0` or `currentIndex < 0`.** Today the data-channel handler enforces these upstream (`totalInt <= 0 || idxInt < 0 || idxInt >= totalInt → drop`). The bubble only checks `hintText.isEmpty`. A future code path producing an invalid snapshot client-side would still reach the bubble. Edge Case Hunter, severity LOW.

## Deferred from: code review of story-6.9 (2026-05-21)

- **DTLN steady-state chunk-length invariant unverified.** Block-buffering may return output bytes of different length than input in steady state (DTLN holds tail samples between blocks). Today's unit tests feed 480-sample chunks of silence which exercises only the startup-passthrough branch. Empirical smoke-gate validation in noisy café was successful (no Pipecat downstream chunk-shape errors observed) so deferred to a future "formal contract test" pass. Risk surfaces if Pipecat ever adds a strict chunk-shape assertion downstream of `audio_in_filter`. File: `server/pipeline/dtln_audio_filter.py:153-155`. Severity LOW.
- **`monkeypatch` of `DTLNNoiseSuppressor` is fragile to future refactor.** Today's `test_init_failure_disables_filter_passthrough` works because `start()` does a function-local `from livekit.plugins.dtln import DTLNNoiseSuppressor`. Moving the import to module level (a routine refactor) would silently regress the test: monkeypatch hits the module attribute but the function uses the already-bound local. Future-proof via factory injection or `monkeypatch.setattr` on the lookup path. File: `server/tests/test_dtln_audio_filter.py:87-103`. Severity LOW.
- **Hardcoded `qwen/qwen3.5-flash-02-23` model ID without env override or startup ping.** Pre-existing from Story 6.6 (not introduced by 6.9). If OpenRouter deprecates the snapshot date suffix, every classify becomes a 404 → `verdict=None` → with the new patience-neutral semantics (Deviation #5), user gets soft-locked indefinitely (compounds with the decision-needed M1 stuck-loop concern). Move model ID to `Settings`; add a startup health check that pings the model. File: `server/pipeline/exchange_classifier.py:65`. Severity LOW.
- **Re-calibrate the 5 launch scenarios after `EXCHANGE_CLASSIFIER_PROMPT` rewrite (D4 / Story 6.9 Deviation #10) + Groq Llama 3.3 70B provider swap (Story 6.9b).** The prompt rewrite added 6 GUIDING PRINCIPLES including "Default to MET when uncertain", which weakens the precision side of the classifier. The provider swap to Groq Llama 3.3 70B also changes the classifier's behaviour profile (Llama 70B is a stronger instruction-follower than Qwen Flash — could apply Principle 5 differently). The 2026-05-22 75-sample corpus bench shows Groq with 0 false positives (vs Qwen's 3) which is reassuring but does NOT replace 5-scenario calibration on Pixel 9 Pro XL. Mugger/Cop (target 15-35% survive) are probably too easy now; Waiter (target 60-90%) may overshoot. Re-run Pass A + Pass B on Pixel 9 Pro XL for all 5 scenarios **post Story 6.9b** (re-tagged from "post 6.9b/6.12" — Story 6.12 now blocks on this re-calibration so it can size its sync-verdict architecture against the real survival distribution under the new classifier). Triggered by Story 6.9 review 2026-05-21, re-tagged by Story 6.9b review 2026-05-22.

- **🟠 Delete the two `classifier_input` / `classifier_output` DEBUG logs from `exchange_classifier.py` before public launch (Story 6.10 close).** Story 6.9b shipped two `logger.debug()` calls (NOT `logger.info()` as the prior version of this entry claimed — Story 6.9b review D6 / P21 corrected the doc) that emit the raw user transcription (`user_text`) + first 80 chars of `success_criteria` + first 80 chars of `last_character_line` + verdict + first 120 chars of raw classifier response on EVERY classify call. They sit at DEBUG-level intentionally so they're inert in prod unless an operator explicitly flips `LOG_LEVEL=DEBUG` on the VPS — but `user_text` is raw STT output which is PII-equivalent (real users' practice utterances), so any temporary debug session leaks practice transcripts to the journal. Suggested action when Story 6.10 closes: delete both lines outright (the goal-based-dialogue debug pass can re-add them locally if needed, but they shouldn't ship). File: `server/pipeline/exchange_classifier.py` `_classify` method (the `classifier_input` and `classifier_output` `logger.debug` calls). Severity LOW post-D6 fix (DEBUG-only means no prod logging today; the residual risk is the operator-toggle, not steady-state).

## Deferred from: code review of story-6.9b (2026-05-22)

- **Story 6.9 D2 timeouts (`_CLASSIFIER_TIMEOUT_SECONDS=2.0` / `_HTTP_TIMEOUT_SECONDS=1.5`) not tightened back after Groq migration.** Groq's measured 320 ms p95 from VPS makes the 2.0 s outer / 1.5 s HTTP budget effectively unused; could tighten to 0.8 / 0.6 (still ~2.5× margin) so degraded-Groq surfaces faster. File: `server/pipeline/exchange_classifier.py:84-85`. Severity LOW. Ride with Story 6.10 if it surfaces in real traffic.
- **`memory/project_story_6_9b_classifier_latency_slash.md` §1 cost matrix not updated with real benched numbers.** Memory file still reads "Qwen 3.5 Flash 300-1500 ms TTFT" + "Groq Llama 3.1 8B" as leading candidate. Real numbers (Qwen 587 ms p50, Groq 70B 121 ms p50, 8B aborted on 12% accuracy) never landed. Future devs reading this before drafting Story 6.10/6.12 will see falsified data. Declared Walid Phase B task (spec Task 2.3) but UPGRADE from optional → required for `review → done`. Severity LOW.
- **`scripts/benchmark_classifier.py` imports private `_parse_classifier_output`.** Bench harness pokes `sys.path` then imports underscore-prefixed private symbol. Any rename or move silently breaks the harness, and the bench tests only exercise the public API. Recommendation: export `_parse_classifier_output` as a public utility, OR add a test that exercises `from scripts.benchmark_classifier import *` against current `exchange_classifier.py`. File: `server/scripts/benchmark_classifier.py:1126-1138`. Severity LOW.
- **`scripts/benchmark_classifier.py::main()` has no unit tests.** The argparse → asyncio.run → file-write path has zero test coverage. A future change that breaks `--providers` parsing silently breaks the operator's workflow. Recommendation: add a subprocess smoke test invoking `main()` against a tiny corpus and asserting clean exit. Severity LOW.
- **Rotate the 3 Groq / DashScope / OpenRouter keys leaked into chat history 2026-05-22.** Sprint-status entry admits the leak. No code-side mitigation in this diff. Phase B Walid task — rotate keys + provision new ones on `/opt/survive-the-talk/.env`. Severity HIGH (security) but already tracked at the sprint-status level — not a code defect. Confirmed scope: Walid Phase B (a).
- **`config.py` 30-line comment block at `extra: "ignore"` is comment-heavy vs the one-line code change.** Risk: a future "cleanup" refactor removes the explanation and accidentally flips `forbid` back. Recommendation: condense to 3 lines + link to a separate ADR if the rationale is load-bearing. File: `server/config.py:156-182`. Severity NIT.
- **Bench `_HTTP_TIMEOUT=10` vs prod 1.5 s gap skews comparison if a candidate has tail latency 2-4 s.** A provider that p95s at 4 s passes the bench (correct verdicts) but fails in prod (`asyncio.TimeoutError → verdict=None`). The Phase A.5 bench did not surface this because Groq 70B p95 was 320 ms, well under both. Recommendation: in `_recommend`, additionally require `p95_total_ms < prod_classifier_timeout * 1000` and warn if the winner's p95 exceeds prod ceiling. File: `server/scripts/benchmark_classifier.py:1587, 1849-1852`. Severity MEDIUM.
- **Bench runs providers serially with no `--rate-limit-rps` flag.** For 75-sample corpus across 5 providers each at 500 ms p50, that's ~3 min minimum. More importantly: serial requests against Groq's 6000 TPM cap can pile up 429 retries that inflate `total_ms` percentile reporting. Recommendation: add `--rate-limit-rps` flag obeyed via `asyncio.sleep` BEFORE the call. File: `server/scripts/benchmark_classifier.py:1804-1821`. Severity LOW.
- **Bench cost computation silently zero on missing `usage` block.** `_parse_response` falls back to `usage = raw_body.get("usage") or {}`; if either token count is missing, cost is `None` and summary averages over only-non-None values — a provider with intermittent usage reporting shows falsely low average cost. Recommendation: when either count is missing, log a warning AND fall back to a length-of-prompt estimate so cost stays comparable. File: `server/scripts/benchmark_classifier.py:1416-1432, 1557-1564`. Severity LOW.
- **`verdict_accuracy_pct = correct / (samples_total - infra_failure)` not surfaced separately from `accuracy_pct`.** `accuracy_pct = correct / len(results)` divides by ALL results including infra failures, conflating "wrong answer" with "no answer". A provider with 50% accuracy AND 50% infra failures shows ~25% accuracy_pct — Deviation #1 gate compares this and the provider looks much worse than its verdict-accuracy deserves. Recommendation: compute both metrics. File: `server/scripts/benchmark_classifier.py:1602-1612`. Severity LOW.
- **`_kwargs()` test helper signature not verifiable from diff.** New 6.9b tests call `_run(_make_classifier().classify(**_kwargs()))`. The helper is pre-existing in the test file but not shown in this diff. If `_kwargs()` doesn't include `scenario_description`, tests would TypeError. Recommendation: cross-check `_kwargs()` against the prod `classify` signature. File: `server/tests/test_exchange_classifier.py:755, 784`. Severity NIT.
- **Split `.env.example` into prod + bench templates.** The diff adds `DASHSCOPE_API_KEY` + `CEREBRAS_API_KEY` + `ANTHROPIC_API_KEY` to the file as benchmark-only slots; mixing prod and bench env vars in one template invites confusion. Recommendation: `.env.example` for prod + `.env.bench.example` for dev. File: `deploy/.env.example:62-66`. Severity NIT.
- **`_FENCE_RE` strips only ONE fence pair.** Model that emits nested fences (` ```\n```json\n{...}\n``` `) silently fails the first match and falls through to the brace-find. Recommendation: idempotent-loop strip, OR simpler "first `{` to matching `}`" scan. File: `server/pipeline/exchange_classifier.py:88-91, 349-352`. Severity LOW.
- **Bench `_percentile` minimum N.** `statistics.quantiles(values, n=100)` interpolates over only two data points at N=2 — p95 ≈ max, misleading. Test corpus has 75 samples (acceptable) but `--providers <single>` with a tiny custom corpus could mislead the recommendation. Recommendation: require `samples_total >= 20` (or doc the minimum). File: `server/scripts/benchmark_classifier.py:1624-1635`. Severity LOW.
- **`_HTTP_TIMEOUT_SECONDS` is cached in the persistent client — patching at module level after instantiation has no effect.** Test that patches the constant then instantiates `ExchangeClassifier` works; a test that instantiates first then patches doesn't. Recommendation: read `_HTTP_TIMEOUT_SECONDS` at every `_get_client()` call and rebuild the client if changed, OR add a comment noting the cached binding. File: `server/pipeline/exchange_classifier.py:174`. Severity LOW.
- **`bot.py` reads `settings.groq_api_key` once at pipeline build; key rotation requires `systemctl restart` not `reload`.** `systemctl reload pipecat` does NOT reread the Settings singleton — cached key wins. Every classify gets 401 → `verdict=None` → consecutive-None backstop fires at N=5 → user soft-hangup. Recommendation: document the requirement clearly in `server/CLAUDE.md` ops section, OR wire SIGHUP to clear the Settings cache. File: `server/pipeline/bot.py:332-335`. Severity LOW (operational doc, not code).
- **Bench TTFT measured on first non-empty chunk; SSE keepalive comments not skipped.** Today's bench payload does NOT use streaming so TTFT == total — moot. But Phase B streaming work would surface this: streamed Groq responses start with `data: \n\n` SSE keepalive, first byte is whitespace, TTFT measurement reflects keepalive arrival not first token. Recommendation: short-circuit `if not body.get("stream"): ttft_ms = total_ms`. File: `server/scripts/benchmark_classifier.py:495-497`. Severity LOW (only matters with streaming).
- **`groq_api_key` not declared as `SecretStr`.** Pydantic v2 Settings logs the model repr on validation failures — a typo'd key triggering an unrelated validation error would include the model in the traceback, leaking the real Groq key. Recommendation: declare credential fields as `SecretStr`. File: `server/config.py:36`. Severity LOW.
- **`api_key` kwarg rename has no backward-compat shim.** Six call sites bulk-renamed from `openrouter_api_key=` → `api_key=`. No deprecation wrapper accepts both. No known out-of-tree callers — intentional rename — but worth noting if any dev script falls outside the repo. Severity NIT.
- **`_BACKOFF_S = 5.0` × 3 retries × 30 s Retry-After = 90 s bench delay possible.** Recommendation: cap `wait_s` at `_MAX_BACKOFF_S = 10` and bail to error after the cap is exceeded. File: `server/scripts/benchmark_classifier.py:1448-1449`. Severity NIT.
- **Test `proceed_handler.set()` is unawaited dead code in `test_close_during_in_flight_classify_does_not_leak`.** The arrived_at_handler/proceed_handler Events are created, `proceed_handler.set()` is called, but neither `.wait()` is ever invoked. The MockTransport handler runs synchronously so the "race" is purely scheduler-dependent. Test passes today but doesn't actually exercise the close-during-in-flight race. Recommendation: delete the unused events OR make the MockTransport handler actually block on `arrived_at_handler.set(); await proceed_handler.wait()`. File: `server/tests/test_exchange_classifier.py:483-512`. Severity LOW.
- **`Settings(...)` ignores `GROQ_API_KEY ` (trailing whitespace) — Pydantic doesn't strip env-var names.** Operator pastes `GROQ_API_KEY =gsk_xyz` (space before `=`) into `/opt/survive-the-talk/.env`. The key `GROQ_API_KEY ` (trailing space) is treated as an extra env var, ignored, and `Settings.groq_api_key` raises ValidationError "Field required". The test catches typos in DECLARED field names, not whitespace in ENV names. Recommendation: document the whitespace gotcha in the comment block; OR add a `model_validator` that scans `os.environ` for keys matching `^GROQ_API_KEY\s+$` and raises with a helpful message. File: `server/config.py:77`. Severity LOW.

## Deferred from: Story 6.9b Pixel 9 smoke gate (2026-05-26)

- **🟠 `_LADDER_IMPATIENCE_AT = 3.0` is hardcoded and too aggressive UX-wise.** Story 6.9b call_id=148 smoke gate 2026-05-26: stage 1 visual `impatience@0.5` Rive enum fired twice at exactly 3.0s after `playback_idle` (T6 and T7), even though Walid was actively formulating his response. He felt the system was "calling him out" too early. The 3.0s window covers: (a) brain parses Tina's last sentence (~0.5-1s), (b) formulates answer (~0.5-1s), (c) starts articulating — total natural response time is 1.5-2.5s **after** perception, so the threshold should be 4-5s to feel respectful. The verbal prompt (stage 2) at `silence_prompt_seconds=6.0` is fine — it's the visual impatience cue that's premature. Two paths to fix: (1) raise the constant to 4.5s (one-line change in `patience_tracker.py:127`), (2) make `_LADDER_IMPATIENCE_AT` configurable per-difficulty in `_DIFFICULTY_PRESETS` (easy=4.5s, medium=3.5s, hard=2.5s for the harder scenarios where the character is *supposed* to be impatient). Option 2 is more honest to the difficulty semantics. File: `server/pipeline/patience_tracker.py:127`. Severity MEDIUM (UX friction — bundles cleanly into the post-MVP voice-pipeline UX tuning pass per `memory/project_post_mvp_voice_ux_tuning.md`).
- **🟠 Silence ladder ticks against the user WHILE the bot is speaking — fires "Hello? Are you still there?" prompts when the user is actively listening, not silent.** Story 6.9b call_id=150 T5→T6 smoke gate 2026-05-26: ladder started at 07:50:39.498 (client `playback_idle` for prior turn), but at 07:50:39.690-816 Cartesia generated + emitted Tina's response to Walid's "Water" ("Water, okay. That's all you need?" — audio played 39.818→42.658). Bot speaking for ~2.8s during which the ladder kept counting. Stage 1 fired at 42.500 (+3.0s from ladder start) while Tina was mid-sentence; stage 2 fired at 45.501 (+6.0s) — pushed verbal prompt "Hello? Are you still there?" cutting Tina off. Walid's "Yeah." arrived at 45.992 — he WAS responding normally, the ladder just didn't pause when the bot started speaking again. The cancel logic in `patience_tracker.py:494-497` handles user speech ("Defensive: VAD start may land before STT finalizes — cancel the ladder") but has NO equivalent cancel-on-`BotStartedSpeakingFrame`. Two clean fixes: (a) cancel ladder on `BotStartedSpeakingFrame` and re-arm on next `playback_idle`, (b) suppress `playback_idle` from arming the ladder if there's a pending LLM generation in flight (more complex — needs frame-flow signal). Walid 2026-05-26: "j'ai toujours répondu normalement et dans les temps, à un moment elle a dit 'Are you still there?' alors que pourtant j'étais en train de parler" — direct user-facing UX regression. File: `server/pipeline/patience_tracker.py:499-560` (handle_playback_idle + cancel paths). Severity HIGH (call-quality bug, easy to reproduce when Tina has multi-frame responses). Bundles with the `_LADDER_IMPATIENCE_AT=3.0` over-aggressive timer finding.
- **🚨🚨 BLOCKER MVP-LAUNCH — Cartesia TTS silently stalls mid-generation, pipeline hangs indefinitely until user hang-up. REPRODUCED 2× across different scenarios.** Story 6.9b Pixel 9 smoke gate 2026-05-26: **call_id=149** (café-noisy attempt) froze on T1 response after Cartesia logged `Generating TTS [Okay.]` + `Generating TTS [What would you like to eat or drink?]` at 07:39:13.271-306. **call_id=151** (off-topic Mode A) froze on T6 response after Cartesia logged `Generating TTS [Okay.]` + `[One pasta and water.]` + `[I'll get that to you.]` at 08:17:35.586-719. **Identical signature in both cases:** Cartesia logs the multi-frame TTS generation request, but the `CartesiaTTSService#0 cleaning up TTS context <uuid>` DEBUG line that normally fires ~1.2s later **NEVER appears**. No `tts_first_audio` LatencyProbe fires, no `TTSStoppedFrame`, no `bot_speaking_ended`, no `playback_idle` from client, no WARNING, no Traceback, no WebSocket disconnect. Pipeline hangs in a soft-lock state until the user manually disconnects (~13-15s window observed). Walid confirms ground-truth: Tina never produces audio on the Pixel during this window. **Repro rate:** 2/3 smoke-gate calls hit this on multi-frame Tina responses. **Probable causes** (need investigation): (a) Cartesia upstream WebSocket buffer / keepalive bug under rapid multi-frame `run_tts` calls — request accepted but generation never completes server-side, (b) Pipecat 0.0.108 CartesiaTTSService race in `run_tts` when ≥3 text frames queue within ~150ms, (c) LiveKit transport-side buffer issue downstream of Cartesia output. **No mitigation currently exists** — no timeout on Cartesia generation, no watchdog, no fallback. File: `server/pipeline/bot.py` (Cartesia wiring), `pipecat.services.cartesia.tts:run_tts` (upstream). Severity 🚨 BLOCKER — every 30-40% of calls become unusable. **This must be fixed before MVP launch.** Bundles into the proposed Epic 6 hardening story. Investigation priorities: (1) add a `Generating TTS` → `cleaning up TTS context` wall-clock timeout (e.g. 5s) that logs WARN + emits a synthetic `TTSStoppedFrame` so the pipeline unblocks, (2) add a TTS health probe that pings Cartesia WebSocket every 10s, (3) capture full Cartesia WebSocket trace (raw frames in + out) during a known-repro scenario, (4) check pipecat upstream issue tracker for known Cartesia multi-frame bugs.
- **🟡 Waiter `clarify` checkpoint fired False on legitimate user input ("Uh, cola." in response to "Water, juice, cola, or coffee?").** Story 6.9b call_id=148 T3 smoke gate 2026-05-26: classifier verdict=False on user saying "cola" because the `clarify.success_criteria` requires a CONFIRMATION ("yes/that's right/correct") not a new drink choice, and Tina had verbally accepted the order ("Cola, got it.") in her previous line. The misalignment is: Tina's prompt asks for a NEW choice while the checkpoint criteria expects CONFIRMATION of the existing order. Two paths: (1) widen `clarify.success_criteria` to accept "any answer to the most recent question Tina asked" (loose), (2) tighten Tina's prompt so she only asks confirmation questions at the `clarify` stage (strict). The 5-scenario re-calibration task (line 451 above) already covers this — but flag specifically that Waiter `clarify` needs the prompt vs criteria alignment audit. File: `server/pipeline/scenarios/the-waiter.yaml` `clarify` block. Severity LOW (recoverable — Walid recovered with "Yeah." next turn, patience +5).

## Deferred from: code review of story-6.13 (2026-05-27)

Context: AC1's Cartesia watchdog is a **safety-net only** since `TTS_PROVIDER=elevenlabs` is the deployed default — the watchdog/Cartesia items below rarely fire in prod, hence deferred.

- **🟠 TTSWatchdog emits only `TTSStoppedFrame`, never `BotStoppedSpeakingFrame`, on a stall.** Downstream consumers keyed on BSSF rather than the TTS Stop are not reset on a stalled turn — e.g. `LatencyProbe._fired_this_turn` only resets on `BotStoppedSpeakingFrame`, so the probe stays armed across the stalled turn, and any output-transport bot-speaking flag may stay half-open until the next turn. File: `server/pipeline/tts_watchdog.py:210-219`. Low prod impact (ElevenLabs default; LatencyProbe is opt-in). (edge-case review)
- **🟠 Late real TTS frames after the watchdog fires are not reconciled.** The Cartesia stall sometimes returns late (`type=error` ~30s later); after the synthetic Stop fires at 5s, the real `OutputAudioRawFrame`/`TTSStoppedFrame` for the same context arrives later → orphan audio plays past a turn the aggregator already finished + a duplicate Stop for an already-closed context. The watchdog only guards against double-*synthetic*-emit. File: `server/pipeline/tts_watchdog.py:169-180`. Low prod impact (ElevenLabs default). (edge-case review)
- **🟠 Watchdog synthetic-push path is untested (`pragma: no cover`) and all tests stub `push_frame`.** The real `FrameProcessor.push_frame` + `TTSStoppedFrame(context_id=None)` construction is never exercised; if it ever raises, the pipeline stays soft-locked — the exact failure the watchdog exists to prevent — and ships green. Add a real-pipeline integration test driving a synthetic Stop end-to-end. File: `server/pipeline/tts_watchdog.py:215-223` + `server/tests/test_tts_watchdog.py`. Hardening. (adversarial review)
- **🟡 AC1 idempotency wording vs code.** AC1 says a re-emitted `TTSStartedFrame` must NOT re-trigger the timeout, but the code re-arms a fresh 5s timer on every `TTSStartedFrame` (and `test_watchdog_fires_at_most_once_per_turn` asserts 2 stops for 2 Starts — the opposite of the AC wording). Behavior (re-arm per turn) is acceptable; reconcile the AC1 spec text. File: `server/pipeline/tts_watchdog.py:157-168`. Spec-text reconciliation. (acceptance audit)
- **🟡 Stage-2 silence-ladder wait clamps to 0 on a misconfigured override.** `stage2 = max(0, silence_prompt_seconds - ladder_impatience_seconds)`; the validator permits `ladder_impatience_seconds` up to 10.0 with no relationship check against `silence_prompt_seconds`, so an override ≥ the prompt window fires the verbal prompt immediately after the impatience face. Presets (4.5/6, 3.5/4, 2.5/3) are safe. Add cross-field validation in `resolve_patience_config`. File: `server/pipeline/patience_tracker.py:843-846` + `server/pipeline/scenarios.py`. Config-interaction, presets safe. (adversarial + edge-case review)
- **🟡 AuthInterceptor fires on ANY 401 including `/auth/*` routes.** `onError` checks only `statusCode == 401` with no path exclusion. Latent today (auth routes return 400/429/502), but a future token-refresh/auth route returning 401 would clear the just-issued token + redirect mid-login (possible loop). Add a `/auth/` path exclusion. File: `client/lib/core/api/auth_interceptor.dart:66`. Latent, not live. (edge-case review)
- **🟢 LLM warm-up `reasoning` key placement.** Warm-up payload puts `reasoning` at the top level, unlike the real LLM call's `extra_body.reasoning`; OpenRouter may ignore or reject it, so the warm-up may exercise a different variant than the real request. Pure optimization. File: `server/pipeline/llm_warmup.py:1496-1503`. (adversarial review)
- **🟢 FreshContext `_build_msg` forwards a hardcoded `add_timestamps` default.** May diverge from the pipecat parent default if re-enabled. Inert in prod (debug-gated `CARTESIA_FRESH_CTX=1`, off by default). File: `server/pipeline/cartesia_instrumented.py`. Debug-only. (adversarial review)

## Deferred from: Story 6.10 post-implementation hardening (2026-05-29)

- **🟡 Re-benchmark + prompt-re-tune the checkpoint judge on Llama 4 Scout.** The 2026-05-29 structured-output fix (commit `cda999c`) switched the judge from Llama 3.3 70B to `meta-llama/llama-4-scout-17b-16e-instruct` (70B can't do `response_format=json_schema`; see `server/CLAUDE.md` §4 + memory `project_checkpoint_judge_structured_output.md`). Measured on the existing 75-sample `tests/fixtures/classifier_benchmark_corpus.json` (single-goal structured calls): **Scout 92.0% accuracy, 6 false positives, 0 false negatives** vs 70B's 98.7% / 0 FP. The error profile is the SAFE direction (over-generous; never wrongly rejects a real attempt → no user frustration) and matches principle 5's Default-to-MET, so it shipped — but it's ~6.7 pts below 70B. **Follow-up:** (1) the bench harness (`scripts/benchmark_classifier.py`) does NOT yet support structured-output providers — it uses the single-goal `EXCHANGE_CLASSIFIER_PROMPT` + `_parse_classifier_output`; add a Scout-structured provider config + a multi-goal corpus so the bench reflects the deployed `classify_multi` path. (2) Try softening principle 5's MET-bias in `EXCHANGE_CLASSIFIER_MULTI_PROMPT` for the smaller/more-lenient Scout to trim the 6 FPs, then re-measure. (3) Re-check whether a bigger structured-output model is on the Groq account (Maverick + Kimi 404'd, gpt-oss-20b 400'd on schema as of 2026-05-29). Severity LOW (current behaviour is safe-direction + the format bug it replaced was worse). Files: `server/pipeline/prompts.py` (`EXCHANGE_CLASSIFIER_MULTI_PROMPT`), `server/scripts/benchmark_classifier.py`, `server/config.py` (`classifier_model`).

## Deferred from: code review of story-6.10 (2026-05-29)

- **🟡 Call-end reconcile fabricates `metIndices=[0..pi-1]` (wrong set on out-of-order completion).** When goals completed out of order AND a final per-flip `checkpoint_advanced` envelope was lost, the `call_end` reconcile marks the first `pi` indices met instead of the real set, because `call_end` carries only the `checkpoints_passed` COUNT, not the index set. Cosmetic, fires at call end, `justFlippedIndex: null` so no animation. Clean fix = have the server include the full met-index set in the `call_end` envelope (then the client renders the true set). File: `client/lib/features/call/views/call_screen.dart:578-585` + `server/pipeline/patience_tracker.py` (`call_end` payload). Documented best-guess, low-impact. (blind + edge-case review)
- **🟢 Latent HUD/envelope ordering + animation edge cases.** (a) An URGENT flip `OutputTransportMessageUrgentFrame` can overtake the QUEUED initial-state `OutputTransportMessageFrame`, so a late `goals_met_indices=[]` could reset the HUD after a flip already showed; (b) the first-snapshot (mount/reconnect) path and a shrinking met-set both skip/miss the completion animation and can leave `_animatedMet` stale (a later legit re-flip of the same index won't re-animate). Latent today because the server emits monotonic full sets and the call screen uses one persistent widget. Fix options: make the initial envelope urgent too (or client-side ignore a strictly-shrinking met set). Files: `client/lib/features/call/views/widgets/checkpoint_step_hud.dart` (`_ingest`), `server/pipeline/checkpoint_manager.py` (`_emit_checkpoint_advanced` vs `build_initial_envelope`). Timing-dependent, low-impact. (blind + edge-case review)
- **🟡 Multi-goal completion not pre-suppressed when ≥2 goals complete in the SAME final turn.** The Deviation #7 terminal predicate is `met_count + 1 >= len(checkpoints)`, which only fires when exactly ONE goal is pending. If the last TWO pending goals validate in one phrase (e.g. "grilled chicken with a cola" → `clarify`+`drink`), the turn is judged non-terminal → the user frame is pushed → the character produces a normal reply that lands alongside the `survived` exit line (disjoint ending Deviation #7 should prevent). Matches the documented narrow Deviation #7, so it's a design gap not a coding bug. **DEFERRED per Walid (2026-05-29): observe on the Pixel 9 smoke gate (call_id=137 replay / Waiter happy path) whether it actually manifests; fix only if seen.** Fixing pre-emptively means a synchronous classify on more near-end turns = latency cost (PRD 2 s kill-criterion sensitive). File: `server/pipeline/checkpoint_manager.py:414-417`. Rare + non-fatal. (blind review)

## Deferred from: code review of story-6.18 (2026-06-04)

- **🟠 `inappropriate_content` exit-line generation embeds the offensive user turn verbatim — model may echo the abuse.** For `reason=inappropriate_content` the guidance says "Do NOT repeat or quote what they said," yet `_normalize_transcript` passes the full transcript (incl. the offending turn) into the prompt, and a 70B model at temp 0.7 may paraphrase/echo it — worse than the canned firm line. **DORMANT in prod today** (`abuse_classifier=None` per `bot.py:381-384`; no shipping code calls `_schedule_hang_up(_REASON_INAPPROPRIATE)` — abuse is handled by the main character LLM emitting a canned in-character line). **MUST be closed before any abuse classifier is wired:** either short-circuit `reason==inappropriate_content` to the canned `hang_up_line_inappropriate` (recommended) or redact/omit the offending turn for that reason; add a test feeding an abusive transcript asserting the output omits the substring. Files: `prompts.py:338-343`, `exit_line_generator.py:102-122`, `patience_tracker.py:_resolve_exit_line`. New-code latent risk, currently zero user impact. (code review of story-6.18)
- **🟢 Line-less hang-up if the hang-up task is cancelled during the ~1.5s generation window.** `_run_hang_up` runs in `_hang_up_task`; `_resolve_exit_line`/`generate_exit_line` correctly re-raise `CancelledError`, so a teardown-cancel landing in the new ~1.5s generation await aborts before the exit `TTSSpeakFrame` — no closing line on an already-tearing-down call. Mostly mitigated once generation is pre-scheduled (review patch P1, which shrinks the post-interrupt window); the residual is the pre-existing ~0.5s `_HANG_UP_PRE_TTS_DELAY` cancel window that already existed for all reasons. Narrow trigger (client disconnect precisely then). Do NOT add an `await push_frame` in a `finally` during teardown (fragile). File: `server/pipeline/patience_tracker.py:1091-1152`. Low-impact teardown race. (code review of story-6.18)
- **🟢 Fresh `httpx.AsyncClient` created per hang-up (TCP+TLS handshake inside the 1.3s budget).** `generate_exit_line._generate` opens a new `async with httpx.AsyncClient(...)` per call — correctly closed (no leak) but pays a cold server→Groq handshake (~100-200 ms) on the latency-critical hang-up. Unlike the per-turn `ExchangeClassifier` (which adopted a persistent client to amortize), this fires ~1-2× per call so amortization buys nothing — defensible to accept. If addressed, have `bot.py`'s closure own a pre-warmed client and pass it in (close in `PatienceTracker.cleanup()`). File: `server/pipeline/exit_line_generator.py:245-246`. Negligible, optional. (code review of story-6.18)
- **🟢 `_resolve_exit_line` except branch logs ERROR (`logger.exception`) vs AC2's literal "WARNING".** The real `generate_exit_line` is exhaustively fire-tolerant (every actual failure → None at WARNING), so this ERROR branch is reached only by a misbehaving injected stub or an exotic infra fault — never a real generation failure. Optional: downgrade to WARNING for literal AC2 alignment, or document the branch as an infra/stub-fault guard where louder is intentional. File: `server/pipeline/patience_tracker.py:1069-1073`. Cosmetic log-level nit. (code review of story-6.18)
- **🟡 Cop's canned fallback is still the accusatory "three versions" line — replays on generation timeout/empty transcript.** Story 6.18 fixes the common case (grounded generated line), but on a generation timeout/failure or an empty transcript (very early hang-up), `_resolve_exit_line` falls back to the YAML line, which for the cop is the call-212 "Innocent people don't need three versions" accusation — the exact incoherence the story targets, now only on the degraded path. This is the AC2-mandated, pre-6.18 fallback behavior (not a regression), but a **neutral per-reason fallback** for accusation-bearing reasons (`character_hung_up`/`inappropriate_content`/`noisy_environment`) used only when the transcript is empty would close the residual without weakening the deliberately-hard cop line on the normal path. Files: `pipeline/scenarios/cop-interrogation-01.yaml`, `patience_tracker.py` fallback selection. Edge residual of the 6.18 motivation. (code review of story-6.18)

## Deferred from: code review of story-6.20 (2026-06-05)

_Independent ultracode 6-lens retrospective re-review of the merged + smoke-gated Story 6.20. The one substantive defect (fast-re-speak double-FAIL premature hang-up + Deviation #7 race) is tracked as **Story 6.25**, not here; the 3 patches (re-entrancy docstring, AC5 multi-flip test, AC3 SET-union test) were applied 2026-06-05. These three are real-but-non-actionable-now: pre-existing classes, zero current impact, or out-of-scope pre-existing code._

- **🟢 Silent exception swallow on the await-not-cancel path.** `_serialize_then_classify`'s `await asyncio.gather(prior, return_exceptions=True)` discards a non-`CancelledError` raised during the prior turn's flip-application (`advance_goals` / `_update_system_instruction` / the `_emit_checkpoint_advanced` push loop / `schedule_completion`) with NO log — a dropped flip / envelope / completion is invisible. Real, but the SAME swallow pattern pre-exists in `_run_classifier_blocking`, `cleanup`, and `_schedule_classification`, so it is a pre-existing class, not introduced by 6.20. Fix when touched: `logger.exception` on a non-`CancelledError` gathered result across these paths. File: `server/pipeline/checkpoint_manager.py:801-803`. (edge-case)
- **🟢 AC4 stopword refactor silently widened the adjacent `lexical_overlap_pairs` heuristic.** Pulling the local 30-word `_stop` set into a shared `_SALIENT_STOPWORDS` and adding 10 imperative-glue words (you/your/tell/ask/say/give/get/let/make/want) for the new drift lint also changed the Story 6.15/6.16 `success_criteria` near-duplicate detector. Empirically ZERO impact on all 6 real scenarios (max Jaccard 0.236, ~0.36 below the 0.6 flag; zero flag changes), advisory build-time tool, defensible direction — but the drift is untested (the existing `test_lexical_overlap_flags_near_duplicates` uses byte-identical strings, Jaccard 1.0, stopword-immune). Fix: give `hint_prompt_drift_pairs` its own glue-word set (or a stopword param) so the established heuristic keeps its 30-word set, + add a glue-word-heavy pinning test. Files: `server/scripts/scenario_builder.py:367-411, 421-444`. (acceptance audit + regression)
- **🟢 AC5 client-dedup no-op asserted only in comments.** No widget test fires the same `CheckpointAdvancedPayload` twice to prove the reliable duplicate is a value-equal no-op (no double completion animation / no double count). The dedup chain (`==` / `_animatedMet`) holds today and was confirmed on call_id=229, but it is pre-existing Story 6.10 code untouched by 6.20. Low-value forward guardrail: a `call_screen` / HUD test pumping an identical snapshot twice asserting a single emission. Files: `client/lib/features/call/views/widgets/checkpoint_step_hud.dart`, `checkpoint_snapshot.dart`. (test-quality)

## Deferred from: code review of story-6.26 (2026-06-10)

_Formal 3-layer adversarial review of the merged + smoke-gated warm bot-process pool (commit `e8e847d`). Zero AC violations; 6 patch items applied separately. These four are real-but-non-actionable-now._

- **🟡 Malformed job line after a pool hit = dead room with the 200 already returned.** `_run_parked` calls `apply_parked_job(line)` uncaught (`server/pipeline/bot.py:939`): a malformed job (bad JSON / missing url-room-token) makes the parked bot die loud (traceback → journald) AFTER `initiate_call` already returned 200 with room+token — the client joins an empty room, no cold fallback possible (the connection info died with the job). By-design fails-loud: the job is built by our own `initiate_call` code, so the path needs server-side drift to trigger; the janitor sweeps the orphaned `pending` row (quota refund). Hardening idea if it ever fires: pool-side post-acquire early-death watch (re-Popen cold within ~2 s of an acquired bot exiting non-zero). (code review of story-6.26, edge-case)
- **🟢 Pool test stubs share one `STUB_JOB_FILE`.** All stub parked processes of a pool inherit the same env, so every stub writes its received job to the SAME file (`server/tests/test_bot_pool.py`). Today only one job is ever delivered per test, so it's inert; a future test delivering jobs to TWO bots of one pool would race two stubs onto one file and read whichever wrote last. Note for test authors: derive a per-job marker (e.g. write `{pid}:{job}` lines append-mode) before writing such a test. (code review of story-6.26, blind)
- **🟢 Steady-state opening-latency observability is `LATENCY_PROBE`-gated.** AC1/AC7's "measured via latency_probe" delta (`Spawned`→`tts_first_audio`) only exists when the operator sets `LATENCY_PROBE=1` + restarts (pre-existing Story 6.8 design, `server/pipeline/bot.py:625-641`); the always-on signals are the `[pooled]`/`[cold]` + `acquired parked bot pid=` lines. If the opening ever regresses silently, there is no continuous per-call first-audio metric to alert on. Candidate for the post-MVP voice-pipeline observability bundle: an always-on coarse INFO delta on the bot side. (code review of story-6.26, auditor)
- **🟢 `_spawning` decrement skippable on a shutdown-window double-cancellation.** `_ensure_full`'s `finally` re-acquires the pool lock before decrementing the reserved-slot counter (`server/pipeline/bot_pool.py:202-204`); a CancelledError delivered DURING that lock acquire would skip the decrement and permanently under-count capacity. Unreachable in practice: refill tasks are only cancelled at event-loop shutdown (pool already dead) and `stop()`'s maintainer cancel releases the lock before the finally needs it. Revisit only if refill tasks ever get cancelled mid-run (e.g. a future per-acquire timeout). (code review of story-6.26, blind)

## Deferred from: code review of 7-2-build-call-ended-overlay-transition (2026-06-10)

_Formal 3-layer adversarial review of the merged + smoke-gated Call Ended overlay (`18b1f05..2f6e447`). Zero unresolved AC violations; 4 patch items applied separately (overflow wrapper, empty-role degraded mode, seeder `end_phrases` shape validation, AC-C10 doc amendment). These three are real-but-non-actionable-now._

- **🟢 `CallEndedScreen` hold timers don't pause on app backgrounding + a11y flag sampled once.** Plain `Timer`s keep counting if the app is backgrounded mid-hold (a phone call / notification tap during the 3–10 s window): the cap/exit fires off-screen and the user returns to the debrief without having seen the overlay (coherent landing, just misses the theatrical beat). Separately, `MediaQuery.accessibleNavigation` is read once under the `_timersStarted` guard, so enabling TalkBack DURING the hold keeps the 3 s min instead of 5 s (vanishingly rare trigger). Fix sketch if it matters: `WidgetsBindingObserver` pause/resume of the deadlines + re-arm the min-hold on an a11y flip. File: `client/lib/features/call/views/call_ended_screen.dart:228-246`. Smoke-validated UX; lifecycle polish. (code review of story-7.2, blind + edge-case)
- **🟢 Element-level screen-reader semantics partial vs the design a11y table.** The combined live-region announcement (P-5, natural-speech duration + outcome word) satisfies AC-C13 and covers the primary SR experience, but element-by-element traversal still hits the raw "02:47" `Text` (no `Semantics` value override), the `CharacterAvatar` image carries no label ("Profile picture of [Name]" per the design), the `LinearProgressIndicator` gets default semantics (not "Achievement progress: N percent"), and the container label is traversed ALONGSIDE the child text nodes (duplication — `excludeSemantics` would fix that but kill the element traversal the design wants; the clean pattern is a separate zero-size live-region node for the announcement + per-element labels). File: `client/lib/features/call/views/call_ended_screen.dart:397-406` + `widgets/character_avatar.dart`. Post-MVP a11y polish. (code review of story-7.2, blind + auditor)
- **🟢 Envelope `survival_pct` (patience-ratio formula) is now fully unread by the client — retire or unify.** Story 7.2 Decision B computes the overlay % client-side (`floor(passed/total*100)`, == the 7.3 debrief formula) and never reads `data['survival_pct']`, whose patience-meter formula differs AND which is absent on `user_hung_up`. The field now rides every `call_end` envelope with zero consumers — two coexisting "survival %" definitions for the same call is drift waiting for a future reader. When Story 7.3 lands, either retire the envelope field or re-emit it with the debrief formula. Files: `server/pipeline/patience_tracker.py:1327-1357` (envelope payload). Pre-existing wire field. (code review of story-7.2, blind)

## Deferred from: code review of 7-3-build-debrief-screen (2026-06-12)

_Formal 3-layer adversarial review of the merged + smoke-gated debrief screen (commit `e7e2f0e`). Zero AC violations (AC1-AC10 all satisfied); 6 patch items applied separately. These two are real-but-non-actionable-now._

- **🟢 BS-7 resume-poll treats ANY non-NOT_READY error as terminal within the budget.** One transient network blip (wifi-to-5G handover, radio wake) during the 30 s window permanently lands "Debrief unavailable" even though the debrief exists seconds later -- the remaining ~27 s of budget go unused. Spec'd behavior (story Task 3.2 "any other exception -> unavailable", mirroring the reviewed 7.2 overlay pattern), so NOT a 7.3 defect. Improvement when touched: re-arm within the budget on transient `ApiException` classes (network/timeout), keep true HTTP 4xx/5xx terminal. Belongs with the pre-MVP connection-resilience bundle (audio jitter + interim-speech items). File: `client/lib/features/debrief/views/debrief_screen.dart:156-178`. (code review of story-7.3, edge-case)
- **🟢 Debrief screen a11y is the Task 2.12 subset -- the design doc's full announcement scheme is unimplemented.** debrief-screen-design.md "Screen Reader Announcements" additionally specifies an on-appear live-region announcement ("Debrief. Survival rate: N percent. ..."), composed per-card announcements ("Error: You said [...]. Correct form: [...]"), a "Longest pause:" label on the first hesitation card, and (blind-layer add) announcing the BS-7 loading->content/unavailable phase change. Implemented today: labeled back button, hero % label, header-flagged section titles, linear order, "Survival Rate" caption de-duplicated from semantics (review P5). Fold the full scheme into Story 7.5's debrief overhaul rather than polishing a screen 7.5 will rework -- same class as the 7.2 "element-level semantics partial" entry above. File: `client/lib/features/debrief/views/debrief_screen.dart`. (code review of story-7.3, blind + auditor)

## Deferred from: code review of 7-4-build-pre-scenario-briefing-display (2026-06-12)

_Formal 3-layer adversarial review of the dev+deploy slice (`dfdf1a2..5220826`). 8 patch items applied separately (canonical-keys `hasBriefingContent`, hairline stale-capture fix, dead controller removal, doc fix, 4 test gaps incl. the 360×800 viewport contract proven under real Inter fonts). This one is real-but-pre-existing._

- **🟢 Malformed 2xx `/calls/initiate` envelope crashes past the `ApiException`-only catch — tap silently dead.** `_startCall` (extracted unchanged from the Story 6.1 `_onCallTap`) only catches `ApiException`; a 200 response whose `data` is null/non-map or wrong-typed would raise a `TypeError` inside `initiateCall`'s parse that escapes as an uncaught async error — no toast, no navigation, the tap just does nothing. Requires a server-side contract bug to trigger (the envelope is Pydantic-built by our own server), which is why it has never fired. Fix sketch if it matters: a trailing `catch (_)` mapping to the generic red `AppToast` (same fallback as unknown `ApiException` codes), or harden `CallSession.fromJson` to throw `ApiException(code: 'MALFORMED_RESPONSE')`. File: `client/lib/features/scenarios/views/scenario_list_screen.dart` (`_startCall` try block). Same client-resilience family as the auth-401 interceptor item. (code review of story-7.4, edge-case layer)

## Deferred from: code review of 7-5-overhaul-debrief-report (server, 2026-06-15)

_Formal 3-layer adversarial review of the server (Python) slice (`74002f6..HEAD -- server/`, 15 files +1408/-315). Client + native are a separate pass. 1 decision-needed + 6 patch items handled in the story file; these two are real-but-not-actionable-now._

- **🟡 `_MAX_TOKENS=3072` headroom vs the v2 worst-case debrief.** The v2 output is much larger than v1 (5 errors each with `explanation` + up to 2 `examples`, 3 areas each with a ~900-char `practice_prompt` ≈ 675 tokens of prompts alone, plus `better_phrasings`, idioms, hesitation contexts). A maxed-out debrief could approach or exceed 3072 tokens → `finish_reason=="length"` → the JSON truncates → no debrief is stored (graceful: GET serves `DEBRIEF_NOT_READY`, logged distinctly). Not reachable to confirm from code — needs real-call measurement. ACTION: per AC9 (<5 s target / 10 s ceiling), measure generation token usage + truncation rate on the VPS over real calls and raise `_MAX_TOKENS` if truncations appear. File: `server/pipeline/debrief_generator.py:58`. (code review of story-7.5, blind + auditor layers)
- **🟢 AC10 deferred-work retirement entries never recorded.** AC10 names the retirement of the F2 (end-reason) / F3 (per-item salvage) residuals + the Story 7.1 hesitation-approximation / by-index-pairing residuals as a completion condition; no such "retired" annotation exists in this file. Bookkeeping only — the code that retires them shipped. ACTION: annotate the corresponding Story 7.1 deferred entries as retired-by-7.5. (code review of story-7.5, acceptance-auditor layer)
- **🟢 Device-authoritative hesitation measurement = follow-up story (Walid-ratified 2026-06-15).** 7.5 ships the server hybrid (HesitationObserver re-anchored on the client `playback_idle` signal). The device path (`DeviceHesitationCollector` + `merge_hesitation_sources`) is built but DORMANT because the client onset detector UNDER-produces (seed-window contamination → misses gaps); wiring the merge as-is would regress coverage. ACTION (next story, client-first): fix the onset detector in `client/lib/features/call/services/hesitation_meter.dart` + `client/android/.../AudioCaptureChannel.kt` → wire `merge_hesitation_sources` at `bot.py` teardown → Pixel 9 smoke. Rationale: device = network-immune timing (the right long-term answer), but only pays off on bad connections; the hybrid is already good on good connections. (code review of story-7.5, decision resolution) — **✅ RETIRED by Story 7.6 (2026-06-16):** the device path is now reliable (native bounded-retry tap + `channelCount` RMS + bounded delivery; Dart gap-clamp / `disarm()` accumulator reset / `minGap` 4 s / seed-from-ambient) and `merge_hesitation_sources` is WIRED at `bot.py` teardown (device-authoritative). Pixel 9 smoke PASSED (calls 307/308 within ±0.5 s, `source:"device"`, noise money-test held). All 9 client-review specifics below are addressed; residuals surfaced by 7.6's OWN code review live in the 7.6 section at the end of this file.
  - **Client-review 2026-06-15 specifics for the above follow-up** (from the Group 2 code review): (1) **LIKELY root cause of under-production** — `AudioCaptureChannel.kt::tryAttachCallback` fails soft with NO retry when `sharedSingleton`/`recordSamplesReadyCallbackAdapter` are still null at `onListen` (record path not warm yet) → the meter gets zero frames for the whole call, silently; add a bounded retry. (2) `hesitation_meter.dart` gap math is inflated by the confirmation offset and has no negative clamp. (3) Kotlin `rmsOf` ignores `channelCount` (stereo skews SNR). (4) native RMS `mainHandler.post` floods the main thread at 100 Hz + races `onCancel`. (5) `disarm()` doesn't reset seed/floor accumulators. (6) `call_screen.dart` mic channel silently drops non-`num` events with no log. (7) `_kRestVisemeId=0` is a magic constant coupled to the viseme scheme. (8) tests `seedFloor`/`arm-idempotent` assert by timing-luck, not logic — re-pin the seed boundary. (9) decide: gate the whole client machinery OFF in prod vs leave running as the foundation.

## Deferred from: code review of 7-6-device-authoritative-hesitation-measurement (2026-06-16)

_Formal 3-layer adversarial `/bmad-code-review` of the merged + smoke-gated device-authoritative hesitation change (`89eb714..HEAD`, 10 code/test files). **Acceptance Auditor: all 10 ACs SATISFIED, no undeclared deviations, locked decisions + CLAUDE.md traps respected.** Gates green (client analyze clean + flutter 553; server ruff/format clean + pytest 908). 1 decision-needed (in the story file: per-CALL-vs-per-TURN merge) + these 7 deferred; 0 patch; 9 dismissed. The 7.5 device-authoritative follow-up entry above is now ✅ RETIRED by this story._

- **✅ Device-authoritative merge made PER-TURN — RESOLVED by this review (Walid chose to fix, 2026-06-16).** The earlier per-CALL merge dropped EVERY resolved server gap the instant the device produced any gap, silently losing an accurate server measure for a turn the device missed (a noisy / censored pause on a multi-pause call) — the censored-sentinel's "server covers this turn" intent was defeated whenever the device caught any other turn. FIXED: `merge_hesitation_sources` now keys each turn by the preceding character line (`_line_key`, stripped + case-folded; both sources snapshot it from the SAME shared `TranscriptCollector`) — the device gap wins for turns it measured, the server covers every OTHER turn (its UNRESOLVED C2 freezes + any RESOLVED turn the device missed). +2 regression tests; `bot.py` untouched (same call shape). Narrow residual: two turns with an IDENTICAL character line share a key (the only per-turn join both sources carry) — far smaller than the per-call drop. [`server/pipeline/device_hesitation_collector.py`] (edge-case layer)
- **🟢 Seeding has no max-gap timeout.** If mic frames stall DURING the seed window, the meter never reaches the listening branch (where the `maxGap` censor lives) and stays armed without ever censoring [`client/lib/features/call/services/hesitation_meter.dart:165-186`]. Benign alone (the next `disarm()` on the following character turn resets it), but it means the "the meter always emits a signal (measured or censored)" property only holds for a stall during LISTENING, not during SEEDING — a contributor to the per-turn coverage gap above. (edge-case layer)
- **🟢 `confirmationOffset` (600 ms) ↔ viseme silence window coupled "by construction" but UNENFORCED.** The meter default and `VisemeScheduler._kDefaultSilenceConfirmation` are two independent constants in two files; the code documents that they MUST be equal but nothing binds them — no shared constant, no contract test. A future viseme-timing retune not mirrored here silently biases EVERY measured gap (exactly the 1700 ms over-correction class the story just recovered from). FIX sketch: a shared exported constant or a source-text contract test (mirrors `server/CLAUDE.md §1` BSF-direction guard). [`client/lib/features/call/services/hesitation_meter.dart:79-89`] (edge-case layer)
- **🟢 No JVM/Kotlin unit coverage for `AudioCaptureChannel`.** `rmsOf` is pure arithmetic (mono identity, stereo interleaved→mono averaging, partial/short-buffer→0.0) with no Android deps, and a JVM test harness already exists (`FftTest.kt`, `FormantVisemeAnalyzerTest.kt`), yet the retry lifecycle, the `channelCount` averaging, the bounded delivery, and the cross-subscription reset are validated ONLY on the Pixel 9 gate (which is mono). At minimum the stereo `rmsOf` (needs visibility) deserves a JVM unit test. [`client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioCaptureChannel.kt:267-299`] (edge-case + blind layers)
- **🟢 Native cross-subscription hygiene.** `pendingPeakRms` is not reset on `onListen`/`onCancel`/`detach` — when `removeCallbacksAndMessages` cancels a not-yet-run `deliverRunnable`, a stale peak survives into the next subscription's first delivery (masked today: the Dart meter is idle at call start, so the stale frame is no-op'd before the meter ever arms). Separately, `deliverScheduled.set(false)` sits OUTSIDE `rmsLock`, so a frame landing between the peak-drain and the flag-reset leaves a buffered peak with nothing scheduled until the next frame (self-heals at ~100 Hz; only "stuck" on the final frame before capture stops, where it's abandoned anyway). Both behavior-neutral on the validated happy path; bundle the one-line `pendingPeakRms = 0.0` resets into the next native pass. [`client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioCaptureChannel.kt:89-137`] (edge-case + blind layers)
- **🟢 `onsetRmsFromEvent` validates type, not domain.** The seam rejects a non-`num` event (the AC8 contract-break path) but lets a NaN/Inf/negative `num` through to the meter [`client/lib/features/call/views/call_screen.dart:444`]. Inert today — native `rmsOf` returns `sqrt(≥0)` (always finite ≥0) and the server collector independently re-guards `math.isfinite` — so it only matters under a FUTURE native contract break, which is what the seam exists to surface. FIX sketch: `event is num && event.isFinite && event >= 0 ? event.toDouble() : null` + 3 test assertions in the existing `call_screen_onset_rms_test.dart`. (edge-case layer)
- **🟢 `postDelayed` attach-retry can register a callback just after `onCancel`.** A retry runnable already executing on the main thread when `onCancel` fires can complete an `ATTACHED` attach for a now-null `eventSink`, leaving a record callback registered on the WebRTC adapter until the next `detachCallback` (frames are dropped meanwhile via the `eventSink` null-check, so fail-soft). Tight thread-timing window; guard with an `eventSink`/cancelled re-check after `attemptAttach()` returns ATTACHED. [`client/android/app/src/main/kotlin/com/surviveTheTalk/client/AudioCaptureChannel.kt:148-187`] (edge-case layer)
- **🟢 Tests pin the seed window at 200 ms; production ships 250 ms.** The meter constructor defaults `seedWindow` to 250 ms but every test (`makeMeter` / `seedFloor`) uses 200 ms. The seed-completion logic is window-agnostic (`now - _armRealTime >= _seedWindowMs`), so behavior is identical — but the shipped constant is never the one exercised. Trivial fidelity gap; align the test default if touched. [`client/test/features/call/services/hesitation_meter_test.dart:18,51`] (edge-case layer)

## Deferred from: code review of 8-1-integrate-storekit-2-and-google-play-billing (2026-06-17)

_Formal 9-auditor adversarial `/bmad-code-review` of the dev-story commit (`afca2ca`, 31 code/test files +2668/-70). AC5 gates re-run green (server pytest 934 / client flutter 567). 1 BLOCKER (F1, in the story file as a patch) + a concurrency/security/correctness cluster (also patches) + 2 decision-needed (F7 account-binding scope, F13 restore) + these defers. Story 8.1 is "purchase plumbing only" (8.2 = paywall UI, 8.3 = full tier-enforcement/cancellation); the on-device gate is BLOCKED on D4 store setup + Story 10-4 iOS pipeline, so it stays `review` regardless._

- **🟡 F4-func — No app-lifetime `purchaseStream` listener.** An interrupted / Ask-to-Buy / next-launch re-delivered purchase is verified only if the paywall sheet (and its bloc) happens to be open — the classic "charged but tier never flipped" hole. This is the dev's DECLARED deviation #2 (knowingly accepted for the minimal 8.1 surface). ACTION (8.2/8.3): mount a long-lived app-scope `purchaseStream` listener in `bootstrap()` before `runApp` that verifies + completes any re-delivered `purchased`/`restored` transaction independent of paywall state (the `EndCallRetryService` app-singleton in `main.dart` is the existing precedent). [`client/lib/features/subscription/bloc/subscription_bloc.dart`] (edge-case + flutter + acceptance layers)
- **🟡 F11 — Google valid path never cross-checks `expiryTime` against now.** `validate_google` returns `valid` purely on `subscriptionState ∈ {ACTIVE, IN_GRACE_PERIOD}`; unlike the Apple path (which rejects `expiresDate <= now`), an ACTIVE state with an already-past `expiryTime` (clock skew, stale cached state, a token re-validated by the background sweep long after expiry) stays paid. Narrow today; matters once 8.3 enforces on `expires_at`. ACTION (8.3): after the state check, also reject when `expires_at` is non-null and parses to `<= now`, mirroring the Apple guard — especially on the revalidation sweep. [`server/billing/google_validator.py:166-179`] (edge-case layer)
- **🟢 F12 — Google `expiryTime` selected by lexicographic `max()` on RFC3339 strings.** `max(expiries)` over raw strings is lexicographic, not chronological, and Google's `expiryTime` can carry variable fractional-second precision. Bounded today (`expires_at` is only stored/echoed in 8.1; valid/invalid is driven by `subscriptionState`). ACTION (8.3, before any expiry enforcement): parse to `datetime` before comparing, or take the matching line item's `expiryTime` directly. [`server/billing/google_validator.py:159-164`] (crypto-validator layer)
- **🟢 F16 — `restored` stream updates auto-flip tier / pop the sheet without a user tap.** The bloc treats `PurchaseStatus.restored` identically to `purchased`; a stale `restored` event for the product arriving while the sheet is open pops it with `true` and triggers a scenario reload as if a fresh purchase completed. Mostly benign (server re-verifies). ACTION (8.2): gate stream-update→completion on having sent `buy()` this session, or distinguish `restored` from `purchased`. [`client/lib/features/subscription/bloc/subscription_bloc.dart:100-103`] (blind layer)
- **🟢 F17 — A `pending` purchase parks the paywall on a perpetual spinner.** On `PurchaseStatus.pending` the bloc cancels the recovery timeout and stays in `SubscriptionLoading` with no progress copy and no in-sheet recovery (iOS Ask-to-Buy / Android deferred-card can persist for minutes–days). Bounded — the modal is dismissible. ACTION (8.2): emit a distinct awaiting-approval state ("Waiting for approval — you can close this"). [`client/lib/features/subscription/bloc/subscription_bloc.dart:96-99`] (flutter layer)
- **🟢 F18 — Lifespan shutdown swallows `CancelledError` per-task.** If the lifespan coroutine is itself cancelled while awaiting the first background task, `except asyncio.CancelledError: pass` continues the loop and the second task (subscription_revalidator) may be neither awaited nor cancelled (a "Task was destroyed but it is pending" warning). No resource leak — `stop_event.set()` makes it exit on its next wait. ACTION: `asyncio.gather(..., return_exceptions=True)` under a single `wait_for`, or re-raise `CancelledError` after the loop. [`server/api/app.py:194-212`] (async-lifespan layer)
- **🟢 F15 — Client `_platform` maps every non-iOS target to `'android'`.** Harmless for the iOS+Android shipping target, but an implicit assumption with no guard (a non-iOS non-Android target would send `'android'` and run the Google validator on a non-Play artifact). ACTION: assert `defaultTargetPlatform ∈ {iOS, android}` before allowing a purchase. [`client/lib/features/subscription/bloc/subscription_bloc.dart:47-48`] (acceptance layer)
- **🟠 F13 — No `restorePurchases` path (Walid-deferred to 8.2, 2026-06-17). ⚠️ MUST-DO BEFORE iOS SUBMISSION.** `InAppPurchaseService` exposes no restore, so a paying subscriber who reinstalls / switches device cannot recover paid access, and **Apple App Review REQUIRES a visible "Restore Purchases" affordance for auto-renewable subscriptions** — shipping without one risks rejection at the Story 10-4 iOS gate. The verify path is already restore-ready (the bloc treats `PurchaseStatus.restored` like `purchased`); only the trigger + UI control are missing. ACTION (8.2 paywall UI): add `restore() => _iap.restorePurchases()` to `InAppPurchaseService` + a "Restore Purchases" control on the paywall, paired with the F4-func app-lifetime listener. [`client/lib/features/subscription/services/in_app_purchase_service.dart`] (edge-case layer)

## Deferred from: code review of 8-2-build-paywall-screen-with-invisible-tier-design (2026-06-17)

_Formal 3-layer adversarial `/bmad-code-review` of the dev-story commit (`a19deee`, 16 client files +1172/-176). Gates re-run green (flutter analyze clean / flutter test 598). All 9 ACs + D1–D5 satisfied; 2 decision-needed + 3 patch handled in the story file. Continuity with the 8.1 review: F13 is now CLOSED (Restore shipped, D2); F16 (restored auto-flip) is now intentional for the restore feature; F17 (pending → perpetual spinner) was NOT picked up by 8.2 and remains open for 8.3. These two are the new defers:_

- **🟢 Restore 3s lapse timer can flash "Nothing to restore." before a slow restore lands.** `_onRestorePressed` arms a fixed `restoreTimeout` (default 3s) → `RestoreLapsed` → `SubscriptionRestoreEmpty`; a genuine `restored`/`purchased` entitlement landing at >3s (slow link) still flows through `_verifyAndComplete` → `SubscriptionPurchased`, so F16 holds (never a fake success) — the only artifact is a transient wrong-then-corrected caption. 3s is aggressive for a store restore round-trip on cellular. Not actionable until the store path is testable on-device (Google Play account = 8.1 D4; iOS = Story 10-4). ACTION: re-tune `restoreTimeout` (or gate `RestoreEmpty` on the `restore()` future completing) once measurable on-device. [`client/lib/features/subscription/bloc/subscription_bloc.dart:47, 125`] (blind+edge+auditor layers)
- **🟢 "Not now" / "Restore purchases" are two full-width 48px text buttons 8px apart, same muted styling.** Mis-tap risk between the dismiss and the restore action; both outcomes are recoverable (mis-tapping Restore as a non-subscriber just shows "Nothing to restore."; mis-tapping Not now dismisses). The binding `paywall-screen-design.md` predates the D2 Restore affordance, so its placement/spacing/visual weight were never specified. ACTION: revisit spacing / visual differentiation (or move Restore to a less collision-prone slot) when the restore path is testable on-device. [`client/lib/features/paywall/views/paywall_sheet.dart:326-348`] (blind layer)

## Deferred from: Story 8.2 on-device gate (2026-06-18)

_The code-review D2 patch (block swipe/scrim dismiss during Loading/Success via a custom modal route) was REVERTED after the Pixel 9 gate — the hand-rolled drag-to-dismiss regressed the native swipe feel (handle-only, fling-velocity-gated, no finger-follow: Walid found swipe-down dead while scrim-tap + "Not now" worked). Reverted to native `showModalBottomSheet` (swipe/scrim/scroll native again); `PopScope` still gates BACK during Loading/Success. D1/FR29 PASSED the same gate (paywall auto-presented on the 3rd free call)._

- **🟡 Block the SWIPE/SCRIM dismiss during an active purchase (Loading/Success).** `showModalBottomSheet`'s drag-dismiss calls `Navigator.pop` directly (bypasses `PopScope`), so `canPop` can't block the swipe — only the back button + (via `maybePop`) the scrim are gated. Today a swipe during the ~1.5s Loading/Success window can dismiss the sheet (self-healing: the tier flips server-side and `/scenarios` reloads next visit; during Loading it's the same "charged but not flipped" window already tracked as 8.1 F4-func). The clean fix is a custom route using the framework `BottomSheet` widget inside a `BlocBuilder` (reactive `enableDrag` via rebuild — NOT a hand-rolled drag), which keeps native drag feel AND gates it by state. ACTION: re-attempt when the real-purchase path is testable on-device (Google Play account = 8.1 D4; iOS = Story 10-4), so the blocked-during-purchase behavior can actually be verified with a live Loading/Success. [`client/lib/features/paywall/views/paywall_sheet.dart`] (blind+edge+auditor layers; reverted per on-device gate)

## Deferred from: code review of 9-1-build-local-cache-with-sqflite-for-scenarios-and-debriefs (2026-06-19)

_Formal 6-lens adversarial `/bmad-code-review` (Opus 4.8, 31 agents, 12 raw → 8 distinct → 7 confirmed). Gates independently re-run green (flutter analyze clean / flutter test 667). 1 HIGH decision-needed (F1 cross-user cache leak via natural token expiry) + 2 patch (F3 refresh-guard, F8 cache-miss CTA icon) handled in the story file; 1 dismissed (F6 unreachable). Story STAYS `review` — the HIGH privacy finding must be resolved before `review → done`. These are the defers:_

- **🟢 F4 — A single corrupt `cache_meta` row (or one bad scenario row) discards the ENTIRE offline scenario cache.** `readScenarios()` wraps the row-by-row `Scenario.fromJson` rebuild AND `CallUsage.fromMeta(rawMeta)` in one try/catch and returns null on any failure, so a bad meta blob nukes perfectly-valid scenario rows → offline user drops to the no-network error screen instead of the last-known hub (degrades AC1/AC3 on partial corruption). Documented "treat as cache-miss" design (dartdoc), and writes are transactional so meta can't desync from rows within one app version — only reachable via cross-version schema drift, and self-heals on the next online fetch. ACTION (when 9.2 bumps the schema): split into separate try/catch blocks so a bad meta falls back to a sentinel/conservative `CallUsage` while still rendering cached scenarios offline. [`client/lib/core/local_cache/scenario_cache_store.dart:33-68`] (blind layer)
- **🟢 F5 — An empty-but-valid `/scenarios` list is read back as a cache-miss.** `writeScenarios` persists zero scenario rows + valid meta for a `data:[]` payload, but `readScenarios` returns null on `rows.isEmpty` BEFORE looking at meta — so a cached empty list is indistinguishable from a never-written cache (offline → error screen instead of an empty hub; the cached meta/budget is unreachable). Not reachable today: `routes_scenarios.py` returns the full catalog regardless of tier, so `data:[]` is never produced. ACTION (before any server-side tier filtering / A-B catalog): treat meta-present + zero rows as a valid empty result (gate on the `scenarios_usage` / `scenarios_synced_at` meta key); add an empty-list round-trip test. [`client/lib/core/local_cache/scenario_cache_store.dart:36-37`] (edge + persistence layers)
- **🟢 F-extra-A — `cached_debriefs` grows unbounded.** Every completed call writes a new row (unique `call_id`), never pruned, while only the latest-per-scenario (`readLatestForScenario`) is ever read. Negligible storage for MVP (small JSON), but monotonic growth over a heavy user's lifetime. ACTION (future): prune to the latest N rows per `scenario_id` (or delete older rows on write). [`client/lib/core/local_cache/debrief_cache_store.dart`] (reviewer independent)
- **🟢 F-extra-B — No `onDowngrade` handler on the cache DB.** `AppDatabase.open()` sets `onCreate`/`onUpgrade` but no `onDowngrade`; sqflite's default throws if the on-disk version is HIGHER than the requested version. Impossible at `version:1`, and `bootstrap()` opens the DB fail-soft (degrades to network-only, never crashes the app), so harmless today. ACTION (when 9.2 bumps to v2): add `onDowngrade: onDatabaseDowngradeDelete` so a user installing an older APK over a newer cache cleanly resets rather than relying on the bootstrap catch. [`client/lib/core/local_cache/app_database.dart:36-43`] (reviewer independent)
